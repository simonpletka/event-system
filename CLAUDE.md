# Event system — working notes

Internal web app for an event agency: events from inquiry to invoicing, finance (quotes/invoices/expenses/reports), time tracking. Source of truth for requirements: `docs/system_info/event-system-zadani.pdf` (Czech brief, 17 Aug 2026). Visual/UX source of truth: `docs/wireframes/` (see `docs/wireframes/README.md` for the screen index and decisions already made there — don't re-derive those, they're settled).

Build plan for the current phase lives at `/Users/simonpletka/.claude/plans/tingly-popping-hippo.md`.

**Terminology note (2026-09-02): "Event" was renamed to "Project" throughout the live app** — the Prisma model, every FK/field (`eventId`→`projectId`, `EventMember`→`ProjectMember`, `EventContact`→`ProjectContact`, `MeetingEvent`→`MeetingProject`, `EventsAccess`→`ProjectsAccess`, etc.), routes (`/events`→`/projects`), all UI text/dictionary keys, and file/component names (`EventForm`→`ProjectForm`, `lib/actions/events.ts`→`lib/actions/projects.ts`, etc.). `InvoiceEvent`/`InvoiceEventType` are unaffected — that "event" is an unrelated audit-log-entry concept, not the renamed entity. The history below predates the rename and still says "Event"/"event" throughout (a changelog, left as period-accurate — like git commit messages, not retroactively edited); when it names a specific field/function/route, mentally substitute the Project-era name above rather than trusting the literal string. The Notion integration's *external* database (built before the rename) is still literally named/structured as "Events" in the user's Notion workspace — `NOTION_PROJECTS_DATABASE_ID` is the correctly-renamed env var pointing at it, but the Notion-side property names were deliberately left alone (renaming a live external workspace wasn't in scope).

## Status

**Phases 1–3: done.** Foundation/design system/auth/Dashboard/Events (Phase 1); Finance — Quotes/Invoices/Expenses/Reports, full RBAC (Phase 2); Time tracker + Compare Events + Settings/Users/Company (Phase 3). Dev login (seeded by `prisma/seed.ts`, password `changeme123` for all): `admin@eventsystem.cz`, `eva.kucerova@eventsystem.cz` (Accountant), `jan.novak@eventsystem.cz` (Producer), `m.dvorak@eventsystem.cz` (Member). **Re-login after every `prisma db seed`** — seeding wipes/recreates all rows with new IDs, so a stale session cookie silently points at users/events that no longer exist (symptom: empty lists, no error).

**Everything below Phase 3 was built incrementally via direct user feedback**, roughly in this order (grouped by theme, not chronological — see git history for exact sequencing):

- **Polish pass**: hold-to-reveal password field on login, Cancel button on every create/edit form, bigger back-links, "+ New event" quick-create modal wired into Quote/Invoice/Expense pickers, Dashboard Timeline + Calendar views, Events Calendar week view (shared `WeekCalendar`/`WeekNav`).
- **Custom roles**: Admin can define roles with configurable reach across 4 dimensions (Events/Finance/Expenses/Settings) × the same tiers built-ins use. `authz.ts` centers on one `resolvePermissions(user)` — every access check reads from it, so built-in and custom roles can't drift apart. User management gained edit-info, deactivate/reactivate (`User.active`, re-checked on every request via `requireUser()`, not just login).
- **ARES integration** (Czech business registry lookup) wired into Event/Client/Company forms. Font swapped to self-hosted **Neue Regrade** (commercial, licensed) via `next/font/local`.
- **Admin-only hard delete** for Events/Quotes/Invoices/Expenses (`isAdmin()` checks literal built-in `role==="ADMIN"`, not a permission tier — a CustomRole with equivalent access never gets it). Every destructive action gated behind an in-app confirm dialog (`ConfirmDialogProvider.tsx`, replacing native `confirm()`/`alert()`). Deleting an event is **blocked** while it has expenses or invoices (checked client- and server-side).
- **Address/geo**: worldwide address autocomplete via **Photon** (komoot's free OSM-based API, chosen over the Czech-only RÚIAN registry). `Client` later gained a structured 4-field address (street/city/postCode/state) with its own `StructuredAddressInput`; Event/Company kept single-string addresses (different use case).
- **Per-document currency** (CZK/EUR/USD) on Quote/Invoice; no FX conversion anywhere — aggregates (Reports, Dashboard, Client "total charged") sum raw amounts across currencies, labeled CZK, a deliberate simplification. QR "Platba" payment code is CZK-only, skipped otherwise.
- **Clients section** (`Client`/`ClientContact`, added going forward only — existing events never auto-linked). `EventContact` (1+ per event) syncs onto the linked Client's contacts on save (deduped, independent copies — no FK back).
- **Quotes got a real detail page + PDF** (previously list/new/edit only). Shared `src/lib/pdf/shared.tsx`/`build-invoice-pdf.tsx`/`build-quote-pdf.tsx` factor out font/QR/render logic so routes, email, and Drive export all share one implementation.
- **Line items**: category field (free string, not FK) + manual reorder; `hideItemPrices` mode (default true) groups items by category as headings with no price columns, for both quotes and invoices. Settings → Templates manages the default category list.
- **Invoice discounts** (`discountType`/`discountValue`, invoice-only) applied pre-VAT with VAT recomputed on the discounted base — the accounting-correct order.
- **Unified `YY-XXX` numbering**: `Event.number` assigned at creation as the lowest unused integer per year (reusable after deletion); quotes/invoices reuse their event's number, appending `_v2`/`_v3`… only past the first document of that type.
- **Revert "mark as paid"**: recomputes `amountPaid` from real `Payment` rows, never wipes a genuine payment.
- **Quote lifecycle**: accept/decline (SENT-only transitions), duplicate a DECLINED quote into a fresh DRAFT, creator info (`Quote.createdById` + `User.phone`).
- **"Glass & gradient" redesign** (supersedes the original Phase-1 "0 border-radius" decision) across all six areas — Dashboard, Events, Clients, Finance, Time tracker, Settings: rounded corners, `--color-positive` (teal)/`--color-warning` (red) semantic tokens replacing ad-hoc pill colors, `.card`/`.glass-panel` classes, shared `SegmentedTabs` pill switcher, gradient page background. `.glass-panel` needs a tinted backing layer under the blur, not just blur — sharp text stays readable through blur alone.
- **Time tracker overhaul**: Timer/Manual toggle, real day/week/month navigation (`?date=` anchor), Calendar view (`TimeTrackerCalendar`, mirrors `WeekCalendar`'s overlap logic). Manual entries now persist real `startedAt`/`endedAt`, not just a minute count. "Compare events" was later **replaced** with **Overview** — Day/Week/Month person-time breakdown with a People filter, no CSV export (out of scope).
- **App-wide color customization + language switch**: `CompanySettings` gained 6 color columns injected as CSS vars on `<html>` (repaints the whole app incl. `/login`); `dictionary.ts`/`i18n.ts` split (EN/CZ) — **`dictionary.ts` has zero server imports and is the only thing a Client Component may import from** (see gotchas). Later made **per-account** (`User.locale`, overrides company default) with a Settings → General tab open to every role (name/email/phone/avatar/password/language) — the first Settings tab that doesn't require Finance/Users/Company permissions.
- **Mobile-responsive implementation** (`md:` 768px cutover): floating glass bottom tab bar (`MobileNav`), `MobileTopBar`, `MobileListRow` for stacked-card lists, calendar day-strip+agenda view, `LineItemsFields` mobile card variant (own inputs must NOT share `name=` with the desktop grid — both stay mounted, `FormData.getAll` would double every line item otherwise). iOS Capacitor shell needed `contentInset: "never"` (was double-applying safe-area insets) and `overflow-x: clip` (see gotchas) to stop WKWebView horizontal rubber-banding.
- **Invoice/quote PDF redesign** matching an approved mockup pixel-for-pixel (mockup is A4 at 96dpi; PDF is A4 in points — every value is `px × 0.75`), dark theme (`shared.tsx` colors were still light-page after the first "redesign" pass — a real bug, since fixed), real bold via flattened static font instances (see gotchas), on-page PDF preview cards mirroring the same layout with a zoom-to-fit scaler. **ISDOC 6.0.2** e-invoice XML embedded as a PDF attachment (`src/lib/isdoc.ts`, schema pulled and read directly from isdoc.cz — do not trust memorized schema details) — discount reconciled by proportionally scaling pre-discount line items so `TaxExclusiveAmount`/`TaxInclusiveAmount` match `Invoice.total` exactly.
- **Invoice emailing** (SMTP via nodemailer — chosen over a transactional API) and **Google Drive auto-export** (service account, not OAuth) for paid invoices/accepted quotes/expense receipts into a `Root/<category>/<year>/<file>` tree. Both are best-effort/non-blocking: a real action (mark paid, accept quote, save expense) never fails because email/Drive isn't configured — both throw a clean `*NotConfiguredError` that's swallowed at the call site (only invoice-email gets a visible `DRIVE_EXPORT_FAILED`-style history/status surface; quotes/expenses fail silently server-side, logged only).
- **Session**: 8-hour fixed JWT expiry (not sliding) + a `lastEmail` cookie so login always prefills the email.
- **Calendar (`WeekCalendar`)**: drag-and-drop reschedule for milestones (time-of-day + day) and event-day bars (whole event shifts together, calendar-day arithmetic to survive DST), live drop-shadow ghost preview while dragging, red current-time line (only when the displayed week contains today), today's header cell gets a solid pill, waypoint icon + venue address as a second line on all-day bars, rounded corners on every item, hourly labels every hour (not every 2h), full 24h grid scrollable but defaulting to a 6–20 view.
- **Dashboard**: Balance (income/expense) chart **removed**, replaced with "My tracked time" — reuses `getOverviewData()` (the same function powering Time Tracker → Overview) for a single person/current week, clean 2-hour-increment axis. Latest expenses / My tracked time sit in a 3:2 grid split. Chart color palette (`chart-colors.ts`) is colorblind-validated, assigned by rank not cycled.
- **Time tracking without an event**: `TimeEntry.eventId` made nullable — start a timer or add a manual entry with no event, assign one later via a (newly) real editable event `<select>` on the entry-edit form. Follows the exact `Expense.eventId` nullable-FK precedent.
- Numerous smaller polish passes (table row/header sizing, sidebar text sizing — settled back near defaults after several back-and-forth rounds, section-title sizing, tag centering, button weights, filter dropdown double-arrow fix, notify/confirm dialog styling) — current visual state reflects the latest of each; check the component directly rather than trusting an exact px value here.
- **Role-based dashboard** (supersedes the single "one dashboard for everyone" page): `dashboard/page.tsx` is now a thin dispatcher — `dashboardVariant(user)` in `authz.ts` resolves permissions → `admin | accountant | producer | member`, each rendered by its own component in `components/dashboard/` (`DashboardShell` frame + shared `widgets.tsx`). Admin = company-wide stat tiles + `getMonthlyCashflow()` + upcoming events + calendar switcher; Producer = assigned events (`assignedEventWhere()`) with budget-health bars + "assigned to me" roadmap items + my tracked time + calendar; Accountant = overdue-invoice/awaiting-quote/ready-to-invoice lists + cashflow + expenses (no calendar/time); Member = read-only assigned events + own expenses + "Add expense" + my tracked time. The List/**Timeline**/Calendar switcher lost Timeline entirely (`getDashboardTimeline` deleted); Calendar stays for Admin + Producer only. Design approved in the "Dashboards by Role" artifact. Admin stat tiles are clickable (`StatTile` optional `href` → the matching filtered list, e.g. `/finance/invoices?bucket=overdue`). Two enabling changes: **(1)** `eventWhereForUser` now returns `{}` for the Member tier (`ASSIGNED_READ`) too — Members can open **any** event read-only (list/detail/calendar); only a custom role with events `NONE` is still narrowed to `{ members: { some } }`. Edit/finance/budget stay gated as before. **(2)** the event form gained a `TeamPicker` (chip toggle, same pattern as roadmap assignees) writing `EventMember` rows directly via create/update actions — previously the only way onto a team was creating the event or being handed a roadmap item.
- **Finance section admits expense-only roles**: `canViewExpenses(user)` (`expenses !== "NONE"`) gates the Expenses sub-tab independently of `canViewFinance` — so a Member (finance `NONE`, expenses `OWN_ONLY`) reaches `/finance/expenses` + `/finance/expenses/new` (own expenses only, scoped by `expenseWhereForUser`). `FinanceTabs` hides quotes/invoices/reports for them (renders nothing if only one tab); `/finance` and those three pages redirect expense-only users to `/finance/expenses`.
- **Sidebar quick-start timer + popup entry edit**: a one-click play button next to "No timer running" in the sidebar's `TimerWidget` starts an eventless timer without navigating to Time Tracker (reuses the existing `startTimerAction`, no new logic). The dedicated `/time-tracker/entries/[id]/edit` page is **gone** — editing (List view's "Edit" link, both Calendar view variants' clickable blocks) now opens `EditEntryButton`'s in-page `Modal` popup, pre-filled and closing itself via the established `useActionState` + `onSaved` pattern, no navigation. `EditEntryButton` takes `children`/`className`/`style`/`title` rather than a render-prop — the render-prop version hit the RSC function-prop bug immediately, since it's used from Server Component callers (`tracking/page.tsx`).
- **Meetings + one-way Notion sync**: new top-level `Meeting` model (Client/Internal, `MeetingEvent` many-to-many join to `Event` — same explicit-join-model convention as `EventMember`/`RoadmapAssignee`) with its own `/meetings` nav section (list + type filter, create/edit form, event multi-picker via `EventPicker`, same chip-toggle pattern as `TeamPicker`). Recurrence is **computed, not materialized** — `recurrenceFreq`/`recurrenceInterval`/`recurrenceUntil` on the row, expanded on demand by `expandMeetingOccurrences()` (`src/lib/meetings.ts`) for calendar display; editing a recurring Meeting edits the whole series, no per-occurrence edit/cancel. Gated by `canViewMeetings`/`canManageMeetings` (`authz.ts`) — no new `CustomRole` dimension, reuses the `events` tier the same way `canManageClients` composes existing dimensions. `WeekCalendar` renders meeting occurrences read-only (dashed accent boxes, no drag — ambiguous for a recurring series) alongside milestones; `getWeekCalendarData` now returns `{ events, meetings }` instead of a flat array. Also pushes a page to a company-wide **Notion** workspace on creation: an Admin-built "Events Dashboard" page (Table/Board/Calendar views on an Events database, plus separate Client Meetings/Internal Meetings databases each with a two-way `Events` relation) — `src/lib/notion.ts` mirrors `gdrive.ts`'s `*NotConfiguredError` + silent-swallow pattern exactly (`NOTION_API_KEY` + `NOTION_EVENTS_DATABASE_ID`/`NOTION_CLIENT_MEETINGS_DATABASE_ID`/`NOTION_INTERNAL_MEETINGS_DATABASE_ID`, all unset-safe). `Event.notionPageId` (nullable, set best-effort after a successful push) is what lets a later Meeting's Notion page populate its `Events` relation — an event synced before Notion was configured, or whose push failed, is just omitted from that relation rather than blocking the meeting. One-way only (app → Notion); no sync back.

- **Finance → Reports rebuilt as an accountant tool** (supersedes the old year-only `getReports` 3-view page). `getFinanceReport(user, {period, anchor, from, to, locale})` in `queries/finance.ts` replaces `getReports`; `src/lib/finance-period.ts` owns the `month|quarter|year|custom` period model (`resolveRange`/`reportBuckets`/`stepAnchor`/`rangeLabel` — deliberately *not* the time tracker's `day|week|month|year` enum). Page is period-selector + always-on KPI strip + 4 sub-tabs (`?tab=`): **Overview** (P&L excl. VAT, invoiced-vs-expenses bar chart reusing `MonthlyBarChart`, by-project, expenses by category, currency exposure), **VAT** (revenue by regime, output VAT by rate, input VAT by rate, net VAT position, EC Sales List / souhrnné hlášení — gated on `CompanySettings.isVatPayer`, captioned as an estimate), **Receivables** (aging buckets + outstanding-invoice list + period cash panel), **Documents** (issued-invoices + expenses tables, each with a CSV export via `?part=` on `/api/finance/reports/export`; `?print=all` stacks every section for the PDF hand-off). Accrual is primary (invoice `subtotal`→CZK), cash secondary (`Payment` rows in period, `overdue`/outstanding remainders). **VAT regime** is inferred from the customer DIČ prefix (`src/lib/vat-regime.ts` — `CZ…`=domestic, other EU prefix=reverse charge, else=export); a non-domestic invoice contributes 0 output VAT whatever its line rates say. **Per-rate VAT** comes from `src/lib/vat.ts` `vatBucketsForInvoice()`, extracted from the proportional-discount-scaling logic that was inline in `isdoc.ts` (now calls the shared helper — output byte-identical, verified). **`Expense` gained `vatRate`/`vatAmount`** (migration `add_vat_to_expense`): `amount` stays gross, `vatAmount` is deductible input VAT (0 = not reclaimable), net = `amount − vatAmount`. `ExpenseForm` auto-derives `vatAmount` from gross×rate (imperative refs, uncontrolled — remounts via a render-phase `resetKey` on save-and-add-another) until hand-edited. Seed `withTotal()` now also sets quote/invoice `subtotal` (was left 0 by the uncommitted subtotal work → old report showed 0 income); Munich Trade Fair project got `companyDic: "DE811128135"` + 0%-VAT items so the EU-reverse-charge path has demo data.

**Environment caveat**: for most of this project's history no browser tool was available, so most work was verified via clean `npm run build`/`npm run lint` + HTTP smoke tests (reconstructing React Server Action form fields and POSTing directly) rather than visually. A Chrome extension connection became available partway through and was used from then on for anything with real interaction/visual stakes. **Outstanding**: the iOS Capacitor shell fixes (`contentInset`, `overflow-x: clip`) have not been re-verified in the Simulator — needs a native rebuild (`npx cap sync ios` + fresh Xcode build) before trusting them.

**Deferred / not built**: Google Calendar sync, Google OAuth login, general audit log (beyond the invoice-specific `InvoiceEvent` log), PWA offline queue, expense approval workflow enforcement (field/hint text exist, nothing enforces it), a custom-styled date/time picker (native popup can't be recolored — `accent-color` only reaches the picker-indicator icon, not the popup's internal highlight; offered, user deferred), per-category expense icons (mapping already specified, just not implemented — see design backlog note in git history if needed), CSV export on Time Tracker → Overview, official DPH přiznání / kontrolní hlášení / souhrnné hlášení XML generation (Reports → VAT is a reconciliation estimate only), per-invoice manual VAT-regime override (DIČ-prefix inference only), VAT/currency on `Expense` beyond the deductible-amount field.

## Recurring gotchas (read before touching these areas again)

- **RSC serialization boundary**: a Server Component may never pass a plain function to a Client Component prop (`Functions cannot be passed directly to Client Components` — runtime error only, `next build`'s typecheck won't catch it). Hit repeatedly with parameterized dictionary strings (`t.foo.bar(count)`) and closures (`eventHref`). Fix: Client Components take a plain `locale: Locale` prop and call `getDictionary(locale)` themselves — **always from `@/lib/dictionary`, never `@/lib/i18n`** (the latter re-exports a server-only `getLocale()` that imports Prisma → `better-sqlite3`, which drags native Node bindings into the client bundle and fails the build outright, a distinct second-order version of the same bug).
- **Destructive-action confirmation**: never gate a server action behind `<form action={fn}> onSubmit={(e)=>{if(!confirm())e.preventDefault()}}` — this races React 19's internal form-action dispatch and doesn't reliably win. Use a plain `<button type="button" onClick={...}>` that calls `confirm()`/the app's own `ConfirmDialogProvider` synchronously first, then only on approval builds `FormData` and calls the imported action directly inside `startTransition()`.
- **CSS Cascade Layers**: Tailwind v4 utilities live in a layer; hand-written unlayered rules in `globals.css` (e.g. `.btno`, `.heading-label`, `.label`) always beat a same-specificity layered utility regardless of source order. Use the `!` important-modifier on the Tailwind class to win.
- **Dev server**: `pkill -f "next dev"` is unreliable and can leave a stale process silently serving an old build on port 3000 while a new one falls back to 3001. Use `lsof -ti:3000 | xargs kill -9`, then verify with `lsof -i:3000` before trusting what the browser shows. Separately: **a Prisma schema/client regen requires a full dev-server restart** — the in-memory client stays stale even though a fresh `npm run build` (a new process) looks clean.
- **react-pdf font registration**: `@react-pdf/font`'s `FontStore` caches a loaded font under its registered family name for the life of the Node process — registering once at module scope leaks subsetting state across unrelated `renderToBuffer()` calls (reproduced: rendering an invoice then a quote back-to-back corrupted specific digits in the second). Fix: call a `registerAppFont()` fresh inside each PDF component's render body, family name suffixed with a unique id per render.
- **Variable fonts in react-pdf**: fontkit doesn't interpolate a variable font's weight axis per the registered `fontWeight` key — it loads the file at its own default instance regardless, so registering one variable TTF under both 400 and 700 silently produces identical (never actually bold) glyphs. Fix: flatten real static instances (`fonttools varLib.instancer`) and register those. The live web app is unaffected — browsers do interpolate variable fonts correctly, so `next/font/local` still uses the original variable file.
- **Local dates**: never round-trip through `new Date(dateOnlyString)`/`toISOString().slice(0,10)` — both convert through UTC and can roll the date by one day depending on offset. Use the project's `isoDate()`/`parseIsoDate()` helpers (`src/lib/calendar.ts`).
- **Sticky page header** is `src/components/ui/PageHeader.tsx` (was 10 inline copies of the same `sticky top-0 -mx-6 … backdrop-blur` div). Must be a **direct child of the page's `<main>` box** so its content lines up with the page body. The frosted background is a **separate absolutely-positioned layer** at `left-[calc(100%_-_100vw)] w-screen` so the strip spans the whole viewport — *including behind the floating sidebar*, which is raised to `z-30` (above the header's `z-20`) so it floats on top of the strip. Put the page's `max-w-*` on a wrapper *inside* `PageHeader` and on the body — never wrap `PageHeader` itself in a `max-w`/`mx-auto` div (that was the bug that stopped the header spanning full width). `pb` is a prop, not `className` (two `pb-*` in one class list don't resolve by source order — cascade-layer gotcha below).
- **`overflow-x: hidden` on `html`/`body`** silently promotes the computed `overflow-y` to `auto` too (per spec, when only one axis is set to non-`visible`) — this broke the pinned sidebar's `position: sticky` (it now stuck to the wrong scroll container). Use `overflow-x: clip` instead — same visual clipping, doesn't establish a scroll container.
- **PDF mockup-to-page conversion**: the mockup canvas is 794×1123 CSS px = A4 at 96dpi; the PDF page is A4 in points (595.28pt). Every mockup pixel value converts via the literal `pt = px × 0.75` — treat it as an exact unit conversion, not an eyeballed guess, when matching a design mockup in `@react-pdf/renderer`.
- **ARES address**: use the `adresaDorucovaci` object (already split into `radekAdresy1/2/3`), not the flat `sidlo.textovaAdresa` one-line string, when a structured street/city/postCode split is needed.
- **`npm install` blocks postinstall scripts** (npm 11 allow-scripts) — run `npm approve-scripts <pkg>` after adding a dependency that needs one (Prisma, native modules).
- **Scripted-click verification with repeated button labels** (e.g. every list row has its own "Edit"/"Delete"): `document.querySelectorAll('button').find(text match)` returns the *first* DOM match, which is easy to mistake for "the one I meant" — cost a real, permanently-deleted seeded time entry once (had to be manually recreated). Scope the query to the specific row/dialog first (`.closest(...)` from a uniquely-texted leaf node, or a distinguishing class on the target button) before matching by text, especially for anything destructive.

## Data model decisions

- **`Quote.createdById`** (required FK) + **`User.phone`** — "Created by" reuses `issuedAt` as the creation date (already `@default(now())`, never edited).
- **`User.locale`** (nullable, "en"/"cs") overrides `CompanySettings.locale` when set. **`User.avatarPath`** mirrors `CompanySettings.logoPath`. No `firstName`/`lastName` columns — Settings → General splits/rejoins the single `name` field client-side since ~20 call sites already read `name` directly.
- `EventStatus` has the brief's 6 states **plus `CANCELLED`** (brief explicitly invites this).
- Event detail is tabbed (3c), not single-scroll (3d) — explicit user choice.
- **Numbering**: `Event.number` = `YY-XXX`, lowest unused integer per year at creation (reusable after deletion). Quotes/invoices reuse the event's number with `_v2`/`_v3`… suffixes past the first document of that type (`src/lib/document-number.ts`). Not concurrency-safe (no locking) — accepted tradeoff.
- **`EventContact`** (1+/event) syncs onto the linked `Client`'s `ClientContact` list on save — deduped by email-then-name, independent copies (no FK back). `Client.invoicingEmail` auto-fills from the first synced contact with an email, only while still blank.
- **`ItemCategory`** — org-wide defaults, deliberately *not* a FK on `QuoteItem`/`InvoiceItem.category` (plain string) so a one-off category never pollutes the global list and deleting a default never touches already-tagged items.
- **Invoice discounts** (`discountType: NONE|PERCENT|FIXED`, invoice-only) apply pre-VAT, VAT recomputed on the discounted base. `Invoice.total` stores the final post-discount amount.
- **`Quote.subtotal`/`Invoice.subtotal`** (pre-VAT base; an invoice's is post-discount) exist alongside `total` (VAT-inclusive) specifically so "value"/rollup figures can exclude VAT — Czech accounting treats VAT as a pass-through liability, not revenue. The documents themselves (PDF, detail page) still show the full VAT-inclusive `total` as always. The scope line: anything answering "how much is this worth / was invoiced" (`Project.quotedValue`, Client "total charged", Reports income/balance/margin, Quotes "open value", Project Finance tab "Invoiced") reads `subtotal`; anything answering "how much cash is owed/received" (invoice payment tracking — `amountPaid`, overdue totals, "paid this month") keeps reading `total`, since that's real money. Every excl-VAT figure in the UI carries an explicit "(excl. VAT)" label — never bare.
- **`InvoiceStatus` has no stored `OVERDUE`** — derived everywhere as `dueDate < now && status !== PAID`.
- **VAT**: `CompanySettings.isVatPayer` + per-line-item `vatRate` (default 21%).
- **`Currency`** (`CZK|EUR|USD`) on Quote/Invoice; individual documents/PDFs always show their own real billed currency, unconverted. Aggregates that sum across documents (Reports, Dashboard, Client "total charged", Finance KPIs) convert non-CZK amounts to CZK via `src/lib/fx.ts` — the Czech National Bank's daily fixing rate on each document's own `issuedAt` date, free/no-key, with a hardcoded fallback rate if the live fetch fails so a CNB outage never breaks a page. `isMixedCurrencyTotal`'s asterisk note now just flags "this total includes a conversion," not an accuracy caveat. QR payment code still skipped for non-CZK (spec is CZK-only) — unrelated to this.
- **`Client`/`ClientContact`** added going forward only, never backfilled from `Event`'s own free-text company fields (fragile fuzzy-matching risk). `Event.clientId` nullable FK; `Event.companyName/Address/Ico/Dic` untouched and still what PDFs/detail pages read from — `clientId` only powers the Clients section's rollup. `canManageClients()` = `canManageFinance() || canCreateEvent()`, no new permission dimension.
- **`Project.quotedValue` is derived, not manually typed** — originally a free-typed "rough estimate" field on the project form, it drifted from the real Quote/Invoice enough to be actively misleading (a EUR quote's CZK-equivalent could differ wildly from a stale CZK guess), and removed from the form entirely now (no UI shows it as editable). `src/lib/quoted-value.ts`'s `syncQuotedValue()` recomputes and stores it at every quote/invoice create/edit/delete/status-change: **the latest quote's subtotal if that quote is ACCEPTED**; else **the sum of every invoice's subtotal issued for the project**, if there are any (covers a quote declined-but-invoiced-anyway, and a project invoiced with no quote at all); else **0** (a DRAFT/SENT quote alone, or a DECLINED one with nothing invoiced, doesn't count as "the value"). Reads `subtotal` (excl. VAT), not `total` — see the `Quote.subtotal`/`Invoice.subtotal` note above. Converted to CZK via `src/lib/fx.ts` per document when foreign. `BudgetField`'s "% of quoted" mode reads this as a fixed snapshot, since nothing on the project form can change it anymore.
- **Expense "paid by"**: defaults to self; non-card-holders can only submit as themselves; card holders (or Admin/Accountant/Producer) get a dropdown of card holders + self.
- **Company overhead**: `Expense.eventId` nullable, not a fake pseudo-event.
- **Expense VAT**: `Expense.amount` is the gross paid; `Expense.vatRate` (21/12/0) + `Expense.vatAmount` (deductible input VAT, 0 = none reclaimable) are separate — net/excl-VAT expense used by Reports is `amount − vatAmount`. `vatAmount` auto-derives from gross×rate in the form but is editable so a receipt's real rounded VAT lands exactly. No `currency` on `Expense` (still always CZK). Pre-existing rows migrated to `vatRate 21 / vatAmount 0` — no retroactive input-VAT claim until edited.
- **VAT regime is inferred, not stored**: `classifyRegime(dic)` (`src/lib/vat-regime.ts`) reads the customer DIČ prefix — `CZ…` domestic, other EU-member prefix = intra-community reverse charge, else export. No per-invoice override (deferred). Used only by Reports → VAT.
- **`CustomRole`**: four tier enums (Events/Finance/Expenses/Settings access), same granularity as built-ins, not a generic bitmask. `User.role` is never nullable — when `customRoleId` is set, `role` holds an ignored `MEMBER` fallback so a missed call site fails closed, not open.
- **Invoice "History"** (`InvoiceEvent`) is a small targeted action log, not a general audit log.
- **Running timer** = a `TimeEntry` row with `running: true`; "one timer per user" is app-level (start stops any existing one), not a DB constraint. `TimePhase` enum inferred from the wireframe legend (brief doesn't name it).
- **`TimeEntry.eventId` is nullable** — entries can be created unassigned and assigned later via edit. Unassigned entries display via a shared `unassignedEvent` dictionary string and group under one bucket in aggregation queries.
- **Compare Events "cost per hour"** (superseded by Overview) was just `quotedValue ÷ totalHours` — no per-user rate field.
- **"Invite user"** creates the account immediately with a shown-once random password — no email system, matches the brief's "issued credentials" model. No pending-invitation state.
- **`User.lastSeenAt`** updates on login only, not every request.
- **Invoice email templates** live on `CompanySettings` (one shared pair, plain `{{token}}` string replacement — no templating library, the token set is small and fixed).
- **`Meeting`↔`Event` is many-to-many** via the `MeetingEvent` join model (an event can have several meetings; a meeting — especially an internal one — can span several events, or none). Recurrence (`recurrenceFreq`/`recurrenceInterval`/`recurrenceUntil`) is a rule on the row, not materialized per-occurrence rows — deliberate scope cut, no per-occurrence edit/cancel. `Event.notionPageId` is the only place an external id is cached on an app row; everything else about the Notion sync is push-only and stateless.

## Open questions from the brief (still open)

From brief §8 (resolved: invoice numbering, VAT, partial payments, company overhead). Still open:
- Event budget vs. actual tracking beyond the simple quoted-value-vs-expenses view.
- Expense approval workflow enforcement (field/hint exist, nothing enforces it).
- Notifications (email/Slack) for milestones/due invoices/expense approvals — email transport now exists (nodemailer) but nothing beyond invoice-sending uses it.
- General audit log for edits (not just invoice actions).
- Time tracker → invoicing (hourly rate → invoice line item) — brief says time is internal-only for now.

## Environment

This machine had **no Node.js, npm, Homebrew, Docker, or working Postgres** at project start (MacPorts present but broken). Network access works.

- **Node.js** (v24.19.0 LTS) installed user-local at `~/.local/node-v24.19.0-darwin-arm64`, symlinked into `~/.local/bin` (on `PATH`). No sudo, no Homebrew.
- **Database**: SQLite via Prisma for local dev (`web/prisma/dev.db`, gitignored). Swapping to Postgres later means changing the datasource + a migration, not a rewrite.
- **Git**: the home directory itself is a separate, unrelated commit-less repo — don't touch it. This project has its own nested repo at `PRODUCTION_APP/.git`.

## Tech stack

- **Next.js 16.3.1** (App Router, TS, RSC, Turbopack), project in `web/`. Newer than most training data — check `web/node_modules/next/dist/docs/` before assuming Next-15-era APIs. `params`/`searchParams` are `Promise`s; `middleware.ts` → `proxy.ts` (unused here, auth is per-layout/per-action); parallel-route slots need `default.js`.
- **Prisma 7.9.1** — also newer than most training data. `datasource.url` lives in `prisma.config.ts`, not `schema.prisma`; SQL providers need an explicit driver adapter (`@prisma/adapter-better-sqlite3` + `better-sqlite3`); generated client is at `web/src/generated/prisma` (gitignored, import via `@/generated/prisma/client` / `@/generated/prisma/enums`). `.claude/skills/prisma-*` (vendored by `prisma init`) has migration guides.
- SQLite (dev) via the adapter above.
- NextAuth (Auth.js), Credentials provider only — bcrypt, admin-seeded accounts, no self-registration (brief §2.1). Google OAuth to be added later via env vars once credentials exist.
- **Tailwind CSS v4**, dark theme tokens: background `#131211`, surface `#1a1918`, text/border `#f3f2f2`, accent `#ec3013`, plus semantic-only `--color-positive` `#2dd4bf` (teal) / `--color-warning` `#dc2626` (red) — reserved for `.tag` status chips, not general decoration. **Neue Regrade** font, self-hosted (`web/src/fonts/neue-regrade/`, commercial license — see `LICENSE.txt`), loaded via `next/font/local`. Rounded corners / `.glass-panel` frosted panels app-wide (supersedes the original Phase-1 sharp-corner decision) — a floating glass panel needs a tinted backing layer under the blur, blur alone doesn't obscure sharp text behind it.
- `@react-pdf/renderer` (invoice/quote PDFs) + `qrcode` (QR Platba). Registers Neue Regrade per-render, not at module scope (see gotchas); uses flattened static `NeueRegrade-Regular.ttf`/`NeueRegrade-Bold.ttf` (not the variable file the web app uses) for real bold.
- **pdf-lib** — attaches the generated ISDOC XML as a named file inside the PDF (`embedIsdoc()`), post-processing only, no react-pdf version-conflict risk.
- **ARES** (`ares.gov.cz`, free, no key) — `src/lib/ares.ts`, route at `api/ares/[ico]`, hook `useAresLookup`.
- **Photon** (`photon.komoot.io`, free, no key) — `src/lib/address-search.ts`, route at `api/address-search`, component `AddressAutocompleteInput`.
- **nodemailer** (`src/lib/email.ts`) — plain SMTP via `SMTP_HOST/PORT/USER/PASS/FROM` env vars (unset in dev — placeholders in `.env`), throws `EmailNotConfiguredError` cleanly when missing. Templates (`src/lib/email-templates.ts`) are import-free of nodemailer so the Settings form (a Client Component) can use them without pulling `net`/`tls` into the browser bundle.
- **googleapis** (`src/lib/gdrive.ts`) — Drive v3 via a **service account** (`GOOGLE_SERVICE_ACCOUNT_JSON_BASE64` + `GDRIVE_FINANCE_ROOT_FOLDER_ID` env vars, both unset in dev), throws `DriveNotConfiguredError` cleanly when missing.
- **@notionhq/client** (`src/lib/notion.ts`) — one-way push to a company Notion workspace via an **internal integration token** (`NOTION_API_KEY` + `NOTION_EVENTS_DATABASE_ID`/`NOTION_CLIENT_MEETINGS_DATABASE_ID`/`NOTION_INTERNAL_MEETINGS_DATABASE_ID`, all unset in dev), throws `NotionNotConfiguredError` cleanly when missing — same shape as `DriveNotConfiguredError`. `APP_BASE_URL` (new env var, no prior equivalent existed) builds the "open in app" link on a synced Event's Notion page.
- Receipts (`web/uploads/receipts/`) and logos (`web/uploads/logos/`) live on local disk (gitignored, not under `public/`), served through authenticated route handlers — receipts additionally check the requester actually has access to that expense; logos/avatars just require being logged in. Won't survive a stateless redeploy without moving to object storage. `UPLOAD_DIR` env var (default `process.cwd()`) redirects both roots for a deployed environment (e.g. a Railway volume) without touching local-dev behavior.
- ESLint `react-hooks/purity`: use `new Date().getTime()`, not `Date.now()`, inside a component body.
- ESLint `react-hooks/set-state-in-effect`: seed a ticking value via the lazy `useState(() => ...)` initializer, only set up the subscription/interval in the effect.
- ESLint `react-hooks/refs`: don't write to `ref.current` during render; if an effect's callback dep is safe to omit because the component unmounts before it could re-fire, say so with an inline eslint-disable rather than reaching for a ref.

## Commands (from `web/`)

- `npm run dev` — dev server (against whatever's already in `dev.db`, doesn't touch it)
- `npm run build` / `npm run lint` — typecheck + build / lint
- `npx prisma migrate dev` — apply schema changes locally
- `npm run seed:demo` (or `npx prisma db seed`) — wipes and reseeds the demo dataset (`prisma/seed.ts`): 4 users, 15 events, 6 clients, quotes/invoices/expenses/time entries, company settings.
- `npm run seed:empty` — wipes everything to a genuinely blank slate: one `admin@eventsystem.cz` login (`changeme123`) and nothing else.
- `npm run dev:demo` / `npm run dev:empty` — seed then start the server in one command (a bare seed script exits immediately, it's not a server — these exist because that confused things once).
- **The dev DB is currently seeded with the demo dataset** (as of 2026-08-24: 15 events / 6 clients, including 3 archived events from late 2025). Don't assume it's empty — run `npm run seed:empty` first if a task needs a blank slate.
