# Event system — working notes

Internal web app for an event agency: events from inquiry to invoicing, finance (quotes/invoices/expenses/reports), time tracking. Source of truth for requirements: `docs/system_info/event-system-zadani.pdf` (Czech brief, 17 Aug 2026). Visual/UX source of truth: `docs/wireframes/` (see `docs/wireframes/README.md` for the screen index and decisions already made there — don't re-derive those, they're settled).

Build plan for the current phase lives at `/Users/simonpletka/.claude/plans/tingly-popping-hippo.md`.

## Status

**Phase 1 (done):** project foundation, design system, auth, Dashboard, Events (list + tabbed detail with working Milestones CRUD + create/edit).

**Phase 2 (done):** Finance module — Quotes (`4a`, line items, one-click convert to invoice), Invoices (`4b` list with KPI strip, `4c` detail with a real generated PDF + embedded Czech "QR Platba" code, payment recording, mark-as-paid, action history), Expenses (list + `4d` new-expense form with receipt upload incl. mobile camera capture, "company overhead" no-event option), Reports (`4e`, by event/month/category, CSV export, print). Full RBAC matrix per brief §2.2. Verified via `npm run build` + `npm run lint` (both clean) plus extensive HTTP smoke tests: full quote→invoice conversion pipeline, PDF generation (confirmed valid PDF bytes with embedded QR image), partial payment → mark-as-paid state transitions, receipt upload + authenticated retrieval (confirmed byte-identical + confirmed a different user correctly gets 404), both CSV exports, and role scoping for all four roles. **No browser tool was available in either phase's session**, so nothing has been visually checked in an actual browser yet — do that before trusting the visual polish, especially the PDF layout and the drag-and-drop receipt input.

Dev login (seeded by `prisma/seed.ts`, password `changeme123` for all): `admin@eventsystem.cz`, `eva.kucerova@eventsystem.cz` (Accountant), `jan.novak@eventsystem.cz` (Producer), `m.dvorak@eventsystem.cz` (Member). **Re-login after every `prisma db seed`** — seeding wipes and recreates all rows with new IDs, so old session cookies/JWTs silently point at users/events that no longer exist (cost real debugging time once already — the symptom is empty lists with no error).

**Phase 3 (done):** Time tracker — one-running-timer-at-a-time (`5b`, live-ticking in the sidebar and on the Tracking page, switch/stop, manual entry with start/end or duration, edit/delete own entries, day/week/month views), Compare events (`5c`, multi-select via query-string chips, hours-by-phase stacked bars, per-person matrix, cost-per-hour, CSV export). Settings — Users & roles (`5d`, Admin-only: create accounts with a generated one-time password since there's no email system, change role, toggle company-card flag, reset password) and Company (`5e`, Admin/Accountant: company/bank/VAT/due-days, "Connected accounts" shown honestly as "Not connected" rather than the wireframe's aspirational mock status). Verified via clean `npm run build` + `npm run lint`, plus HTTP smoke tests: start→stop→switch timer with correct elapsed-minutes math, manual entry create/edit with both start/end and duration-only paths, Compare Events numbers hand-verified against seed data (cost-per-hour = quotedValue ÷ hours, confirmed exact), user creation followed by an actual login with the generated password, role/card-holder toggles, company settings save. Role scoping confirmed: Member has no Settings access but full personal Time-tracker access (it's inherently personal, not event-scoped); Producer's Compare Events is limited to their own/assigned events.

**Post-Phase-3 polish pass (done):** hold-to-reveal password field on login (`PasswordInput`, eye icon, press-and-hold not click-toggle); removed the "no public registration" login copy; Cancel button/link on every create/edit form (Event, Quote, Invoice, Expense, time-entry edit, Company settings); bigger, more visible back-link (`BackLink`) replacing the near-invisible 8px "← Events" breadcrumbs on event/invoice detail; a "+ New event" quick-create modal (`QuickCreateEventModal` + `EventPicker`) wired into the Quote/Invoice/Expense event pickers — creates a minimal event (title/client/company/dates only, full details editable later) without leaving the form, via a non-redirecting `quickCreateEventAction`; **Dashboard Timeline and Calendar views** (previously disabled tabs) and **Events Calendar week view (`3e`)** now implemented, sharing one `WeekCalendar` component (all-day band for multi-day prep/build vs. event-day spans, timed grid with milestones split into side-by-side columns on overlap) and one `WeekNav`. Timeline is a chronological build/start/milestone feed — not detailed in any wireframe (the brief's "timeline-first" direction was explicitly rejected per `docs/wireframes/README.md`), so its design is mine, not spec'd. Caught and fixed a real bug along the way: `getWeekCalendarData` originally only matched events whose build→strike span overlapped the displayed week, silently dropping milestones that happen well before an event's on-site dates (e.g. an early planning call) — now it also matches on milestone date directly. Also fixed a **UTC/local date bug**: `isoDate()` used `toISOString().slice(0,10)`, which converts to UTC first and silently rolls the date back a day for negative UTC offsets (and would for positive ones the other way) — the symptom was the calendar's "Today" link pointing at yesterday. Fixed by formatting from local date components instead; added a matching `parseIsoDate` for the read side so the round-trip never touches `new Date(dateOnlyString)`. Verified via clean build/lint plus HTTP checks (correct all-day/prep-build/event-days segments and a milestone-only week both rendering, "Today" link resolving to the actual local today). **The quick-create-event modal itself couldn't be exercised via curl** (it only mounts after a client-side click, never appears in server-rendered HTML) — try that one in an actual browser before trusting it.

**Custom roles + user management (done):** the four built-in roles (Admin/Accountant/Producer/Member) stay exactly as they were — user explicitly asked to leave them alone — but Admin can now also create **custom roles** with their own configurable reach across four dimensions (Events/Finance/Expenses/Settings), each with the same tiers the built-ins already use (e.g. Events: none/assigned-read/own-edit/all-read/all-full). This was a real architecture change, not a UI tweak: `authz.ts` was rewritten around one `resolvePermissions(user)` function that maps either a built-in `role` or an assigned `CustomRole` onto the same four-dimension shape, and every access check (`canCreateEvent`, `eventWhereForUser`, `canViewFinance`, etc.) now reads from that instead of switching on `user.role` directly — built-in and custom roles flow through identical logic, so there's no special-casing to get out of sync. Settings gained a third "Custom roles" tab (create/edit/delete; delete is blocked with a clear error while any account is still assigned) and the per-user role `<select>` now lists custom roles in an optgroup alongside the built-ins. Also added: editable name/email/card-holder per user (`/settings/users/[id]/edit`) and **deactivate/reactivate** (`User.active`) — deactivating keeps all their historical expenses/payments/time entries intact (explicitly chosen over hard-delete, which would either violate foreign-key constraints or destroy financial history) but blocks login and, notably, **also cuts off any already-open session** — `requireUser()` re-checks `active` against the DB on every request, not just at login, otherwise a JWT session would stay valid for up to its full max-age after "removal," which defeats the point.

**Real bug hit and fixed while wiring this up**: `signOut()` mutates cookies, which Next.js only permits from a Server Action or Route Handler — calling it inside `requireUser()` (invoked mid-render from Server Components) threw `Cookies can only be modified in a Server Action or Route Handler` on every request. Fixed by having `requireUser()` just `redirect("/login")` without clearing the cookie, and instead teaching the **login page itself** to check `active` before honoring an existing session and bouncing to `/dashboard` — otherwise a deactivated user's stale cookie would create redirect ping-pong (`/dashboard` → kicked to `/login` → bounced back to `/dashboard`). The cookie is inert either way since every protected route re-verifies `active`; it just isn't proactively cleared.

**Verified via**: clean `npm run build` + `npm run lint`, plus HTTP smoke tests — since the "New role"/"Edit role" forms are behind client-only `useState` (never in server-rendered HTML, so uncurlable), the permission-resolution logic itself was verified by seeding a custom role directly and driving it through real requests: a role with `events=ALL_READ` + everything else `NONE` correctly saw all 7 seeded events with no "New event" button and no Finance/Settings access; deactivating that user mid-session correctly redirected them away (no loop) and blocked a subsequent login attempt with "Invalid email or password"; reactivating restored both; deleting a role with an assigned user correctly failed with a message, then succeeded once reassigned; edit-user-info round-tripped correctly. **The role-management forms' own click-driven UI (create/edit role, the modal-style bits) still haven't been exercised in an actual browser** — same caveat as the quick-create-event modal.

**Font**: swapped Archivo (a Phase 1 placeholder pick) for **Neue Regrade**, a commercial font the user supplied (licensed for commercial use, see `web/src/fonts/neue-regrade/LICENSE.txt`) — self-hosted via `next/font/local` using the variable-weight TTF, since it isn't on Google Fonts and can't be fetched the way Archivo was.

**Settings UI reorg + ARES integration (done):** three follow-up fixes from user feedback on the custom-roles work. (1) The Users table's Role column was an inline `<select>` that saved on change — replaced with plain text (built-in role label, or the custom role's name via a lookup map) since role changes now happen deliberately on `/settings/users/[id]/edit` instead of by accident while scanning the table. `updateUserInfoAction` absorbed the old standalone `assignRoleAction`; it skips the role/customRoleId write entirely when `id === user.id` (self-role-change guard), verified by POSTing a spoofed `role=ROLE:MEMBER` directly at the endpoint for the logged-in admin's own account — silently ignored, role stayed `ADMIN`. (2) Deactivate wasn't discoverable — there was no button for it in Settings at all before this pass; it's now on the Edit User page as a clearly labelled, `confirm()`-gated action (hidden for your own account, same as role editing), with explanatory copy about what deactivation does and doesn't affect. (3) **ARES lookup is now live**, not deferred — `src/lib/ares.ts` calls the free public `https://ares.gov.cz/ekonomicke-subjekty-v-be/rest/ekonomicke-subjekty/{ico}` endpoint (no auth/API key needed), exposed via an auth-gated `src/app/api/ares/[ico]/route.ts` and a `useAresLookup` client hook. "Load from ARES" buttons now actually work in both the Event form's client/company fields and Settings → Company, filling in company name/address/DIČ from an 8-digit IČO. Verified end-to-end against two real companies (IČO `21342601` gen live production s.r.o., IČO `23288183` hdhd studio s.r.o.): direct ARES API call → own route handler → Event creation with the returned data → Invoice creation for that event → invoice detail page → generated PDF, all matching. **Found and fixed a real bug along the way**: the invoice PDF used the standard "Helvetica" font, which only covers WinAnsi/cp1252 and has no glyphs for ě/ř/ů/ď/ť/ň — Czech diacritics were silently dropping from generated PDFs (e.g. "Vojtěšská" rendered as "Vojtská"). Confirmed by properly decoding the PDF's content stream with `pdfminer.six` (a naive raw-byte search on the compressed/CID stream missed it). Fixed by registering the Neue Regrade TTF via `Font.register()` in `InvoicePdf.tsx` and switching `fontFamily` to it — confirmed via `fontTools.getBestCmap()` that it has full Czech coverage, then re-verified the PDF text extracts correctly.

**Time tracker delete confirmation (done):** the Delete button on Time tracker → My entries used to submit immediately on click, unlike every other destructive action in the app. Added the same `confirm()`-gated client form pattern already used for user deactivation (`DeleteEntryButton.tsx`). No schema/action changes — `deleteTimeEntryAction` itself was untouched, only the trigger got a confirmation gate.

**Invoice PDF branding (done):** `CompanySettings` gained `logoPath` (nullable filename) and `accentColor` (hex, default `#ec3013`). Settings → Company has a new "Branding" section: a logo upload (PNG/JPG/SVG, 2MB cap, live preview, remove-logo checkbox) and an accent-colour picker (native `<input type="color">` paired with a text field for the hex value). Logos are stored on disk at `web/uploads/logos/` (gitignored, same pattern as receipts) and served through an authenticated route handler (`src/app/api/uploads/logo/[filename]/route.ts`) — unlike receipts, there's no per-owner check beyond "logged in," since a company logo isn't sensitive per-user data. The generated invoice PDF (`InvoicePdf.tsx`) now takes `logoDataUrl`/`accentColor` props instead of a hardcoded accent constant — the PDF route handler reads the logo file and base64-encodes it into a data URL before rendering (react-pdf's `Image` can't reach an authenticated route, and can't render SVG at all — SVG previews fine in the Settings UI but is silently skipped in the PDF; documented in the form's helper text). Verified by uploading a real PNG, confirming byte-identical retrieval through the route handler, then generating an actual invoice PDF and decoding its content stream to confirm both the embedded logo image and the exact custom accent RGB triple (`#1478dc` → `0.0784 0.4706 0.8627 scn`) are present — and confirming removal correctly deletes the file from disk and falls back to the default `#ec3013` in a freshly generated PDF.

**Admin-only delete for events and finance items (done):** Admin can now permanently delete Events, Quotes, Invoices and Expenses — nothing else could before this (only Milestones and time entries had delete). New `isAdmin(user)` in `authz.ts` deliberately checks the literal built-in `role === "ADMIN"` (with `customRole === null`) rather than a permission tier, so a CustomRole configured with `ALL_FULL`/`FULL` access for day-to-day work never gets the irreversible-delete button just by having equivalent read/write reach — matches the user's explicit "visible only to admin," not "visible to anyone with full access." All four delete actions (`deleteEventAction`, `deleteQuoteAction`, `deleteInvoiceAction`, `deleteExpenseAction`) check `isAdmin` first and no-op otherwise (defensive against a spoofed direct POST, not just a hidden button) — verified by POSTing each one directly as a Producer account and confirming the row survives untouched. UI uses a new shared `ConfirmDeleteButton` (generalizes the pattern already used for time-entry/user-deactivation) placed on the item's own page rather than in list rows, consistent with how deactivate-user was placed: event detail header (next to Edit), invoice detail page (bottom of the sidebar, past History), and inline per-row on the Quotes and Expenses list pages (no dedicated detail/edit page exists for either, so the row is the only place). Deleting an Event cascades through the schema's existing `onDelete: Cascade` relations (members, venues, milestones, time entries, quotes+items, invoices+items/payments/history) — deliberately not blocked or narrowed, since the confirm dialog states plainly what's about to be destroyed. Deleting a Quote that's already been converted correctly leaves the invoice intact and just clears its `quoteId` link (schema already had `ON DELETE SET NULL` there from Phase 2). Expense receipt files aren't part of any DB cascade (they live on disk), so `deleteEventAction`/`deleteExpenseAction` explicitly unlink them too — added a `deleteReceipt()` helper to `uploads.ts` alongside the existing `deleteLogo()`. Verified end-to-end by building a real test event with a milestone, time entry, expense-with-receipt, quote and invoice, then deleting it and confirming every related row *and* the receipt file on disk were gone — plus separately verified quote/invoice/expense deletion (including a converted quote leaving its invoice behind) and the Producer-role rejection for all four actions.

**Confirm dialog on every hard-delete button (done):** user feedback (in Czech) after the admin-delete work above: every hard-delete action needs a confirmation dialog warning the user data will be destroyed, not just some of them. Audited every `delete*Action`/`deactivate*Action` in the codebase and found two that had shipped with no confirmation at all — immediate delete on click: Milestone delete (`events/[id]/milestones/page.tsx`) and Custom role delete (`RolesTab.tsx`). Fixed by routing the milestone button through the same `ConfirmDeleteButton` used for events/quotes/invoices/expenses, and adding an inline `onSubmit` confirm to the custom-role delete form (kept as a plain form rather than switching to `ConfirmDeleteButton` since it needs to keep its `useActionState` error message for the "still assigned" case). Everything else audited — event/quote/invoice/expense delete, time-entry delete, user deactivate — already had it from when each was originally built. Verified both fixes still submit correctly (milestone delete and custom-role delete both re-tested end-to-end via direct POST after the change).

**Event delete now blocked when expenses exist (done):** user reported they could still delete events/expenses "without confirmation," and asked for a hard gate — an event with any expenses recorded against it must not be deletable at all, with a popup explaining why. Investigated the "no confirmation" report by walking React 19's actual `react-dom-client.development.js` source for how `<form action={fn}>` + `onSubmit` interact: confirmed `event.preventDefault()` inside `onSubmit` correctly stops React from ever invoking the action (verified in the bundled source, not just docs) — so the `ConfirmDeleteButton` code itself was already correct. But a fresh `sqlite3` check of the dev DB turned up **0 rows in `Event`** despite 4 users still present — meaning every seeded event really had been deleted, which lines up with the user's report and strongly suggests they were testing against a stale dev-server/browser-cache state from before the confirm-button work landed, not a logic bug in the current code (a `pkill` + `rm -rf .next` + fresh `next dev` + reseed was done as part of this fix as a precaution). New `DeleteEventButton.tsx` (replacing the generic `ConfirmDeleteButton` for this one case, since it needs event-specific logic) checks `expenseCount` **before** ever calling `confirm()`: if it's greater than 0, submission is blocked immediately and an `alert()` explains exactly why and what to do (remove/reassign the expenses first) — no server round-trip needed since the count is already on the page. `deleteEventAction` also re-checks `expenseCount` server-side and silently no-ops if it's nonzero, same defense-in-depth pattern as the `isAdmin` check — verified via a direct spoofed POST against a seeded event with 3 expenses (survived) and a same-shaped request against a zero-expense event (deleted normally, confirming the gate is precise, not just always-blocking).

**Fixed the real confirm-dialog race (done):** the previous turn's "0 events in the DB" theory turned out to be only half the story — the user reproduced it again on a genuinely fresh `npm run dev`, deleting a time entry with no dialog at all, and pasted the terminal log showing `deleteTimeEntryAction` executing directly. That's a real bug, not a stale-cache artifact. Root cause: every confirm-gated destructive button was built as `<form action={serverAction}> onSubmit={(e) => { if (!confirm(...)) e.preventDefault() }}`, which relies on the browser's `onSubmit` handler's `preventDefault()` winning a race against React 19's own internal form-action dispatch (a separate "action" pseudo-event React's `extractEvents$1` creates for the same native `submit` event) — in practice that race doesn't reliably go the safe way. **Fix: decoupled confirmation from form submission entirely.** Every one of these is now a plain `<button type="button" onClick={...}>` (not inside a form-with-action at all) that calls `confirm()`/`alert()` synchronously first, and only *if approved* builds a `FormData` and invokes the imported server action directly inside `startTransition()` — matching the officially-supported "call a server action programmatically" pattern, with zero framework machinery in between that could race. Touched: `ConfirmDeleteButton.tsx` (shared — milestones/quotes/invoices/expenses), `DeleteEntryButton.tsx` (time tracker), `DeleteEventButton.tsx` (events, keeps its expense-count pre-check), the custom-role delete in `RolesTab.tsx` (calls the `useActionState` dispatch function directly instead of via `<form action>`), and `DeactivateButton` in `EditUserForm.tsx` (same anti-pattern, same fix, even though it's deactivate not delete). **Verified in an actual browser** (Chrome extension connected mid-session — first time browser access worked in this whole project): logged in as Admin, and for each of a time entry, an event with expenses, an event without expenses, and an expense, monkey-patched `window.confirm`/`window.alert` via the JS console to log calls and control the return value (rather than letting a real blocking native dialog hang the automation) — confirmed `confirm()` fires with the exact expected message, returning `false` correctly leaves the record untouched, and returning `true` actually deletes it; separately confirmed the expense-count gate's `alert()` fires with the exact message and `confirm()` is never even reached in that case. This is the first real end-to-end confirmation of the fix (earlier attempts to replicate a direct action call via curl hit a wall — `dispatchAppRouterAction` is wired into the full App Router client state machine, not a plain fetch, so it genuinely couldn't be faked from the command line).

**In-app confirm/notify dialogs, event-delete gate extended to invoices (done):** two follow-ups from the confirm-dialog fix above. (1) User wanted the confirmation itself to be an in-app dialog matching the app's design instead of the browser's native `confirm()`/`alert()` popup, with a specific layout: centered, orange Cancel on the left, plain Confirm on the right. Built `ConfirmDialogProvider.tsx` — a context provider mounted once in `(app)/layout.tsx` exposing `confirm(message, opts): Promise<boolean>` and `notify(message, opts): Promise<void>` (an imperative in-app replacement for `window.confirm`/`window.alert`, styled with the same `bg-surface`/`border-ink` tokens as the existing `Modal.tsx`). Deliberately styled with Cancel as the solid `.btn` (filled accent) and Confirm as the plain `.btno` (outlined, no accent) — inverted from the usual "primary action is the colored one" convention, on purpose: since every use of this dialog is for a destructive action, making the *safe* choice visually louder (and autofocused, so Enter/Escape/click-outside all cancel) reduces accidental confirms. Every one of the six confirm/notify call sites converted from `window.confirm()`/`window.alert()` to this. (2) Extended `deleteEventAction`'s block-list from "has expenses" to "has expenses **or** invoices" (checked client-side before showing anything, and server-side via `Promise.all` counts as defense in depth) — an event with either now gets an in-app `notify()` explaining exactly what's blocking it and what to do, never reaching the confirm step at all. Quotes were deliberately left out of the gate (an accepted-but-not-yet-invoiced quote is still safe to lose along with the event). **Verified in the browser** (same Chrome connection as the prior fix): screenshotted the actual dialog to confirm the centered/orange-left/plain-right layout: time-entry delete (cancel keeps it, confirm removes it), event blocked by an invoice (`Roadshow`, single OK button, correct singular wording), event with no expenses/invoices deleting cleanly end-to-end (`Summer Gala`, cancel then confirm), and an expense-list delete rendering with identical styling — confirming the dialog looks and behaves consistently everywhere it's used, not just on the one screen tested first.

**Notify-dialog OK button is orange; sidebar is now hover-to-reveal (done):** two small follow-ups. (1) The single "OK" button on the `notify()` dialog (the "can't delete this" popup) was `.btno` (plain outline) — changed to `.btn` (solid accent) per user request, matching the Cancel button's treatment on the confirm variant. (2) Reworked `Sidebar.tsx` from a binary collapsed/expanded toggle into a pin/auto-hide model: default state is `pinned` (behaves exactly as the sidebar always has — static, in-flow, pushes content); clicking the corner button un-pins it, at which point the sidebar becomes `position: fixed` (removed from the flex flow, so `main`'s `flex-1` reclaims the full width) and slides fully off-screen (`-translate-x-full`) via a `transition-transform`. A slim invisible hot-zone (`fixed inset-y-0 left-0 w-3`) along the very left edge, present only while unpinned, triggers `hovering` on mouse-enter; the sidebar itself also tracks its own `onMouseEnter`/`onMouseLeave` so it stays revealed while the cursor is over it and retracts the moment it leaves — unless re-pinned via the same corner button, whose icon/tooltip flips between "Unpin sidebar" («) and "Pin sidebar" (»). No persistence (plain `useState`, same as the collapse toggle it replaced) — survives client-side nav within the layout since Next.js doesn't remount it, but resets on a hard reload. **Verified in the browser**, including a real gotcha caught during testing: the Chrome automation's hover state doesn't persist *across separate tool calls* (only within one continuous action batch), which made the very first pin/unpin round-trips look broken — confirmed via direct DOM inspection (`getBoundingClientRect()` on the button, and reading its `title` to check the actual `pinned` state) that this was purely a testing artifact, not a real bug, then re-verified with hover+click chained in a single batch: unpin retracts the sidebar and `main` reclaims the width, hovering the left edge reveals it as an overlay on top of content, and re-pinning via the button makes it stick regardless of subsequent mouse movement — matching the original default layout exactly.

**Sidebar polish (done):** three small tweaks. Auto-hide slide transition slowed 20% (`duration-200` → `duration-[240ms]`, the exact requested ratio, not just "a bit slower"). "My timer" → "My tracker" in `TimerWidget.tsx` (both the idle and running-state headings). All sidebar text made bolder via a single `font-medium` class on the sidebar's outer container rather than touching each line individually — `.nav-item`/`.label`/`.heading-label`/`.placeholder-text` in `globals.css` don't set their own `font-weight`, so they inherit the 500 from the wrapper; elements that already had their own weight (`EVENT SYSTEM`'s `font-bold`, the running timer's `font-semibold`, `.nav-item.active`'s inline 600) are untouched since a directly-applied weight always wins over an inherited one, regardless of CSS specificity ordering. Verified via zoomed screenshots of both the top nav and the bottom user/settings block in a real browser.

**Empty-slate seed for real-data testing (done):** user wants to start using the app with their own real events/finance data instead of the demo fixtures. Added `prisma/seed-empty.ts` (run via `npm run seed:empty`) alongside the existing `prisma/seed.ts` demo seed — deliberately a separate script rather than a flag on the existing one, so the well-established demo seed stays untouched. Wipes every table the demo seed does, plus `CustomRole` and (as of the Clients batch below) `Client`/`ClientContact` — both gaps that would otherwise let old test data leak into what's supposed to be a blank slate — then creates exactly one login (`admin@eventsystem.cz` / `changeme123`, same credential convention as the demo accounts) and nothing else — no `CompanySettings` row either, since Settings → Company already handles a null company gracefully (confirmed the form renders correctly with "No logo" and blank fields) and the point is for the real company to enter their own real data through the UI rather than start from seeded placeholders. **Verified every major page renders cleanly against the fully empty DB** (Dashboard, Events list + calendar, Finance tabs, Time tracker, Settings tabs, and all four "new" forms) — all 200s, no error markers, sensible empty-state copy ("No upcoming events", "No expenses yet", "Not enough data yet"). **The dev database is currently left in this empty state** (not reseeded back to demo) since that's the actual point of this change — run `npm run seed:demo` to get the demo dataset back for feature work.

**Large batch: dark-mode form controls, event date auto-adjust, worldwide address autocomplete, per-document currency, and a new Clients section (done).** Ten distinct asks in one request; grouped here by theme rather than one entry per item.

*Small UI/UX fixes* — (1) Native browser chrome (date/datetime-local picker popups, number-input spin arrows) was rendering light-mode regardless of the app's own dark theme; fixed with one `color-scheme: dark` on `body` in `globals.css` (verified via `getComputedStyle` in a real browser — the OS-level picker popup itself isn't screenshot-capturable, so this is the authoritative signal Chrome actually uses to decide native-control theming, not a visual guess). (2) Quote/invoice line-item column labels (Qty/Unit price/VAT %) weren't aligned with their input boxes — they had no left padding while the inputs did; added matching `pl-2.5` to the header labels in `LineItemsFields.tsx`. (3) Number inputs (qty/unit price/VAT) kept their old value as a visible prefix when you started typing over them (e.g. typing "5" into a "0" field could read "05" before settling); added `onFocus={(e) => e.target.select()}` so focusing a number field selects its full contents, matching how spreadsheet-style numeric entry normally behaves.

*Event start/end date logic* — changing "Event start" in `EventForm.tsx` now always pulls "Event end" onto the same calendar day. If the end's existing time-of-day still lands after the new start time on that day, it's left as-is (same duration-preserving intent, just re-dated); otherwise — the naive same-day placement would make the event run backwards — it falls back to `newStart + originalDuration`, which can roll into the next day for a moved-late start (verified both branches directly via DOM `value` + `dispatchEvent('input')`, since native `datetime-local` segmented-input keystrokes are too browser-idiosyncratic for reliable automated typing).

*Worldwide address autocomplete* — user wanted this to work outside Czechia, which ruled out the Czech-only RÚIAN registry that would otherwise have matched the ARES-integration precedent most closely. Used **Photon** (`https://photon.komoot.io/api/`, komoot's free public instance over OpenStreetMap data) instead — no API key, no billing account, same "free public API" spirit as ARES. `src/lib/address-search.ts` does the fetch + label formatting, `src/app/api/address-search/route.ts` wraps it behind session auth, and `AddressAutocompleteInput.tsx` is a reusable debounced (300ms) dropdown component — wired into `EventForm`'s company address, `CompanySettingsForm`'s company address, and the new `ClientForm`/`QuickCreateClientModal`. Verified against the real Photon API directly and through the app's own route, and confirmed the dropdown-then-select flow live in the browser.

*Per-document currency (CZK/EUR/USD)* — added a `Currency` enum + `currency` field to `Quote` and `Invoice` (default CZK, user-selectable when creating either). Converting a quote to an invoice propagates its currency automatically. `formatCurrency(amount, currency = "CZK")` in `format.ts` now uses `Intl.NumberFormat` with `style: "currency"` instead of a bare number formatter — this is also what made currency symbols show up *at all* for the first time (the old implementation had no `style: "currency"`, so CZK amounts never actually displayed "Kč" anywhere despite the CLAUDE.md history casually referring to them that way; only `InvoicePdf.tsx`'s separate hand-rolled `money()` helper appended "Kč" manually). All ~30 pre-existing call sites needed no changes since the new second parameter defaults to CZK; only quote/invoice-specific displays and the two PDF templates pass an explicit currency. Per user's explicit instruction, aggregate statistics (Reports, Dashboard, event budget-vs-actual, the Clients list's "total charged") deliberately keep summing raw amounts across whatever currencies happen to be present, labeled as CZK — no FX conversion anywhere, a known simplification, not a bug. The Czech "QR Platba" QR code is CZK-only by definition (`CC:CZK` is hardcoded in the SPD payment-string spec), so `InvoicePdf`'s route handler now skips generating one for non-CZK invoices and the PDF footer explains why instead of misleadingly claiming "no bank account on file." Verified end-to-end: created a real EUR quote, confirmed the currency badge and correct `Intl` formatting on the detail page and in the downloaded PDF (decoded via `pdfminer.six`), converted it to an invoice and confirmed the currency carried over, and confirmed the invoice PDF has zero embedded images (no QR) with the correct footer explanation.

*Quotes get a real detail page + PDF* — previously quotes had no `/finance/quotes/[id]` route at all (only list/new/edit), no PDF generation, and `createQuoteAction`/`updateQuoteAction` redirected back to the bare list — so there was no way to preview, download, or navigate to a quote after creating one. Extracted `src/lib/pdf/shared.tsx` (font registration, `money()`/`pdfDate()` helpers, the shared `StyleSheet`) out of `InvoicePdf.tsx` so `QuotePdf.tsx` could reuse it instead of duplicating ~150 lines; added `/api/quotes/[id]/pdf` and `/finance/quotes/[id]/page.tsx` (preview pane, Download PDF, Edit-if-draft, Convert-to-invoice-if-accepted, linked invoice(s), admin delete). Both quote actions now redirect to the new detail page instead of the list. Verified via a real create → redirect → PDF-download round trip.

*New Clients section* — per user's explicit choice, a real `Client`/`ClientContact` entity added going forward rather than auto-grouping existing events by free-text company name (which would be fragile — "Kobra a.s." vs "Kobra, a.s." would silently split what should be one client). `Event.clientId` is a new nullable FK; existing events stay unlinked and Event's own embedded `companyName`/`companyAddress`/`companyIco`/`companyDic` fields are untouched and still what every other screen (PDFs, event detail) actually reads from — `clientId` purely powers the Clients section's own event/revenue rollup. `canManageClients(user)` in `authz.ts` is deliberately `canManageFinance(user) || canCreateEvent(user)` — reusing the two roles who already touch a client's data day to day (Producer via events, Accountant/Admin via Finance) rather than inventing a fifth permission dimension for one new screen; Member gets neither. New pages: `/clients` (list — name, IČO, contact count, event count, total charged), `/clients/[id]` (detail — full contact list with add/remove, linked events, total charged, admin delete), `/clients/new` + `/clients/[id]/edit`. `EventForm` gained a `ClientPicker` (mirrors the existing `EventPicker`/`QuickCreateEventModal` pattern) that, on selecting an existing client, pre-fills the company name/address/IČO/DIČ fields — **caught and fixed a real bug here**: `getClientOptions()` originally only selected `{id, name}` for the picker's dropdown, so selecting a client filled in the name but silently left address/IČO/DIČ blank; fixed by selecting the full set of fields the picker actually needs. Verified end-to-end in the browser after the fix: created a client, linked an event with two invoices in different currencies to it, and confirmed the client detail page's "total charged" exactly matched the hand-computed sum (411 400 + 1 210 = **412 610 Kč**) and that selecting the client from `EventForm` correctly auto-filled every company field.

**Six-item feedback batch: orange calendar highlight, auto-create-client, quote-from-event shortcut, Czech PDFs with EN/CZ chooser, invoice-filter contrast fix, quote-to-invoice discoverability (done).**

*Orange calendar highlight* — `accent-color: var(--color-accent)` added to `body` in `globals.css`, alongside the existing `color-scheme: dark`, fixing checkboxes/radios/range sliders and the small calendar-icon glyph on date inputs. **Correction from an earlier turn's claim in this file**: `accent-color` does NOT reach the native date-picker's *popup calendar* (the selected-day highlight inside the dropdown) — that was a genuine mistake, only caught because the user reported the highlight still wasn't showing after the fix shipped. Researched properly this time: `accent-color` is spec'd to cover checkboxes/radio/range/progress and the date-input's picker-indicator icon only; the popup itself is a native OS/browser-chrome widget with no CSS hook into its internal highlight color, in any browser. `getComputedStyle` confirming the property is *set* was never proof it does anything for that specific sub-part of the widget — a real gap in how the earlier fix was "verified." The only real fix would be a fully custom-built date/time picker component swapped in everywhere; offered that to the user, who said not now — the popup's default (browser-blue) highlight color is accepted as a known limitation, tracked for a possible future pass.

*Auto-create client on event save, "+ New client" button removed* — per explicit user instruction ("get rid of the 'new client' button"), deleted `QuickCreateClientModal.tsx` and the manual-creation half of `ClientPicker.tsx` entirely; it's now just a `<select>` between existing clients and "New client — from the company info below." A new `resolveClientId()` helper (`src/lib/actions/clients.ts`) runs inside both `createEventAction` and `updateEventAction`: if no client was explicitly picked but company fields are filled in, it looks up an existing `Client` by IČO-or-name first and only creates a new row if neither matches — the same dedup concern the manual flow never actually had to solve, since it required explicit user action every time. Verified in the browser: created an event with a brand-new company name/address and no client selected, confirmed a `Client` row was created automatically with the correct (diacritic-preserving) address, with no separate step required.

*"New quote for this event" shortcut* — event detail page (`events/[id]/page.tsx`) gained a direct link to `/finance/quotes/new?eventId=<id>` next to the existing "View quotes & invoices" link (gated by `canManageFinance`, same as the rest of Finance). `QuoteForm` gained an `initialEventId` prop threading through to `EventPicker`'s `defaultValue`. Verified in the browser: clicking the shortcut from a freshly created event landed on the quote form with that exact event already selected.

*Czech PDF templates + EN/CZ download chooser* — new `src/lib/pdf/i18n.ts` (`PdfLang = "en" | "cs"`, a `PDF_LABELS` dictionary, `pdfLabels(lang)`), threaded through both `InvoicePdf.tsx` and `QuotePdf.tsx` (every hardcoded English string replaced with a lookup) and `pdfDate()` in `shared.tsx` (locale-switches `cs-CZ`/`en-GB`). Both PDF route handlers now read `?lang=cs|en` (default `en`) off the query string. Clicking "Download PDF" no longer downloads directly — a new `DownloadPdfButton.tsx` client component reveals an inline in-app dropdown ("English" / "Čeština"), **not** a native `prompt()`, consistent with this app's standing rule against native dialogs for in-app interaction; wired into the invoice list row, invoice detail page, and quote detail page. **Found and fixed a real, serious bug while verifying this**: rendering an `InvoicePdf` and a `QuotePdf` back-to-back in the same long-running Next.js server process silently corrupted specific digit glyphs (reproduced deterministically: "3" and "4" were dropped) in whichever PDF rendered second — e.g. IČO `09112233` came out as `091122`, a quote total of `154 880 Kč` came out as `15 880 Kč`. Root-caused via a bisected reproduction script (isolated `renderToBuffer` calls, varying order and component) to `@react-pdf/font`'s `FontStore` caching the loaded/parsed font object under a fixed family name (`"NeueRegrade"`) at module scope for the process lifetime — its glyph-subsetting state leaks across unrelated `renderToBuffer()` calls for different documents. Fixed by no longer calling `Font.register()` once at module load; `shared.tsx` now exports `registerAppFont()`, called fresh inside each PDF component's render body, which registers a uniquely-suffixed family name (`NeueRegrade-${Date.now()}-${random}`) every time — giving each rendered document its own independent `FontSource` and thus independent subsetting state. Confirmed the fix with the same reproduction script (both documents fully correct in either render order) and then over real HTTP through the actual dev server process, and confirmed correct Czech diacritics (Ř/Ě/Ů/Č) and correct digits in both languages via `pdfminer.six` text extraction on real seed data.

*Invoice-list filter contrast fix* — the bucket-filter buttons (All/Issued/Paid/Overdue/Partly paid) were unreadable (light text on light background) on whichever bucket was active, always including the default "All". Root cause: Tailwind v4 utility classes live inside CSS Cascade Layers, while `.btno`'s own hand-written `color` rule in `globals.css` is unlayered — and unlayered rules always beat layered ones regardless of specificity or source order, so the non-`!important` `text-bg` utility silently lost. Fixed with the `!` important-modifier (`!bg-ink !text-bg`), which flips the priority back. Verified via screenshot: "All" now renders dark text on a light background, clearly legible.

*Quote-to-invoice discoverability* — `convertQuoteToInvoiceAction` already existed and worked (built during Phase 2), but a user landing on `/finance/invoices/new` for the first time had no way to discover it — the actual gap was navigational, not functional. `/finance/invoices/new` now queries for accepted-but-not-yet-invoiced quotes up front and, when any exist, shows a "From an accepted quote" box above the blank-invoice form listing each with a one-click "Convert to invoice →" button (reusing the existing action, unchanged). Verified in the browser: with an eligible quote in the seed data, the box appeared with correct quote number/event/total/accepted-date and linked through to the quote detail page.

Every item verified via clean `npm run build` + `npm run lint`, plus a mix of HTTP smoke tests (`pdfminer.six` text extraction on real generated PDFs, direct DB checks on the auto-created `Client` row) and live browser verification via the Chrome extension for everything with a visual/interaction component.

**Six-item batch: event contacts + client sync, quote/invoice categories with hide-price mode and reorder, invoice discounts, auto-fill invoice from event, unified YY-XXX numbering, revert-mark-as-paid (done).**

*Event contacts, synced onto the linked Client* — `Event.clientName/clientPhone/clientEmail` (a single embedded contact) removed entirely and replaced with a new `EventContact` model — an event now supports 1+ contact people, edited via the same repeatable-row pattern as Venues. On save, `syncClientContacts()` (`lib/actions/clients.ts`) copies each contact onto the resolved `Client`'s own contact list — deduping on email first, falling back to exact name — and, only if the client's new `invoicingEmail` field is still blank, fills it in from the first contact that has one (never overwrites a value someone set deliberately). These are independent copies, not linked by a foreign key back to the `EventContact` they came from — deleting a synced `ClientContact` from the Client page never touches the event's own contact list, and vice versa; confirmed by deleting one side and re-querying the other. `invoicingEmail` also got its own editable field on `ClientForm`/the Client detail sidebar, since it's meant to be correctable by hand too, not just auto-populated once. Since the DB had no real event data yet (empty seed state), this was a clean schema break rather than a migration with data preserved.

*Line-item categories + reorder + hide-item-prices* — `QuoteItem`/`InvoiceItem` gained a `category` field (plain string, not a foreign key) and reused the `sortOrder` field they already had for manual reordering. `LineItemsFields.tsx` (shared by both Quote and Invoice forms) gained ▲/▼ buttons per row that swap array positions, and a category `<input list=…>` backed by a `<datalist>` of the org's default categories (Settings → Categories, seeded Video/Audio/Rigging/People/Other) — typing anything not in that list just becomes a one-off category name for that single item, exactly matching the ask that "categories can be added for only the one quote" without polluting the global list. A new `ItemCategory` model + Settings tab (Admin/Accountant, same tier as Company settings) supports add/rename/delete on the default list; deleting a default category has zero effect on items already tagged with it, since there's no FK relationship — the tag is just free text that happens to have matched a Settings entry at the time. `Quote.hideItemPrices`/`Invoice.hideItemPrices` (both default **true**, per explicit instruction) control a form checkbox and a PDF/preview rendering mode: when on, each category becomes a heading with its items listed by description only (no qty/unit/VAT columns) followed by a category subtotal, and a default category with zero items on that particular document simply isn't rendered — no empty headings. `src/lib/line-items.ts` (`groupItemsByCategory`, `categoryTotal`) is shared by the on-screen quote/invoice detail pages, `InvoicePdf.tsx`, and `QuotePdf.tsx` so all three stay visually consistent.

*Invoice discounts* — `Invoice` gained `discountType` (`NONE | PERCENT | FIXED`, default NONE) and `discountValue`. Deliberately invoice-only, not on quotes, per the literal ask. The discount is applied to the pre-VAT base, and VAT is *recomputed* on the discounted base (not a flat cut off the already-VAT'd total) — this is both the accounting-correct way to handle a discounted invoice (VAT is legally owed on what was actually charged) and a judgment call favoring correctness over the simpler-to-code alternative. `Invoice.total` stores the final, already-discounted amount — the same field every existing consumer (KPIs, reports, payment tracking, `amountPaid` comparisons) already reads, so nothing downstream needed to change. **Caught and fixed a real display bug while testing this**: the invoice detail page's "Discount" summary row was computed as `base + vat - total` using floating-point `vat` (a sum of fractional per-item amounts) against an already-rounded integer `total`, so a genuinely non-discounted invoice could show a spurious "Discount -0 Kč" row from pure rounding noise (e.g. a 23,554 Kč base with 21% VAT lands on `total=28500` vs an unrounded `base+vat=28500.34`). Fixed by rounding `base + vat` before diffing, in both the detail page and `InvoicePdf.tsx`.

*Invoice auto-fill from event* — picking an event in the blank "New invoice" form (`InvoiceForm.tsx`) now auto-fills a single line item with the event's title as the description and its `quotedValue` as the unit price — but only while the item list is still in its untouched default state, and only once, so it never clobbers something already typed and never re-fires on a later re-selection. `LineItemsFields` had to become a fully controlled component (items/onChange lifted into the parent) to make this possible, which also happened to make wiring up the reorder buttons cleaner.

*Unified YY-XXX numbering for events, quotes and invoices* — replaced the old independent per-year `YYYY-Q##`/`YYYY-####` counters with one scheme: `Event.number` (new field) is "YY-XXX" (e.g. `26-001`), assigned once at creation by scanning existing numbers for the current year and taking the **lowest unused** integer — not just count+1 — so a permanently-deleted event's number becomes reusable rather than leaving a permanent gap, exactly as asked. Quotes and invoices don't get independent sequences at all: each reuses its parent event's base number, appending `_v2`, `_v3`, … only once there's more than one document of that type for the same event (the first has no suffix) — `nextQuoteNumber(eventId, eventNumber)`/`nextInvoiceNumber(eventId, eventNumber)` in `document-number.ts` both just `count()` existing documents for that event and format accordingly. A quote and an invoice for the same event can therefore legitimately show the same number string (e.g. both `26-001`) — they're always distinguishable by which section of the app they're in, and this wasn't treated as a collision to avoid. `variableSymbolFor()` (strips non-digits for the QR-payment variable symbol) is unaffected — a number like `26-003_v2` still yields a valid all-digit VS (`260032`). Event number now shown in the events list, the event detail header, and threaded through `quickCreateEventAction` too. Verified end-to-end against the reseeded demo data: Team offsite's two invoices came out `26-003` and `26-003_v2` exactly as the rule predicts, and a fresh event created through the browser picked up the next free number.

*Revert "mark as paid"* — new `revertInvoicePaidAction`, shown as an "Undo mark as paid" button wherever "Mark as paid" would otherwise be, whenever `status === PAID`. Deliberately doesn't just flip status back to `ISSUED` — it recomputes `amountPaid` from the sum of the invoice's *actual* recorded `Payment` rows (ground truth) and re-derives status from that (`PAID` if it still covers the total, `PARTLY_PAID` if partial, `ISSUED` if zero), clearing `paidAt` only if the result isn't `PAID`. This means it only ever undoes the artificial "mark as paid" shortcut's fabricated full-payment bookkeeping — a real payment recorded independently (e.g. via "Record payment") is never at risk of being wiped out by clicking undo. Verified both branches live: an invoice marked-paid with no real `Payment` row reverted cleanly to `ISSUED`/`0 Kč` paid; a seeded invoice with a real matching `Payment` correctly stayed `PAID` after "undo" since there was nothing artificial to undo.

Every item verified via clean `npm run build` + `npm run lint`, direct DB checks (`sqlite3`), and live browser testing through the Chrome extension for every interactive piece — including the discount math end-to-end (created a real 10%-off invoice, decoded its generated PDF with `pdfminer.six`, confirmed Base/VAT/Discount/To-pay all reconcile), the category settings CRUD (add/rename/delete, including the in-app confirm dialog), the up/down reorder buttons, and the event-contact → client-contact sync (including the delete-independence check).

**Nine-item batch: clickable quote/invoice rows, quote accept/decline, demo clients, undo-mark-paid bug, quote creator info (done).**

*Small fixes* — (1) Event detail's "Quotes & invoices" tab rows are now `Link`s straight to the quote/invoice detail page (were plain non-interactive divs); also dropped a stale "this is a read-only view, Finance lands in a later phase" note left over from Phase 1, and fixed the currency arg on `formatCurrency()` there so a non-CZK quote/invoice shows correctly instead of silently defaulting to CZK. (2) Event detail's Edit button now reads "Edit event" instead of the ambiguous "Edit". (3) `/finance/quotes`' per-row action area is now `justify-between` so Delete sits at the row's right edge instead of bunched next to the Convert/Edit/Remind link. (4) Reports → By month's breakdown table no longer filters out zero-activity months — all 12 always show, with `0 Kč` explicit rather than the month just missing from the list (the `byMonth` data already had all 12 initialized to zero; the page was just hiding them). (5) Sidebar nav reordered to Dashboard/Clients/Events/Finance/Time tracker per explicit request.

*Quote accept/decline* — a SENT quote had no way to move to Accepted/Declined short of leaving it stuck forever (Edit is DRAFT-only, and nothing else touched status). New `updateQuoteStatusAction`, scoped deliberately narrow: only fires SENT→ACCEPTED or SENT→DECLINED, silently no-ops on any other current status. "Mark accepted"/"Mark declined" buttons show up next to the existing Convert-to-invoice button, only while status is SENT.

*Quote creator info* — quotes now record who made them: `Quote.createdById` (required FK to `User`, set from the logged-in user in `createQuoteAction`) plus a new `User.phone` field (optional, editable on the Edit User page in Settings, since Users never had a phone before this). The quote detail page's sidebar gained a "Created by" block: name, email, phone, and the creation date — reusing `Quote.issuedAt` rather than adding a redundant `createdAt`, since `issuedAt` is already `@default(now())` and never independently editable, so it already *is* the creation timestamp.

*Demo clients, matching real data* — the demo seed's 7 events previously sat completely unlinked from any `Client` (the Clients section existed but had nothing to show against demo data). `prisma/seed.ts` now creates one `Client` per demo company (Kobra a.s., Nordika, Vela s.r.o., Aeris) with a `ClientContact` that exactly mirrors the matching `EventContact` on each event, an `invoicingEmail`, and links every event's `clientId` — the /clients page and each client's "total charged" rollup now show real, correct numbers instead of an empty list. Also seeded a live example of the discount feature (Team offsite's balance invoice, `26-003_v2`: 10% off, `48 000 → 43 200` base, VAT recomputed on the discounted amount, total `52 272 Kč`) so that feature has real demo data to look at, not just an empty toggle.

**Found and fixed a real bug reported after the previous batch shipped: "undo mark as paid" leaves the invoice permanently un-markable-as-paid again.** Reproduced directly: for an invoice that reached PAID via a *real*, fully-covering `Payment` record (not the "mark as paid" shortcut — e.g. every seeded invoice with a matching deposit/payment), clicking "Undo mark as paid" recomputed `amountPaid` from real payments (unchanged, since it was already fully real), so status stayed PAID — but the button never went back to "Mark as paid", and worse, it logged a misleading "Undid 'mark as paid'" history entry for an action that changed nothing. Root cause: the button was shown for *any* PAID invoice, not just ones where the PAID status came from the artificial shortcut. Fixed two ways: `revertInvoicePaidAction` now no-ops entirely (no DB write, no history entry) when real payments already cover the total; and the invoice detail page only renders "Undo mark as paid" when `realAmountPaid < total` — i.e. only when there's actually something artificial to undo. Verified both paths: a shortcut-marked invoice with no real payment correctly cycles Mark→Undo→Mark again; a genuinely fully-paid invoice (real `Payment` = total) shows neither button, since there's nothing left to toggle.

Every item verified live in the browser (Chrome extension) against the reseeded demo data, plus `npm run build` + `npm run lint` clean, plus direct `sqlite3` checks and a decoded PDF (`pdfminer.six`) confirming the demo discount invoice's Base/VAT/Discount/To-pay all reconcile.

**Four-item batch: duplicate a declined quote, Reports "Overview" first, expense editing, finance-list links fixed to their own detail page (done).**

*Duplicate a declined quote* — new `duplicateQuoteAction`: copies the event, line items (incl. category), currency and hide-item-prices setting from a DECLINED quote into a fresh DRAFT (own new `createdById`/`issuedAt`, own next `_v#` number off the same event), resets `validUntil` to two weeks out rather than copying the old — likely already expired — date, and redirects straight to the new quote's edit page. "Create new (duplicate)" button on the quote detail page and a matching "Create new →" row-action on the quotes list, both gated to `status === "DECLINED"`.

*Reports "Overview" first* — the by-month view (already the default) is now also the first/leftmost tab and relabeled "Overview" instead of "By month"; "By event"/"By category" unchanged. Tab order was previously `event, month, category` with `month` as the default `view` — reordered the array to `month, event, category` so the visually-first tab matches the one that was already selected by default.

*Expense editing* — expenses previously had create + delete but no edit. New `updateExpenseAction` mirrors `createExpenseAction`'s validation/permission logic exactly, plus receipt handling: keep the existing file untouched, replace it with a newly-uploaded one (deleting the old file from disk), or explicitly remove it via a new "Remove" checkbox in `ReceiptInput.tsx` (which now optionally shows "Current receipt on file" with a link, when editing). New `canEditExpense()` in `authz.ts` — full expense access (Admin/Accountant) can edit any expense; everyone else only their own (`paidById === user.id`), matching who could have logged it in the first place, not scoped by current event access (so losing access to an event later doesn't lock you out of correcting your own historical entry on it). "Edit" link added to every expense-list row where `canEditExpense` allows it — a new grid column, always reserved (permission varies per-row, not a fixed admin/non-admin split like the existing Delete column).

*Finance list links fixed* — on `/finance/quotes` and `/finance/invoices`, each row had two competing links: the document number correctly went to its own detail page, but the event-title text went to `/events/{id}` instead — a real UX inconsistency (reported directly: "the lines... relink you to quote/invoice details, not to event details"). Both links now point to the same quote/invoice detail page; getting to the linked event is still one click away from inside that detail page's own "Linked" section, same as before.

Every item verified live in the browser against reseeded demo data (duplicate flow produced `26-005_v2` from a declined `26-005` correctly; expense edit round-tripped an amount change end-to-end with the list total recalculating), plus clean `npm run build` + `npm run lint`.

**Unified row-hover style: quotes/invoices lists, extended to events (done).** The events list already had a subtle `hover:bg-ink/5` row-background lighten (its rows are a single `<Link>` per row); quotes/invoices didn't have that at all, and neither list turned any text orange on hover. Standardized on: row background lightens (`hover:bg-ink/5`) *and* the row's descriptive text turns the accent orange, applied via a `group` class on the row plus `group-hover:text-accent` on plain text and `group-hover:!text-accent` on `.placeholder-text`/`.label` elements — the `!` is required because those two classes are plain unlayered CSS in `globals.css`, which otherwise beats a non-`!important` Tailwind utility under CSS Cascade Layers rules (the same root cause as the invoice-bucket-filter contrast bug fixed earlier in this project). Deliberately scoped to non-interactive descriptive text only — status pills (their color carries real meaning: overdue, to-invoice, etc.) and the row's own action controls (Convert/Edit/Delete/Download, which already have their own local `hover:text-accent`) are left alone, so hovering the row doesn't repaint a status pill's meaning or drown out a specific button's own hover feedback. Verified visually (zoomed screenshots) on all three lists — quotes, invoices, and events — confirming the row background lightens, all plain/placeholder text goes orange, and status pills keep their distinct styling in each case.

**Client detail page redesign + two small polish items (done).** (1) The client detail page's event list gained a price column — name, date, price (`Event.quotedValue`, the "only new attribute"), status — matching what a producer actually wants to see at a glance without opening each event. (2) "+ New contact" now opens a centered modal (`AddContactModal.tsx`, built on the existing `Modal.tsx`) instead of the old always-visible inline form row — `addClientContactAction` was converted from a bare `void`-returning action to a `useActionState`-compatible one returning `{error?, success?}`, so the modal can auto-close itself via the same "watch `state.success` in a `useEffect`" pattern already used by `QuickCreateEventModal`. (3) Header restructured: the client name stays top-left but at literally double the size (`text-[2.5rem]`, up from `text-xl`'s 1.25rem), and IČO/DIČ moved out of the header into the right-hand info column in the requested order — Address, IČO, DIČ, Invoicing email, (Note, if present), Total charged, Delete — each its own labeled block instead of IČO+DIČ sharing one header line. While already touching this row template for the price column, also brought the event sub-list's hover behavior in line with the row-hover standardization from the previous turn (background lightens + text goes accent orange, status pill untouched) — it previously only had the background half of that effect.

*Two more small ones*: `DownloadPdfButton.tsx`'s EN/CZ dropdown now lists Čeština first, English second (was the reverse) — this app's actual documents are Czech-first in practice. `LineItemsFields.tsx`'s column headers (Description/Category/Qty/Unit price/VAT %) — removing their `pl-2.5` in this same turn was necessary but **not sufficient**; the user immediately reported the labels were still visibly offset, worsening left-to-right (Category/Qty/Unit-price/VAT drifting further from their boxes the further right they sat), confirmed from a screenshot. Real root cause, found on the second pass: the header row and every item row were each their *own* separate CSS Grid container sharing the same `grid-cols-[24px_1fr_110px_70px_110px_70px_auto]` template string — but the header row rendered nothing in the trailing `auto` column (no Remove button there) while every item row did, so each grid's `auto` track resolved to a *different* width, which cascaded into a different `1fr` (Description) width, offsetting every column after it between the header and the rows. Two grids with an identical template string are not guaranteed identical column widths if their content differs — only a single shared grid formatting context is. Fixed by merging the header spans and every item row's cells into *one* flat grid (using `Fragment` per item row so React doesn't need an extra wrapper element that would itself become a stray grid item) — CSS Grid's implicit row-wrapping places 7 children per row automatically, and one grid can only ever compute one width per column. Verified with two line items in the browser (zoomed screenshot): header, row one, and row two all align pixel-for-pixel now.

Verified live in the browser: the new contact modal round-tripped a real contact end-to-end (added, auto-closed, appeared in the list); zoomed screenshots confirmed the line-item labels now sit flush with their input boxes and the PDF dropdown's language order.

**Pinned sidebar now stays fixed to the viewport on tall pages (done).** Reported directly: on a long page (New Event was the example), Settings and the sign-out block at the sidebar's bottom were impossible to reach without scrolling the whole page past them. Root cause: the *pinned* sidebar (the default state — see the auto-hide/pin rework earlier in this file) was `shrink-0` and nothing else — a completely normal-flow flex child with no positioning of its own, so once `<main>`'s content grew taller than the viewport, the whole page scrolled and the sidebar scrolled away with it like any other content. (The *unpinned*/auto-hide state was never affected — it's already `fixed inset-y-0`, which is relative to the viewport regardless of page scroll.) Fixed by making the pinned state `sticky top-0 h-screen overflow-y-auto` instead of just `shrink-0` — `position: sticky` keeps it pinned to the top of the viewport for the full scroll of a tall `<main>`, `h-screen` caps it at exactly 100vh so it can never itself grow past the viewport, and `overflow-y-auto` is there defensively in case the sidebar's own content ever exceeds that (not currently the case). Verified in the browser: scrolled the New Event form (long enough to need real scrolling) all the way down — Settings and the user/sign-out block stayed visible in the sidebar throughout.

**"Glass & gradient" redesign — Foundation (done).** After several turns of mocking up a new visual direction as editable Claude Design canvases (rounded corners, a frosted-glass sidebar, gradient background, a semantic tag-color system), the user asked to implement it for real. This **supersedes the Phase-1 "0 border-radius, hairline rules only" decision** documented earlier in this file — the app is no longer sharp-cornered by design. Scoped via Plan Mode into a foundation pass (this entry) followed by six page-by-page passes, desktop-only for now (none of the five non-Dashboard mockups got a mobile layout, so mobile stays a separate later pass rather than inventing unreviewed UI).

Foundation pieces, all in `globals.css` plus three components: (1) two new semantic color tokens, `--color-positive` (`#2dd4bf`, teal — orange's complement) and `--color-warning` (`#dc2626`, red) — `--color-accent` (orange) is unchanged and stays the general action color; (2) `.tag`/`.tag-positive`/`.tag-neutral`/`.tag-warning`, a soft-filled-chip status class replacing the old bordered `.pill`/`.pill-red` everywhere it was used (`StatusPill.tsx`'s three pill components, the dashboard timeline's kind badge, every non-CZK currency badge across quotes/invoices, Settings' Inactive/Active/Not-connected badges) — `.pill`/`.pill-red` had zero remaining callers after the migration, so they were deleted rather than left dead; (3) `.card` (gradient-tinted rounded panel) and `.glass-panel` (frosted/blurred panel) utility classes; (4) `.btn`/`.btno`/`.input` all got `border-radius` added (pill-shaped buttons, 10px inputs); (5) a subtle two-tone radial-gradient background replacing the flat fill, `background-attachment: fixed` so it doesn't reflow oddly on tall scrolling pages.

`StatusPill.tsx`'s three components were remapped onto the new tag variants rather than just re-skinned: **positive** (teal) = `EventStatus.CONFIRMED`, `QuoteStatus.ACCEPTED`, `InvoiceStatus.PAID`; **warning** (red) = `CANCELLED`, `TO_INVOICE` (unchanged from the existing `EVENT_STATUS_ATTENTION` red treatment), `QuoteStatus.DECLINED`, invoice Overdue, and — new — an invoice due in **under 3 days**; everything else neutral, including the existing 3–7-day "Due soon" window (deliberately left unchanged in wording/threshold — the new <3-day rule only narrows the top end of that range into its own more urgent state, it doesn't replace it). `Sidebar.tsx` picked up `.glass-panel` plus flush-left/rounded-right positioning — no logic changes, since the pin/unpin + hover-reveal interaction was already fully built. `TimerWidget.tsx`'s running state got a small square icon Stop button instead of a text button (the Stop functionality itself already existed). New `src/components/ui/SegmentedTabs.tsx` — a reusable glass-pill segmented control with a sliding highlight that tracks each option's *measured* width/position via `getBoundingClientRect` (not assumed-equal widths, since real labels vary in length) — built to replace the hand-rolled view-switchers on Dashboard/Events/Time-tracker in the page-by-page passes that follow.

**Real bug caught and fixed while verifying live**: the first pass at `.glass-panel` (light blur, low-opacity tint) looked fine for the *pinned* sidebar — which only ever sits over the body's own gradient — but was unreadable for the *unpinned, hover-revealed* sidebar, which floats directly over real page content: a bold 30px page heading stayed crisply legible right through the "glass," clashing badly with the sidebar's own text on top (caught via a zoomed screenshot mid-interaction, not visible at normal viewing scale). Root cause: blur alone doesn't obscure sharp high-contrast text the way it does a busy photo — frosted-glass UIs need a tinted, partially-opaque backing layer under the blur, not just the blur. Fixed by compositing a `color-mix(in srgb, var(--color-bg) 72%, transparent)` layer under the existing gradient tint and bumping blur from 24px to 28px. Verified by re-triggering the exact same unpinned-hover overlay state and confirming the page content underneath is now a soft illegible smear rather than readable text, while the pinned/docked state (checked separately) still reads as a light, non-muddy glass tint.

Verified via clean `npm run build` + `npm run lint`, plus a live browser pass against the reseeded demo data: sidebar pin/unpin cycle (including the glass-legibility fix above), an idle timer widget rendering in the new card style, and — without touching `finance/invoices/page.tsx` at all — confirmed the `StatusPill` remap alone was enough to correctly show `PAID 26 MAR`/`PAID 1 AUG` in teal, `OVERDUE 33 D` in red, and `DUE SOON`/`PARTLY PAID` in neutral gray on a real invoices list.

**"Glass & gradient" redesign — Dashboard (done).** First of the six page-by-page passes following the Foundation entry above. `src/app/(app)/dashboard/page.tsx`'s List view rebuilt onto the new system: the header/view-switcher row is now a `sticky top-0` blurred glass toolbar (bleeds edge-to-edge via negative margins matching `<main>`'s own padding, then re-adds it) so it stays pinned while content scrolls under it; the hand-rolled List/Timeline/Calendar switcher is now the shared `SegmentedTabs` component; the three "Needs attention" tiles are rounded `.card`s with a small icon chip each (alert-triangle/paper-plane/invoice-plus) and a real type-scale jump (32px value vs. the previous 20px) — the overdue tile now genuinely uses `--color-warning` end to end (top rule, icon chip, label, value) instead of the old accent-orange "attention" treatment; upcoming-event cards are rounded `.card`s (dropped the placeholder image strip, which never held a real image); the Balance chart's bars got gradient fills and rounded tops, plus their month labels now render as text under each pair (the data already had them, the old markup just never displayed them). Timeline and Calendar sub-views were left as-is internally (`TimelineList`, `WeekCalendar`) — they already sit inside the same new toolbar/shell, and the timeline's kind badges (`Build`/`Start`/`Milestone`) picked up the new neutral tag styling automatically via the Foundation's `.pill`→`.tag` migration.

Verified via clean `npm run build` + `npm run lint`, plus live browser checks on real seeded data: all three `SegmentedTabs` options (List/Timeline/Calendar) actually switch views with the glass thumb correctly tracking the clicked option (checked via a zoomed screenshot, since the unzoomed one was hard to judge precisely), the overdue tile's full red treatment, and status tags rendering correctly in both the event cards and the calendar view's "This week's events" legend.

**"Glass & gradient" redesign — Events (done).** Second page-by-page pass. `events/page.tsx` (list, both Table and Calendar sub-views), `events/[id]/layout.tsx` (header/tabs), and `events/[id]/page.tsx` (Overview) all moved onto the new system: sticky blurred glass toolbars, the hand-rolled Table/Calendar switcher replaced with the shared `SegmentedTabs`, rounded hover rows, and — the one real layout change here, not just a re-skin — the detail page's right column went from one continuous `border-l` strip to four separate glass `.card` sections (Budget vs actual / Time logged / Documents / Team), matching the approved mockup instead of reading as one long list.

**Also swept the new warning-red color across every destructive/error UI the codebase has**, not just what this page touches — found while looking at `DeleteEventButton` still rendering accent-orange next to the now-teal Confirmed pill and it read as visually wrong. `!border-accent text-accent` → `!border-warning text-warning` (or the bare `text-accent`/`bg-accent`/`border-accent` equivalent) on: every delete button (`ConfirmDeleteButton`, `DeleteEventButton`, `DeactivateButton`, `DeleteEntryButton`'s hover, the Custom-role and Category delete links' hover), every form's `state.error`/`ares.error` validation message (15 files — login, event/client forms, every modal, every settings tab, every finance form, time-entry forms), and the two date-conflict indicators (`QuoteForm`'s validity-date clash, invoice/quote detail pages' Overdue/Expired status boxes and the invoice payment-progress bar). All were genuinely "something needs attention" states that were only ever orange because that was the only non-neutral color available before this redesign — none of them were meant to look like a primary action, so this isn't a stylistic preference, it's correcting a semantic mismatch the new red/orange distinction exposed. Deliberately left alone: active-tab underlines (`EventTabs`, `FinanceTabs`, the Settings tab bar) and hover states on non-destructive controls (calendar milestone chips, the receipt drag-and-drop zone) — those are genuinely general-purpose accent uses, not warnings.

Verified via clean `npm run build` + `npm run lint`, plus a live browser pass: the Events table (`TO INVOICE` correctly red, `CONFIRMED` teal, others neutral), the Table/Calendar `SegmentedTabs` switch, and the event detail page's four new glass cards on real seeded data — then re-checked the Delete Event button specifically (zoomed screenshot, before/after) to confirm it now reads as a distinct red rather than the same orange as a primary action next to it.

**"Glass & gradient" redesign — Clients (done).** Third page-by-page pass: list, detail, and both forms (`clients/new`, `clients/[id]/edit`, both backed by `ClientForm.tsx`). Sticky glass toolbar on the list, rounded hover rows on both the client list and the detail page's linked-events sub-list, the right info column wrapped as one glass `.card` (was plain label/value blocks under a bare `border-l`), `ClientForm` wrapped in a `.card` too. `ConfirmDeleteButton` already went warning-red app-wide in the Events pass, so "Delete client" needed no color change here — it got an explicit `w-full text-center` override instead, matching the mockup's ask for a full-width button rather than an inline text-sized one.

Verified via clean `npm run build` + `npm run lint`, plus a live browser pass on real seeded data: the clients list, a real client detail page (Kobra a.s. — linked events with Confirmed/Inquiry tags, contacts, the glass info card, the now full-width red Delete client button), and the New client form rendering as a rounded card with working inputs.

**"Glass & gradient" redesign — Finance (done).** Fourth page-by-page pass: `finance/layout.tsx` (shared header/tabs for the whole section), Invoices (list with KPI strip + detail), and — extended beyond the plan's original "Invoices" scope for consistency, since Quotes/Expenses sit under the same `FinanceTabs` and would otherwise look visibly stale next to a redesigned Invoices — Quotes (list + detail) and Expenses (list, rounded rows only, no structural change). KPI tiles are now glass `.card`s with the Overdue tile in full warning-red (top rule/label/value, replacing the old plain `text-accent` "attention" treatment); bucket filters became real pill chips with a filled-orange active state; both invoice and quote detail pages split their right column into separate glass cards (Payment state / Record payment / Linked / History / Delete for invoices; Status / Linked / Created by / Delete for quotes) instead of one continuous list; the payment-progress bar is rounded and switches to warning-red only when actually overdue (accent otherwise, matching the KPI convention).

**Found three more `ConfirmDeleteButton` callers with an explicit `className` override still hardcoding accent-orange** that the Events pass's app-wide color sweep couldn't catch (that sweep searched for `text-accent` co-occurring with a destructive keyword on the *same line*; these three had the destructive context one JSX prop away, not in the className string itself) — quotes-list's inline delete, expenses-list's inline delete, and milestone-list's inline delete. All three now `hover:text-warning`, matching every other delete affordance in the app.

Per explicit instruction this turn, **not independently verified in the browser** — the user is reviewing this pass and the remaining two (Time tracker, Settings) themselves. Verified only via clean `npm run build` + `npm run lint`.

**"Glass & gradient" redesign — Time tracker (done).** Fifth page-by-page pass, scoped to Tracking (`time-tracker/layout.tsx`, `tracking/page.tsx`, `RunningTimerBox.tsx`, `ManualEntryForm.tsx`) per the plan — Compare Events wasn't touched this pass. The timer is now a real hero glass card at the top in both states (idle "Start a timer" inline form and running, with the elapsed-time figure bumped to 38px), not a small boxed widget; the Day/Week/Month switch is the shared `SegmentedTabs`; entries are rounded hover rows; Manual entry and This-week totals are separate glass cards in the sidebar.

Per explicit instruction this turn, **not independently verified in the browser** — verified only via clean `npm run build` + `npm run lint`.

**"Glass & gradient" redesign — Settings (done).** Sixth and final page-by-page pass: `settings/page.tsx` (sticky glass header/tabs), Users & roles (`CreateUserForm` as a glass card, `UsersTable` rounded rows with a real check-icon swatch replacing the raw `✓`/`—` characters for the company-card column, `RoleReferenceTable` as a card — kept as a real 5-column table rather than forced into the mockup's simplified per-role card grid, since the mockup never had the actual per-dimension access data in front of it and the real table is more informative), Custom roles (`RolesTab`, same card treatment), and Company (`CompanySettingsForm` — card-wrapped form and Connected-accounts panel, rounded logo preview/accent swatch). Also the Edit User page/form.

**Fixed the real stale-copy bug flagged back in the Finance pass**: the Numbering section was still showing pre-unification placeholder text ("Invoices YYYY-NNNN" / "Quotes YYYY-Q##" / "Restarts each year — fixed") from before numbering was unified onto the single `YY-XXX` scheme — now reads "Events, quotes & invoices — YY-XXX" / "Reused per event · lowest unused number", matching what `document-number.ts` actually does.

Per explicit instruction this turn, **not independently verified in the browser** — verified only via clean `npm run build` + `npm run lint`. This completes the "glass & gradient" redesign across all six planned areas (Dashboard, Events, Clients, Finance, Time tracker, Settings); mobile/responsive layouts remain a separate future pass per the scope decision made at the start of this work.

**Deferred to later phases (not built yet):** Google Calendar sync, Google OAuth login, general audit log (beyond the invoice-specific action log), PWA offline queue, expense approval workflow (the field and hint text exist, nothing enforces it), a custom-styled date/time picker (native pickers can't be recolored for their popup highlight — see the calendar-highlight note above; offered, user deferred it). Mobile/responsive layouts (deferred here originally) are now built — see the "Mobile design pass" and "Mobile-responsive implementation" entries further down. Invoice-sending emails (deferred here originally) are now built too — see the "invoice emailing" entry further down; it just needs real `SMTP_*` credentials in the environment to actually send, which the user hasn't supplied yet. Every nav item is now live; nothing lands on a placeholder anymore.

**Design backlog — per-category expense icons (not built yet):** during the Dashboard redesign pass (design-canvas mockup, not yet implemented in the app), the user asked for a small monochrome icon per expense category wherever expenses are listed (Dashboard's "Latest expenses", the Expenses list page) — explicitly deferred for a later design/implementation pass, noted here so it isn't lost. Requested mapping: Food — meal/drink → an eating-out icon; Food — grocery (rename this category) → shopping basket; Travel — gas → gas pump; Travel — car → car; Travel — flight → plane; Travel — taxi → taxi; Travel — parking → parking sign; Travel — public transport → bus; Gear → AV/technical-equipment icon; generic/other → tentatively a credit card. All icons monochromatic (single stroke color, matching the app's existing icon style), not multi-color illustrations.

**Five-item batch, items 1–3: Client ARES lookup, structured client address, quote/invoice blank-row fix (done; items 4–5 of this batch still pending).** (1) `ClientForm.tsx` gained the same "Load from ARES" button/`useAresLookup()` pattern already used by `EventForm`/`CompanySettingsForm` — was simply missing despite the IČO field already existing. (2) `Client.address: String` split into `street`/`city`/`postCode`/`state` (all `String @default("")`) — a clean migration since the dev DB was empty at the time. `src/lib/address-search.ts`'s `AddressSuggestion` now also returns Photon's structured `street`/`city`/`postcode`/`state` parts (previously collapsed into one `label` string and discarded), and new `src/components/ui/StructuredAddressInput.tsx` is a four-field component (Client-only for now — `EventForm`/`CompanySettingsForm` keep their single-string `AddressAutocompleteInput`, which serves a different embedded-PDF-address purpose and wasn't part of this request) where the street field drives the same Photon dropdown and selecting a suggestion fills all four at once; fields not returned by a given suggestion (e.g. a landmark result with no postcode/state) are simply left as-is rather than overwritten with blanks (`street: s.street || value.street`). `formatClientAddress()` in `format.ts` renders the four fields as street / "postcode city" / state, used by the client detail page and by `EventForm`'s `ClientPicker` autofill. `prisma/seed.ts`'s four demo clients now carry real structured Czech addresses. (3) Quote/invoice line items could get "stuck" — `LineItemsFields.tsx`'s description `<input>` had a bare HTML `required` attribute, so the browser blocked submission if a freshly-added, still-empty trailing row existed, even though `parseItems()` in `src/lib/actions/finance.ts` already silently skips any row with a blank description server-side (confirmed by reading it — the "at least one item" check runs after that filter). Fix was just dropping `required` and adding a placeholder; no server-side change needed. Verified live in the browser: created a client via ARES (IČO `21342601`) with a manually-completed address, confirmed the 3-line address render on the detail page; typed a landmark search into the street field on both the New and Edit client forms and confirmed the Photon dropdown appears and selecting a result fills city (leaving postcode/state blank when Photon's result lacked them, as designed); submitted a real quote with a second, deliberately-untouched blank row (defaults: qty 1, VAT 21, no description) and confirmed it saved successfully with only the one real line item. Build + lint clean.

**Five-item batch, item 4: Settings reorganization (done; item 5 still pending).** Tabs reordered/renamed to Company, Users & roles, Templates (was Users & roles, Custom roles, Company, Categories) — Company now defaults first when the current user has company access. The standalone "Custom roles" tab is gone; its create/edit/delete capability was merged directly into the "What each role can reach" reference table (`RoleReferenceTable.tsx`, now a client component absorbing what used to be `RolesTab.tsx`, which was deleted) — a "+ New role" button sits next to that section's heading, and once any custom role exists (or the create form is open) a "Custom roles" divider/heading separates the four built-in rows from the custom ones; each custom row keeps its own inline Edit/Delete (built-in rows get no actions, since they're not editable). The new Templates tab is exactly the pre-existing `CategoriesTab` component, unchanged, just relocated under a "Quote" section heading — categories are specifically quote/invoice line-item categories, so the heading makes that scope explicit now that it's not a standalone "Categories" tab. **Found and fixed a real bug while testing this**: the ported custom-role delete handler called the `useActionState` dispatch function directly from a `button onClick` (copied verbatim from the old `RolesTab.tsx`), which is exactly the anti-pattern already documented and fixed elsewhere in this app (see the "Fixed the real confirm-dialog race" entry) — except this one spot had been missed. It didn't silently misfire like the form-action race did; instead React 16.3's dev overlay surfaced it directly as a console error ("An async function with useActionState was called outside of a transition"). Fixed by wrapping the call in `startTransition()`, matching the pattern used everywhere else. Verified live in the browser: tab order and default landing tab, created a custom role from the merged view (divider and inline actions appeared correctly), deleted it via the in-app confirm dialog with no console error on either create or delete, and confirmed the Templates tab renders the categories list under the new "Quote" heading. Build + lint clean.

**Five-item batch, item 5: Time tracker overhaul — Timer/Manual switch, day/week/month navigation, Calendar view (done — batch complete).** (a) The Tracking page's hero spot now shows a Timer/Manual `SegmentedTabs` toggle (`?mode=timer|manual`, defaults to Timer) instead of always showing both a "Start a timer" card and a separate sidebar "Manual entry" card simultaneously — picking Manual swaps the hero spot to `ManualEntryForm` (redesigned from its old narrow sidebar-card layout into a wider 3-column grid to fit the hero spot) and the sidebar's standalone copy is gone. The toggle is moot whenever a timer is actually running — `RunningTimerBox`'s running state always takes over the hero spot regardless of `mode`, unchanged from before. (b) Added real day/week/month navigation to the entries list, which previously could only ever show "the current day/week/month" — a new `date` search param (anchor, defaults to today) threads through `getMyTimeTrackerData(user, period, anchor)` (`src/lib/queries/timetracker.ts`, reworked to take a real `[from, to)` range via new `rangeStart`/`rangeEnd` instead of an open-ended `gte` filter, which is also what makes navigating to a *past* period show only that period's entries instead of everything up to now), with prev/next/Today links stepping by one day/7 days/one calendar month depending on the active period — same interaction shape as the existing `WeekNav`, but period-aware so it's inlined directly in the page rather than reusing that component. The sidebar totals card (`periodTotals`/`periodTotalMinutes`, renamed from the old hardcoded `weekTotals`) now reflects whatever period/date is actually being viewed, labeled "Today"/"This week"/"This month" only when the anchor is genuinely inside the real current period, and the full `periodLabel` (e.g. "Week 33 · 10–16 Aug 2026") when browsing elsewhere — otherwise a user paging into last month would see a misleading "This month" heading over stale-looking data. (c) New Calendar view (`?view=calendar`, `List`/`Calendar` `SegmentedTabs` next to "My entries", mirroring Events' own Table/Calendar switch including its `week` param convention) — new `TimeTrackerCalendar.tsx`, an hourly-grid component in the same family as `WeekCalendar.tsx` (same `assignColumns` overlap logic, same day-header row) but built fresh rather than reused, since it's positioning time entries, not multi-day event spans; entries with a real clock time render as positioned blocks (top/height derived from `startedAt`/`endedAt`), duration-only entries render as compact chips in an "Unscheduled" lane above the grid (mirroring `WeekCalendar`'s all-day bar), and every block shows exactly the three confirmed lines — event title, description (only if non-blank), duration — never a 4th field. This surfaced a real, load-bearing gap: manual entries never persisted `startedAt`/`endedAt` even when the form's start/end fields were filled in (only used transiently to compute a minute count, then discarded) — a duration-only row has nothing to position on an hourly grid. Fixed in `src/lib/actions/timetracker.ts`: `computeMinutesAndTimes()` (replacing the old `computeMinutes()`) now also returns real `Date` objects when start+end are both given, and both manual-entry actions write them to `TimeEntry.startedAt`/`endedAt` (already-nullable columns from Phase 3 — no migration needed); `EditEntryForm` gained optional `startTime`/`endTime` props so editing a scheduled entry doesn't lose its clock time. While touching these two files, also fixed the same UTC/local date bug documented earlier in this file (`toISOString().slice(0,10)` silently rolling the date back a day) in both `ManualEntryForm`'s default date and the edit page's date prefill — switched both to the existing `isoDate()` helper from `src/lib/calendar.ts`, the same fix already applied to the Dashboard/Events calendars. Verified live in the browser end-to-end: toggled Timer↔Manual with no running timer: added a manual entry with start/end times (09:00–11:30, correctly computed 2:30) and a separate duration-only entry (90 min, no start/end) — Week view correctly listed both with the right per-event sidebar breakdown; Month view correctly pulled in an earlier-in-the-month seeded entry alongside the two new ones under the "August 2026" heading; Calendar view showed the timed entry as a positioned 09:00–11:30 block with all three expected lines and the duration-only entry as an Unscheduled-lane chip on the correct day; the Edit page correctly prefilled Start/End time from the persisted values. Build + lint clean throughout; test entries removed from the dev DB after verification.

**Five-item feedback batch: Calendar view keeps the timer, floating sidebar (pin/unpin removed), single-arrow dropdowns, semibold "New X" buttons, Compare Events replaced with Overview (done).** Five items from direct user feedback on the prior batch.

*Time tracker Calendar view regression fix* — the previous turn's Calendar view had dropped the "Start a timer"/running-timer hero card entirely (a judgment call made without asking, since Events' own Table/Calendar switch doesn't carry anything analogous into its Calendar view) — user reported it should stay. Also reported the List/Calendar switcher jumped position between the two views (it sat inline next to "My entries" in List view but right at the very top in Calendar view, since Calendar view had no hero card above it). Restructured `tracking/page.tsx`: the hero (running timer, or the Timer/Manual toggle when idle) and the List/Calendar switcher are now both rendered once, in the same position, before branching into the List-specific or Calendar-specific body — neither moves or disappears when switching views. `getTimeTrackerCalendarData` gained a `running` field (previously only `getMyTimeTrackerData` had it) since the hero now needs it regardless of which view is active.

*Floating sidebar, pin/unpin removed* — per explicit request, deleted the pin/unpin toggle and its hover-to-reveal auto-hide mechanic entirely (the corner button, the `pinned`/`hovering` state, the invisible hot-zone div) — `Sidebar.tsx` is back to a plain function of `pathname` with no interactive chrome beyond nav links. In its place: an always-visible "floating" sidebar — `sticky top-3 h-[calc(100vh-24px)] ml-3 rounded-2xl` instead of the old pinned state's flush-left `sticky top-0 h-screen rounded-r-2xl border-l-0` — margin on all sides and rounded on all four corners rather than just the right two. Confirmed the "stays pinned to the viewport on tall pages" fix from an earlier turn survived the rewrite (`top-3`/`h-[calc(100vh-24px)]` is just the pinned math with a symmetric inset, not a different mechanism) by scrolling the New Event form to the bottom and checking the sidebar stayed in place.

*Double dropdown-arrow fix* — every filter `<select>` across Quotes/Invoices/Expenses/Events (`Status`, `Event`, `Category`, `Client`, `Place`) had a hardcoded "▾" character baked into its placeholder `<option>` text (e.g. `<option>Status ▾</option>`), stacking with the browser's own native select chevron to show two arrows — confirmed from a user-supplied screenshot (`docs/ERROR/drop down error.png`) showing exactly that, next to a third dropdown (`year`, no hardcoded arrow) rendering correctly with just one. Fixed by deleting the hardcoded arrow from all 7 occurrences — the native chevron alone is the affordance now, consistent with every other select in the app.

*Semibold "New X" buttons* — added `font-semibold` to the "New event"/"New client"/"New quote"/"New invoice"/"New expense" create-shortcut buttons across Dashboard, Events, Clients, and all three Finance list pages, plus the event-detail "New quote for this event" shortcut and `EventPicker`'s inline "+ New event" button — `.btn`/`.btno` carry no font-weight of their own (inherit the body's 500), so these previously looked the same weight as every other button; scoped narrowly to the create-shortcut family the user named, not a blanket change to every `.btn` in the app (e.g. "Save changes", "Create quote" etc. are untouched).

*Compare Events → Overview* — per explicit request ("change the compare tab to overview"), replaced the event-comparison feature (hours-by-phase stacked bar, cost-per-hour, per-person-by-event matrix — Phase 3's "5c") with a new team time-overview screen at the same tab position: `/time-tracker/compare` → `/time-tracker/overview`, tab label "Compare events" → "Overview". New screen: Day/Week/Month `SegmentedTabs` (reusing the exact period semantics `rangeStart`/`rangeEnd` already established for the Tracking page) with prev/next/Today navigation plus a native `<input type="date">` "jump to" picker (`DateJumpPicker.tsx`, a small client component — its own browser popup **is** the "select the date using calendar" ask, no custom calendar widget needed) — extracted the shared `stepDate`/`periodLabel`/`periodNounFor`/`currentPeriodLabel` helpers out of `tracking/page.tsx` into `src/lib/period-nav.ts` so both pages share one implementation. A People filter (multi-select via the same "chip to remove / + name to add" URL-param pattern the old Compare Events page used for events, now for people) lets the viewer see themselves alone (the default) or add any other active user — deliberately visible to everyone with Time-tracker access rather than gated by a new RBAC dimension, since this is a small internal tool and the "per person" breakdown already showed every team member's name across an event with no extra permission check before this. Day view shows each selected person as a card with their total plus a by-event breakdown (a day-by-day split of a single day isn't meaningful); Week view buckets by the 7 calendar days; Month view buckets by week-within-month (clipped to the actual month boundary so a partial first/last week doesn't pull in adjacent-month days) — a day-by-day grid for a whole month would be 30+ unreadable columns. `getCompareEventsData` and its CSV-export route were deleted outright (fully superseded, not kept alongside); the new screen doesn't have a CSV export — out of the stated scope, not an oversight, easy to add later if wanted. Verified live in the browser against reseeded demo data: added/removed people via chips, paged Week 34→33 and back, switched to Month and confirmed real seeded hours landed in the correct week-bucket columns (Admin User 20:00 in Week 1, Eva Kučerová 29:00 in Week 2 of August), and confirmed Day view's per-person cards.

Every item verified live in the browser against reseeded demo data (dev DB reseeded to empty again afterward per standing convention), plus clean `npm run build` + `npm run lint`.

**App-wide color customization + English/Czech language switch (done).** A new "App settings" tab in Settings (Company / Users & roles / Templates / **App settings**, gated the same as Company — Admin/Accountant) with two company-wide (not per-user) features, both stored on the `CompanySettings` singleton so they follow the same precedent `accentColor`/`logoPath` already set:

*Color customization* — 6 new `CompanySettings` columns (`bgColor`/`surfaceColor`/`inkColor`/`accentColor` [existing, now dual-purpose]/`positiveColor`/`warningColor`), each defaulting to the exact hex already hardcoded in `globals.css`'s `:root`, so nothing changes visually until someone actually edits a swatch. `src/lib/theme.ts` (`getAppColors()`, `colorsToCssVars()`) turns those into inline CSS custom-property overrides injected on `<html>`'s `style` prop by the root `layout.tsx` (now `async`) — since Tailwind v4's `@theme inline` block already maps every `bg-accent`/`text-ink`/etc. utility through `var(--color-*)`, this one inline-style injection repaints the *entire* app (buttons, active nav, status tags, focus rings) with no per-component changes, and it covers `/login` too since that route sits under the same root layout. `--color-accent-hover` changed from a separately-stored hex to a `color-mix(in srgb, var(--color-accent) 85%, black)` formula in `globals.css`, so a custom accent always gets a coherent hover shade without needing a 7th stored color. `AppSettingsForm.tsx` (6 colour-swatch pairs with a one-line hint under each naming exactly what it paints, a live 6-chip preview strip, and a "Reset to defaults" button) replaces the old lone "Accent colour" field that used to live in Company settings' Branding section (removed from `CompanySettingsForm.tsx` — a small note there points to where it moved, since both edited the same underlying field and having two places to change it would've been confusing).

*Language switch* — `CompanySettings.locale` (`"en" | "cs"`, default `"en"`), a two-radio field in the same tab. **This is the single largest diff this project has seen** — essentially every page and component with visible text now reads from a real EN/CZ dictionary rather than hardcoding English, done as a genuinely complete pass (not a token gesture): dashboard, events, clients, finance (quotes/invoices/expenses/reports), time tracker, settings, login, and every shared component (Sidebar/nav, StatusPill's three components, the calendar components, modals, confirm dialogs, buttons). Two deliberate scope boundaries, decided up front rather than discovered as gaps later: date/number formatting (`format.ts`, `Intl.DateTimeFormat`) stays en-GB-shaped everywhere — only UI *text* is translated, reworking every date/currency call site to be locale-aware is a different, much bigger project; and the existing PDF EN/CZ system (`src/lib/pdf/i18n.ts`) is completely separate and untouched — it already lets a PDF's language be chosen per-download independent of the app's UI language.

Architecture: `src/lib/dictionary.ts` is the actual dictionary — `Locale` type, a `czCount(n, one, few, many)` Czech-pluralization helper (a simple 1/2-4/5+ bucket, not full grammatical declension — deliberately not chasing exhaustive perfection per the same "best-effort" spirit as the scope boundaries above), the full `en`/`cs` objects (structured as nested namespaces mirroring the app's IA — `nav`, `common`, `dashboard`, `events` with `form`/`picker`/`milestones`/etc. sub-namespaces, `finance` with `quotes`/`invoices`/`expenses`/`reports`/etc., `timeTracker`, `settings`, `statusEvent`/`statusQuote`/`invoicePill` for `StatusPill.tsx`'s three components, `phases`, `expenseCategories`, `accessLevels`, `roles`), and `getDictionary(locale)`. Deliberately **zero imports of Prisma or anything server-only** — this file is safe to import from Client Components. `src/lib/i18n.ts` re-exports everything from `dictionary.ts` (so existing/new Server Component call sites can keep doing `import { getLocale, getDictionary } from "@/lib/i18n"` unchanged) and additionally exports the one genuinely server-only piece, `getLocale()` (reads `CompanySettings.locale` via Prisma).

**A real, serious architecture bug was hit and fixed while wiring this up, and is worth documenting precisely since it'll recur if this pattern is ever extended:** dynamic dictionary strings (anything parameterized, e.g. `t.dashboard.invoiceOverdue(count)`) are plain JS functions. React Server Components cannot pass a plain function as a prop across the server→client boundary — attempting it throws `Functions cannot be passed directly to Client Components` at runtime (a runtime error, not a build-time one — `next build`'s type-check passed clean every time this bug was present, since TypeScript has no idea about the RSC serialization boundary; it was only caught by actually loading a page in the browser). The first, narrower version of this bug (passing the single `Sidebar`/`TimeTrackerTabs`/etc. dictionary slice as a prop) was found and fixed by auditing every "use client" component's dictionary-typed prop for arrow-function values (`awk`-scripted, not eyeballed) and switching the ones that had any to receive a plain `locale: Locale` string prop instead, calling `getDictionary(locale)` themselves inside the component — a client component computing its own translations from a locale string is fine; only *passing the pre-computed object* from server to client is not. Components nested *inside* another already-"use client" component (e.g. `EventPicker` inside `QuoteForm`) never needed this fix regardless of their own namespace's function content, since client→client prop passing is normal React with no serialization boundary. The second, harder-to-spot version of the same class of bug: even after every entry-point component received `locale` instead of a dictionary object, the build still failed — because `i18n.ts` itself imports `getCompanySettings` → `prisma` → `better-sqlite3`'s native binding, and a **client** component importing `getDictionary` *from `@/lib/i18n`* (rather than from the pure `dictionary.ts`) was enough to make the bundler try to pull the entire server-only module graph (including native Node bindings needing `fs`) into the browser bundle, which fails outright. Fixed by ensuring every Client Component imports `getDictionary`/`Locale`/`Dictionary` from `@/lib/dictionary` specifically, never from `@/lib/i18n` — the module split isn't just organizational, it's load-bearing for keeping the server-only DB layer out of client bundles.

Given the sheer number of files (~90: every `page.tsx`/`layout.tsx` under `dashboard`/`events`/`clients`/`finance`/`time-tracker`/`settings`, plus every corresponding form/list/detail component), the bulk of the page-by-page string-swap sweep was done via three parallel forked agents split by section (Dashboard+Events+Clients / Finance / Time-tracker), each briefed to touch only its own file list and never edit the shared dictionary — new keys needed mid-sweep were either already present (the dictionary was built essentially complete beforehand, from an exhaustive string inventory of the real files) or left as a plain hardcoded English fallback rather than improvised, to avoid the exact same-file-conflict risk a shared dictionary write would have created between agents running concurrently. All genuinely cross-cutting/shared pieces (`layout.tsx` files, `Sidebar`/`TimerWidget`/`StatusPill`/`ConfirmDialogProvider`/`WeekCalendar`/`WeekNav`/`EventPicker`/`ClientPicker`/`FinanceTabs`/`TimeTrackerTabs`/login) plus the entire Settings section and the RSC-boundary bugfix sweep were done directly, not delegated, since those needed single-writer consistency.

Verified via clean `npm run build` (a real end-to-end check here, not a formality — it's the only thing that actually bundles for the browser and would have caught the Prisma-in-client-bundle bug) + `npm run lint`, plus an extensive live browser pass: toggled the accent colour to a custom blue and confirmed it repainted buttons/active-nav/focus-rings/hover-states everywhere including on `/login` (unauthenticated); switched the language to Czech and confirmed full, correct translation on the Dashboard, Events list, an event detail page (all four glass cards, tabs, delete button), the Invoices list (KPI tiles, bucket filters, status pills), Time tracker's Tracking and Overview pages, the Settings section itself, and the login page — including status-pill text, expense category names, and the in-app confirm-dialog default button labels, with the blue accent consistently applied throughout every one of those screens.

**Client ARES lookup now splits the address instead of dumping it all into "street" (done).** `ClientForm.tsx`'s "Load from ARES" previously set the entire one-line `textovaAdresa` string as the street field and left city/postCode/state blank — reported directly by the user. Fixed in `src/lib/ares.ts`: `lookupCompanyByIco()` now also parses ARES's own `adresaDorucovaci` ("delivery address") object, which is already split into exactly the three lines needed — `radekAdresy1` (street + house/orientation number, e.g. "Vojtěšská 211/6"), `radekAdresy2` (a part-of-town qualifier like "Karlín" or "Nové Město" — kept unused, there's no field for it in the app's 4-field structured address), and `radekAdresy3` ("PSC City", split by regex into `postCode`/`city`). Falls back to reconstructing from the raw `sidlo` fields (`nazevUlice`/`cisloDomovni`/`cisloOrientacni`/`nazevMestskehoObvodu`/`nazevObce`/`psc`) on the rare record missing `adresaDorucovaci`. `state` is hardcoded to `"Česká republika"` for every ARES result, since every ARES-registered company is Czech by definition. `AresCompany` gained `street`/`city`/`postCode`/`state` fields alongside the existing flat `address` string — the flat string is kept unchanged since `EventForm.tsx`/`CompanySettingsForm.tsx` both use it for their own single free-text address field (a different, deliberately-unstructured use case not part of this fix) and didn't need touching. Verified against the exact same two real companies used in earlier ARES testing (IČO `21342601` gen live production s.r.o. → "Vojtěšská 211/6" / "110 00" / "Praha 1"; IČO `23288183` hdhd studio s.r.o. → "Za Poříčskou bránou 355/17" / "186 00" / "Praha 8") plus a third, non-Prague company (Škoda Auto, `00177041`) fetched directly against the live ARES API first to confirm the `adresaDorucovaci` shape actually generalizes before writing any code — then re-verified end-to-end through the real "New client" form, including a full create → detail-page display round-trip for the second company.

**Made local uploads path env-configurable, prep for deploying the demo to Railway (done — code prep only, deployment itself is a manual step for the user).** `src/lib/uploads.ts`'s `UPLOAD_ROOT`/`LOGO_ROOT` were hardcoded to `path.join(process.cwd(), "uploads", ...)`. Added a `DATA_ROOT = process.env.UPLOAD_DIR ?? process.cwd()` fallback so both paths can be redirected onto a mounted persistent volume in a deployed environment without changing local-dev behavior at all (`UPLOAD_DIR` unset → identical to before). This was the one piece of the app that couldn't already be pointed at persistent storage via existing env vars — `DATABASE_URL` (Prisma) already fully controls where the SQLite file lives. Purely additive, no other files touched. Verified via clean `npm run build` + `npm run lint`.

Investigated deploying the demo (with sample data) to a persistent-disk host after confirming with the user that Vercel's serverless model (ephemeral filesystem, no local SQLite writes, no local file uploads) is a poor fit for this app's current architecture without a Postgres + object-storage migration — user chose Railway. Read `prisma.config.ts` (datasource URL already comes from `process.env.DATABASE_URL`, no code change needed there), `next.config.ts` (default, no `output: "standalone"`), `package.json` (`start: "next start"`, no `-p` flag — Next's CLI defaults to `process.env.PORT` when `-p` isn't passed, but the walkthrough given to the user still pins it explicitly with `-p $PORT` as cheap insurance), and `src/auth.ts` (NextAuth v5, JWT sessions, no `trustHost`/`AUTH_TRUST_HOST` set — required as an explicit env var on any host other than Vercel, since Vercel is the only platform NextAuth auto-trusts; omitting it behind Railway's reverse proxy throws `UntrustedHost` on every auth attempt). No `engines.node` in `package.json` — flagged to the user to pin a Node version via Railway's `NIXPACKS_NODE_VERSION` (this machine's local dev Node is v24) since Nixpacks' own default could otherwise land on an older version than this Next 16 / Prisma 7 stack expects.

**Railway deployment actually completed this session (done) — the demo is live.** The user set up the Railway project themselves (root directory `/web`, all 5 env vars, the `npx prisma migrate deploy && next start -p $PORT` start command) closely following the walkthrough above, then hit two real build/runtime bugs the walkthrough hadn't anticipated — both fixed and verified with a real Chrome session connected directly to the user's live Railway dashboard (a first for this project — Railway access, not just localhost). (1) **Build failed with `Can't resolve '@/generated/prisma/client'`** — `src/generated/prisma/` is gitignored and only existed locally because `prisma generate` had been run here before; a fresh Railway checkout never had it. Fixed with `"postinstall": "prisma generate"` in `package.json` — confirmed correct by actually deleting the local generated client and running a clean `npm install`-equivalent (`prisma generate`) + `npm run build` before pushing, rather than just reasoning about it. (2) **Build then failed differently**: `TypeError: Cannot open database because the directory does not exist` while prerendering `/_not-found`. Root cause: the root layout (`src/app/layout.tsx`) is `async` and calls `getCompanySettings()` (Prisma) on every render for the app-wide color/locale injection; `next build` tries to statically prerender the auto-generated `/_not-found` page by default, which runs that layout at *build* time — but a Railway Volume only mounts into the *runtime* container, not the build container, so `DATABASE_URL=file:/data/prod.db`'s directory genuinely doesn't exist yet during the build step. Fixed with `export const dynamic = "force-dynamic"` on the root layout — legitimate here, not a workaround, since every route in this app already depends on the session/DB per request and there was never anything to usefully prerender. **Reproduced the exact failure locally first** (`DATABASE_URL="file:/tmp/nonexistent-dir-xyz/prod.db" npm run build` failed identically) and confirmed the fix resolves it before pushing, then confirmed a normal build against the real local `dev.db` still works unchanged. (3) A third issue wasn't a code bug at all: after both fixes, the build succeeded and Railway showed the service "Online," but the live URL 502'd. Runtime logs showed Next actually listening on port 8080 (Railway's dynamically-injected `$PORT`), while the service's Networking → Target port setting was still the stale default of 3000 from before the custom start command was configured. Fixed directly in the Railway dashboard (Settings → Networking → edit the domain → Target port 3000 → 8080) — nothing to do with the repo. After that the site loaded correctly; ran `npm run seed:demo` via Railway's in-browser Console (a real SSH-backed shell into the running container) to populate the 7-event demo dataset against the live volume-backed DB, then confirmed a real login (`admin@eventsystem.cz` / `changeme123`) and the Dashboard rendering correctly end-to-end on the live Railway URL. Both code fixes verified via clean `npm run build` + `npm run lint` before pushing; the port-mismatch fix has no local-repo equivalent to verify, only the live redeploy.

**Finance tab bar now uses the glass-pill `SegmentedTabs` (done).** Reported directly: Finance's nav didn't match the rest of the app's "glass & gradient" redesign. `FinanceTabs.tsx` was still hand-rolled plain underline `Link`s (a pattern deliberately kept for `EventTabs`/the Settings tab bar during that redesign, since those were judged as general-purpose section navigation rather than a view-switcher) — but the user wants Finance's specifically to match the glass-pill look used by the Dashboard/Events/Time-tracker view switchers. `SegmentedTabs` already accepts `{value, href, label}` options backed by real `Link`s, so this was a straight swap, not a new capability. `EventTabs`/Settings' tab bar were deliberately left untouched — not part of this request. Verified live in the browser (both localhost and the change is pushed): pill renders with the sliding thumb, clicking Invoices/Expenses/Reports navigates and the thumb tracks correctly. Build + lint clean.

**Mobile design pass (done) — Claude Design canvas, 26 screens.** Before touching any app code, mocked up a mobile-phone version of every screen in a published Claude Design canvas ("Event System Mobile"), organized into 7 pages (Overview cover + Core/Events/Clients/Finance/Time tracker/Settings), built from the real app's tokens/`.card`/`.tag`/`.btn` classes and each screen's actual desktop structure rather than invented from scratch. Established the new patterns later implemented for real: a floating glass **bottom tab bar** (Dashboard/Clients/Events/Finance/Time tracker — 5 items, matching desktop's own nav order) replacing the sidebar below phone width, with an **Apple "Liquid Glass"-style capsule** wrapping the whole selected tab (icon + label together, not just the icon) — went through several rounds of user feedback on this one component alone (add the glass highlight → wrap label not just icon → round the shape into a true stadium/pill → tighten tab spacing → make the bar itself fully round → back to full-width tabs but padded so none get clipped by the rounded ends, with the highlight pill sized visibly smaller than the bar around it); an avatar-triggered **profile sheet** for Settings/sign-out (Settings didn't fit as a 6th bottom tab); dense desktop tables collapsing to stacked cards; the `1fr_280px` detail-page split collapsing to one column; and calendar/week-grid views replaced by a horizontal day-strip + vertical agenda list, since a 7-day grid can't survive 375px. Built via a small Python generator (shared chrome/icon/CSS helpers, not hand-duplicated per screen) rather than hand-authoring 26 near-identical `.dc.html` files. A review pass caught two real issues before final publish: a stray duplicate `class` attribute silently dropping a style, and — more seriously — every button/row/chip being under the 44px minimum tap-target size system-wide; both fixed at the shared-component level so the fix applied everywhere at once.

**Mobile-responsive implementation (done) — the real app, all ~35 pages.** Implemented the approved mockup for real. Planned as five phases (shell foundation, then Dashboard/Events/Clients, Finance, Time tracker, Settings) using Tailwind's default `md:` (768px) as the single cutover — below `md` gets the new mobile shell and reflowed screens, `md`+ is the untouched existing desktop layout. New: `src/components/mobile/MobileNav.tsx` (the bottom tab bar, exact glass-capsule recipe from the approved canvas) and `MobileTopBar.tsx` (wordmark + avatar opening the profile sheet, reusing the existing `TimerWidget` rather than rebuilding it), both `md:hidden`; `src/components/ui/MobileListRow.tsx` (the one shared stacked-card list-row component every list page's mobile variant renders through). `Sidebar.tsx` gained `hidden md:flex`. Every list page (Events/Clients/Invoices/Quotes/Expenses/Users/Roles/Reports' event+category tables) got a `hidden md:block` (desktop grid, byte-for-byte unchanged) / `md:hidden` (stacked cards, same already-fetched data) split — `MobileListRow` where the desktop row is a whole-row `Link`, a small non-link card built inline where it isn't (rows with separate per-row Edit/Delete actions, e.g. Expenses, Users, the client contacts list). Every `1fr_280px`/`1fr_300px` detail-page split became `grid-cols-1 md:grid-cols-[1fr_280px]` app-wide in one pass. `LineItemsFields.tsx` (shared by Quote/Invoice forms) got a genuine second mobile JSX branch — per-item cards instead of the shared 7-column grid — sharing the exact same `update`/`move`/`onChange` handlers as the desktop grid, not a rewrite. `WeekCalendar.tsx` (shared by Dashboard + Events calendar views) and `TimeTrackerCalendar.tsx` both gained a `md:hidden` day-strip-plus-single-day-agenda path alongside their unchanged desktop hourly/week grid. Finance's Reports page — never brought into the earlier desktop "glass & gradient" pass, still the old flat bordered look — was brought up to the current `.card`/`.tag`/`SegmentedTabs` system as part of this pass, not just given breakpoints. A general sweep afterward caught several fixed multi-column grids in create/edit forms that weren't on the original file list (`EventForm.tsx`'s status/value and date-pair rows, its venue/contact repeatable rows, `QuickCreateEventModal`'s date pair, `RunningTimerBox.tsx`'s idle-start and running-state grids, `ManualEntryForm.tsx`) and converted them the same way.

**Four real bugs caught and fixed during this pass, all only visible at runtime (build/lint stayed clean throughout each):** (1) Converting `WeekCalendar.tsx` to a client component (needed for the day-strip's `useState`) initially passed it a `t` dictionary object and an `eventHref` closure straight from its Server Component callers — the exact "Functions cannot be passed directly to Client Components" RSC-boundary bug already documented earlier in this file, reproduced fresh. Fixed with the same established pattern: the component takes a plain `locale: Locale` prop and calls `getDictionary(locale)` itself from `@/lib/dictionary` (never `@/lib/i18n`, which would pull Prisma into the client bundle); `eventHref` was just hardcoded to `` `/events/${id}` `` since both callers passed the identical function anyway. `TimeTrackerCalendar.tsx` applied the same fix from the start once the pattern was established. (2) Adding `MobileTopBar` as a new first child inside `<main>` collided with every page's own sticky toolbar, which already used a `-mx-6 -mt-5` negative margin to bleed flush against what used to be `<main>`'s own top edge — with the top bar now occupying that slot, every toolbar would have rendered pulled up underneath it. Fixed by changing the top-margin cancellation to `mt-0 md:-mt-5` on all 11 occurrences of the sticky-toolbar pattern app-wide, so it only fires at `md`+ where the mobile top bar doesn't exist. (3) The first pass at `LineItemsFields.tsx`'s mobile card variant gave its inputs the *same* `name="itemDescription"` etc. attributes as the desktop grid's inputs — since both variants stay mounted simultaneously (one just `display:none` via the responsive classes) and `FormData.getAll("itemDescription")` collects every matching input regardless of CSS visibility, submitting a quote or invoice would have silently doubled every line item. Caught by counting `name="itemDescription"` occurrences in a real rendered `/finance/quotes/new` page (should be 1 per item, was 2) rather than just trusting the build. Fixed by stripping `name` from the mobile card's inputs entirely — the desktop grid's inputs stay present-but-hidden with correctly synced values (React keeps `display:none` inputs' bound `value` current), so they alone carry the real submission. (4) A parallel background pass on Settings independently found `CreateUserForm.tsx` was never actually the single-column form the plan assumed — it's a real 5-column fixed grid (`grid-cols-[1fr_1fr_150px_100px_auto]`) that would have broken badly at 375px; fixed the same way as everywhere else (`grid-cols-1 md:grid-cols-[...]`).

**No browser tool was available this session** (the Claude-in-Chrome extension reported disconnected on every attempt) — verified instead via clean `npm run build` + `npm run lint` after every file, plus extensive HTTP smoke tests against a real authenticated session across every route this pass touched (all ~30 list/detail/form/tab routes, including query-param view variants like `?view=calendar`), confirming 200s with zero server errors in the dev log, and the `LineItemsFields` duplicate-submission fix specifically confirmed by counting rendered `name=` occurrences in real HTML. **Visual/interactive verification (does the glass capsule actually look right on a real phone, does the day-strip tap-to-select actually work, etc.) is still outstanding** — do that in an actual browser before trusting this pass's visual polish, same caveat this file has given after every phase built without browser access.

**Two real iOS-shell bugs reported after actually running the app in the Simulator, both fixed (done — still needs a real-device/Simulator re-check, no browser tool available this session either).** User ran the Capacitor app for the first time and reported (a) a "quite weird" bottom tab bar and (b) the whole screen not fitting the iPhone's width — has to scroll left/right. Investigated by reading code only (no browser attach possible to a WKWebView; Chrome extension also reported disconnected). Audited every `grid-cols-[...]`/fixed-width element in the codebase against the mobile-responsive pass documented above — all of them were already correctly gated behind `hidden md:block`/`md:hidden` with their own `overflow-x-auto` wrapper where genuinely wide (line items, report charts, calendars), so no single overflowing element could be pinned down statically. Found two real, independent bugs instead: (1) `ios-app/capacitor.config.ts` had `ios.contentInset: "always"` — this makes WKWebView's own native scrollView *also* auto-apply safe-area insets on top of the ones this app already adds itself via CSS (`env(safe-area-inset-*)` in `MobileNav`/`MobileTopBar`, built specifically for this purpose in the mobile-responsive pass) — double-applying insets is exactly the kind of thing that pushes a `position: fixed` bottom bar to the wrong place. Changed to `contentInset: "never"` so only the app's own CSS insets apply (also updated the synced copy at `ios-app/ios/App/App/capacitor.config.json`, which `npx cap sync` would otherwise silently leave stale). (2) `globals.css` had no `overflow-x: hidden` anywhere on `html`/`body` — a normal browser tab tolerates a stray pixel of horizontal overflow invisibly, but WKWebView rubber-bands/pans the *whole page* on even 1px of it, which is what reads to a user as "I have to scroll left/right" even when no single element is meaningfully wider than the viewport. Added `overflow-x: hidden` to both `html` and `body` as a backstop — doesn't affect any of the app's intentional internal `overflow-x-auto` scrollers, which clip independently of the page. **Requires a native rebuild to take effect** — a capacitor.config.ts change only reaches the app after `npx cap sync ios` (regenerates `capacitor.config.json` inside the Xcode project) followed by a fresh build/run from Xcode; reloading the Simulator alone won't pick it up since the config is baked into the native binary, not fetched from the live Railway URL. Verified via clean `npm run build` + `npm run lint` only — **not yet re-verified in the Simulator**, since no browser/device tool was available this session; user should rebuild and re-check both the bottom-bar position and whether the horizontal scroll is gone.

**Chart readability pass — real app implementation of the "enhanced" design mockups (done).** Followed up the design-canvas mockup work (see the design-tool session notes) by implementing the same three charts for real: the Dashboard Balance chart, Finance Reports' by-month chart + "Top categories" list, and a brand-new grouped bar chart on Time Tracker → Overview (which had no chart at all before — just a plain number grid). New shared `src/lib/chart-colors.ts` (`CATEGORICAL_CHART_COLORS`, `categoricalColor(index)`, `INCOME_CHART_COLOR`) — a colorblind-validated 8-hue palette (dataviz skill's method, re-run against the app's actual dark card surface `#1a1918`), assigned by rank/order rather than cycled, so "Income" is a distinct blue instead of dim gray and each expense category in Reports gets its own hue instead of every bar being the same accent-orange (the literal bug the user was pointing at — category color carried zero information before this). New shared `src/components/ui/ChartAxisGrid.tsx` (gridlines + axis labels) reused by all three chart locations. New `formatCompactCurrency`/`niceAxisMax` in `format.ts` for readable axis tick labels (e.g. "400 tis. Kč" instead of a raw six-digit number). Dashboard's `buildMonthlyBalance()` query widened from `.slice(-4)` to `.slice(-6)` months to match the approved mockup. Time Tracker Overview's new chart also added color-dot swatches to the existing per-person table rows, so the chart and the table reinforce the same color→person mapping. Verified via clean `npm run build` + `npm run lint`, plus HTTP smoke tests against reseeded demo data in both English and Czech confirming correct bar heights/axis values/per-series colors — dev DB reseeded back to its standing empty state afterward.

**Three-item feedback batch: invoice emailing (new feature), semibold buttons everywhere, branding file-input cleanup (done).** (1) **Invoice emailing** — the two invoice "Send"/reminder buttons had been inert placeholders since Phase 2 (`ti.emailNotWired`/`ti.notWiredYet`, explicitly flagged in this file's "Deferred to later phases" list). Asked the user how to handle actual delivery given the app had zero email infrastructure (no SMTP/API credentials anywhere) — user chose **SMTP via nodemailer** over a transactional-email API or a `mailto:`-only fallback. Built: `Invoice.sentAt`/`lastReminderAt` (new nullable `DateTime` columns) and two new `InvoiceEventType` values (`EMAILED`, `REMINDER_SENT`) for the History panel; `CompanySettings` gained four editable template fields (`invoiceEmailSubject`/`Body`, `reminderEmailSubject`/`Body`, each with a sensible `{{token}}`-based default) surfaced on a new **Settings → Invoice emailing** tab (`InvoiceEmailingForm.tsx`) — same `useActionState` pattern as `CompanySettingsForm`/`AppSettingsForm`, same "upsert with empty-string placeholders for the required-but-unrelated company fields" trick as `updateAppSettingsAction` for the case where no `CompanySettings` row exists yet. `src/lib/email-templates.ts` (pure, no nodemailer import — `EMAIL_TEMPLATE_TOKENS`, `renderEmailTemplate()`, the default template strings) is deliberately split from `src/lib/email.ts` (the actual nodemailer transport + `sendEmail()`) for the same reason `dictionary.ts`/`i18n.ts` are split elsewhere in this file — a Client Component (`InvoiceEmailingForm`) needs the tokens/renderer but must never pull `nodemailer` (which needs Node's `net`/`tls`) into the browser bundle; this bit on the first build attempt exactly like the documented Prisma-in-client-bundle failure mode, fixed the same way. `resolveInvoiceRecipient()` in `actions/finance.ts` prefers the linked `Client.invoicingEmail`, falling back to the event's first `EventContact` with an email, erroring clearly if neither exists. The invoice detail page's header gained a real "Send invoice" button (was the disabled span) and the Payment State card gained "Invoice sent"/"Reminder" status rows plus a conditional "Send reminder" button (only when actually overdue) — both wired through a new shared `SendInvoiceEmailButton.tsx` client component (`useActionState`, inline error/success text, no confirm dialog since sending isn't destructive). PDF generation was extracted from the `/api/invoices/[id]/pdf` route into `src/lib/pdf/build-invoice-pdf.tsx` so the email action and the download route share one implementation instead of duplicating the QR/logo/render logic. **Verified end-to-end over real HTTP** (reconstructing the React Server Action's hidden `$ACTION_*` form fields from rendered HTML and POSTing them directly — the same technique used earlier in this file for the confirm-dialog fix era, since there's no browser tool this session either): confirmed the full pipeline (auth → recipient resolution → template rendering → PDF build → send attempt) runs correctly and fails gracefully with the exact `EmailNotConfiguredError` message when `SMTP_HOST`/`SMTP_USER`/`SMTP_PASS` aren't set (they aren't, since the user hasn't supplied real credentials yet — that's the one remaining step on their end, documented as commented-out placeholders in `.env`), confirmed no `sentAt`/`InvoiceEvent` row gets written on a failed send, confirmed the "Send reminder" button is present only on a genuinely overdue invoice and absent otherwise, and confirmed the Settings form itself persists real edits to the DB. (2) **All buttons semibold** — added `font-weight: 600` directly to the shared `.btn`/`.btno` base classes in `globals.css` rather than the ad-hoc per-button `font-semibold` Tailwind class already sprinkled on a few "New X" buttons from an earlier pass; this covers every pill-shaped button app-wide in one place (the existing per-button `font-semibold` instances are now redundant but harmless — same computed value). Deliberately scoped to the two button classes, not every `<button>` tag in the codebase — small inline text-link actions (table-row "Edit"/"Delete", icon toggles) read as links, not buttons, in this app's visual language and were left alone. (3) **Branding "Choose file" cleanup** — Settings → Company's logo upload used a bare `<input type="file">`, so browsers appended their own "No file chosen" text next to the button, which can't be hidden with CSS (it's inside the input's UA shadow DOM, not a separate styleable node). Fixed with the standard workaround: the native input is now `sr-only` (visually hidden but still focusable/functional) paired with a `<label htmlFor="logo-upload" className="btno">` as the visible trigger (clicking a `<label>` associated with a file input opens the picker natively, no JS needed) — the chosen filename, when there is one, is now shown via the component's own state (`logoFileName`, set in the existing `onChange` handler) rather than the browser's default text, so nothing renders at all until a file is actually picked, matching the literal ask. Scoped to this one file input only, per the user's explicit "in settings → branding" — the separate expense-receipt upload (`ReceiptInput.tsx`) has the same browser-default behavior but wasn't part of this request and was left untouched. All three items verified via clean `npm run build` + `npm run lint`; items 2–3 additionally confirmed by inspecting real rendered HTML from an authenticated session (no "No file chosen" text present; `.btno`'s `font-weight:600` confirmed by reading the compiled `globals.css` rule directly, not just trusting the source edit).

**Automatic Google Drive export for paid invoices, accepted quotes, and expense receipts (done — needs the user's own Google Cloud setup to actually upload, exactly like SMTP).** User wants a prepared Drive folder tree (`Finance/Quotes`, `Finance/Invoices issued`, `Finance/Invoices received`, each with `YEAR` subfolders) kept in sync automatically: a paid invoice's PDF into Invoices issued, an accepted quote's PDF into Quotes, and an expense's uploaded receipt into Invoices received — "PAID button" standing in for a future real bank-API trigger, per the user's own framing. Asked the same kind of transport question as the email-SMTP decision (the app had zero Google API setup — no OAuth, no service account) and the user again picked the simpler, credential-based option over an interactive OAuth consent flow: a **service account** (JSON key as an env var, sharing the Drive root folder with its email) plus a **pasted root folder ID** (no folder-name search) — both documented as setup steps directly in `.env`. New `src/lib/gdrive.ts` (`googleapis`'s Drive v3 client via `google.auth.JWT`, `DriveNotConfiguredError`, `uploadFinanceDocument()` which finds-or-creates the category/year folder chain and overwrites-by-exact-filename rather than accumulating duplicates on a re-trigger, and a `tryUploadFinanceDocument()` best-effort wrapper that never throws) reads `GOOGLE_SERVICE_ACCOUNT_JSON_BASE64` (the downloaded key, base64'd — sidesteps `.env`'s handling of the PEM private key's embedded newlines) and `GDRIVE_FINANCE_ROOT_FOLDER_ID`.

Filenames: `<document number>-invoice.pdf` / `<document number>-quote.pdf` using the quote/invoice's own `number` field (e.g. `26-003_v2-invoice.pdf`) rather than the bare event number the user's message literally showed — a deliberate deviation, since the event-number-only form would collide/overwrite when an event has more than one invoice (the existing `_v2`/`_v3` versioning scheme this app already has specifically to handle that case). Expense receipts: `<event number>-<original filename>` for event-linked expenses; for company-overhead expenses (no event, so no `YY-XXX` to name the file after — a real gap in the user's spec, asked about directly) a `OVERHEAD_<expense date>-<original filename>` name instead, per the user's explicit choice, so nothing is silently skipped. The year folder is keyed off "when it happened," read literally: the linked **event's `startDate`** for quotes/invoices, the **expense's own `date`** field for expenses (including overhead ones, which have no event to read a date from otherwise) — not the event-number's creation year or the invoice's `issuedAt`, which can legitimately differ.

Every trigger point is **best-effort and non-blocking** — `markInvoicePaidAction`, `updateQuoteStatusAction` (only on the SENT→ACCEPTED transition), and both expense actions (only when a receipt file is actually part of the submission) all call `tryUploadFinanceDocument` (or the plain `uploadFinanceDocument` wrapped the same way) *after* their real DB write succeeds, and a Drive failure never fails or rolls back the primary action — mirroring the invoice-emailing precedent from the previous batch. Only invoices get any visible failure feedback: a new `InvoiceEventType.DRIVE_EXPORT_FAILED` history entry, and only for a **real** failure — the ubiquitous "not configured yet" case (true for everyone until they set up the service account) is deliberately excluded from logging, otherwise every single "mark as paid" click before that setup would spam the History panel with an expected, not-yet-relevant message. Quotes have no history log at all (out of scope to add one just for this) and expenses have no per-row status UI, so those two just fail silently server-side (still `console.error`'d) — acceptable given this is a nice-to-have sync, not the record of truth (the app's own DB/PDF-on-demand always still work regardless of Drive's state).

Reused the invoice-PDF-extraction pattern from the previous batch for quotes too: `src/lib/pdf/build-quote-pdf.tsx` now holds the render logic shared by `/api/quotes/[id]/pdf` and the new Drive-export path, exactly like `build-invoice-pdf.tsx` already did for invoices — no duplicated QR/logo/render code for either document type now. `InvoiceEventType` gained the new value at the SQLite/Prisma level with no migration file needed (confirmed: SQLite has no native enum type, Prisma represents one as a plain `TEXT` column with an app-level check, so a new enum *value* — unlike a new column — never requires a migration; `prisma generate` alone was enough to teach the TypeScript types about it).

Verified via clean `npm run build` + `npm run lint`, plus HTTP smoke tests against reseeded demo data: marked a real invoice paid (status → PAID, correct `MARKED_PAID` history entry, no `DRIVE_EXPORT_FAILED` spam since Drive isn't configured), accepted a real SENT quote (status → ACCEPTED), and created + later edited a real expense with an actual uploaded receipt file (both times correctly saved to disk under a fresh random filename, both actions completing normally) — confirming the three trigger points never block or fail their real action just because Google Drive isn't set up yet. One real testing hiccup along the way, not a code bug: the first two attempts to POST the expense-creation Server Action from curl accidentally reconstructed the *sign-out* button's hidden `$ACTION_ID_...` field instead of the actual form's (both sit in the same rendered page, and a naive regex grabbed whichever occurred first) — this silently logged the session out and 303'd to `/login` on every attempt, which looked exactly like a real bug until the full form markup was read carefully and the correct `$ACTION_REF_2`/`$ACTION_2:0`/`$ACTION_2:1`/`$ACTION_KEY` set was used instead.

**Real regression fix: the pinned sidebar had silently stopped staying in place on scroll (done).** User reported the sidebar wasn't staying put while scrolling a tall page. Root cause traced to the iOS-shell fix from an earlier session in this same file: `html { overflow-x: hidden }` / `body { overflow-x: hidden }` (added purely to stop WKWebView's horizontal rubber-banding). Per the CSS Overflow spec, setting `overflow-x` to anything other than `visible` while leaving `overflow-y` unset silently promotes the computed `overflow-y` to `auto` too — so that fix had the unlisted side effect of turning *both* `html` and `body` into their own scroll containers. `Sidebar.tsx`'s `position: sticky` sticks to the nearest scroll-container ancestor, and with two new candidates now sitting between it and the true viewport, it was no longer reliably the viewport itself — which is what broke the "stays put while the page scrolls" behavior the sidebar has relied on since the original pin/unpin → floating-sidebar rework. Fixed by switching both selectors from `overflow-x: hidden` to `overflow-x: clip` — `clip` visually clips the same way `hidden` does (still stops the WKWebView bounce) but, per spec, never establishes a scroll container in the first place, so it doesn't trigger the `overflow-y: auto` promotion; this is the documented, standard fix for exactly this "clip one axis, leave the other alone" case. Verified via clean `npm run build` + `npm run lint` and confirmed the compiled CSS output actually contains `overflow-x:clip` (not `hidden`) on both selectors — **not independently re-verified by scrolling a real page in a browser**, since no browser tool was available this session either; the fix is a well-established, spec-documented one, but do a real visual scroll-check before fully trusting it.

**Invoice/quote PDF redesign implemented for real, embedded ISDOC e-invoicing, 8-hour session expiry with email-remembering login (done).** Three items from one request, following up the "Invoice Quote Mockup" design-canvas session referenced earlier in this file.

*PDF redesign, full mockup fidelity (user's explicit choice, offered a "structural-only" alternative first)* — `InvoicePdf.tsx`/`QuotePdf.tsx`/`shared.tsx` rebuilt to match the approved mockup exactly, not just incrementally: a big lowercase wordmark ("invoice"/"faktura", "quote"/"nabídka") replaces the old small "Invoice"/docNumber corner label; the header's 2px rule is gone; issued/due (or issued/valid-until) dates stack in a big 12pt row where a single date used to sit; addresses render as up to 3 lines via a new `addressLines()` best-effort comma-split helper (`shared.tsx`) — this app has no structured address for `CompanySettings` or an Event's own embedded `companyAddress`/only `Client` does — with a `clientAddressLines()` counterpart used instead whenever the invoice/quote's event has a linked `Client` (both `getInvoiceDetail` and, newly, `getQuoteDetail` now `include: { client: true }` on the event relation) — real structured data when it's available, a reasonable heuristic when it isn't, documented in code as exactly that rather than pretending it's real parsing. The invoice header row is now 4 columns (Supplier / Customer / Payment details / a dedicated QR column beside it) instead of the old 2-column-plus-footer-QR layout; `CompanySettings` gained `accountNumber` and `swiftBic` (both new, previously only `bankAccount`/IBAN existed) surfaced in Settings → Company's Bank section and on the PDF's Payment details block alongside IBAN and the variable symbol. The quote's header row is 3 columns (Supplier / Customer / Created by), with `createdBy`'s name genuinely larger (13pt vs the 11pt party names) since it no longer shares its column with the dates block. Every new label (`paymentDetails`/`accountNumber`/`swift`/`qrPlatba`/`scanToPay`/`invoiceThanks`/`createdBy`) added to both `en`/`cs` in `pdf/i18n.ts` — the existing PDF-language chooser (independent of the app's own UI-locale setting) needed no other changes.

*ISDOC 6.0.2 embedded directly into the invoice PDF, one file* — user wants the invoice PDF to double as a machine-readable e-invoice accounting software (Pohoda, Money S3, ABRA, iDoklad, …) can import, without a second download. Rather than trust training-data memory of a government e-invoicing XML schema, the actual current schema was pulled directly (`curl`, not the lossy AI-summarized `WebFetch` path, which truncated badly on a 140KB/2779-line XSD — see the schema hunt earlier in this session) from `isdoc.cz/6.0.2/xsd/isdoc-invoice-6.0.2.xsd` and read element-by-element before writing anything. New `src/lib/isdoc.ts` (`buildIsdocXml()`) builds a schema-faithful `DocumentType=1` (regular tax invoice) document: `AccountingSupplierParty`/`AccountingCustomerParty` (`PartyIdentification`/IČO, `PartyName`, `PostalAddress` — `StreetName`/`BuildingNumber`(blank, this app doesn't split it out)/`CityName`/`PostalZone`/`Country`(hardcoded `CZ`/"Česká republika" — this app is Czech-market-only), `PartyTaxScheme` omitted when DIČ is blank since it's `minOccurs="0"`), `InvoiceLines`, `TaxTotal`/`TaxSubTotal` per distinct VAT rate, `LegalMonetaryTotal`, and `PaymentMeans` (bank-transfer code `42`, with the local account number split into ISDOC's separate `ID`/`BankCode` pair by the `/` in "123456789/0300"). The schema's many required "AlreadyClaimed\*"/"Difference\*" fields (there because `LegalMonetaryTotal`/`TaxSubTotal` double as deposit-invoice reconciliation bookkeeping) are simply written as `0`/the full amount since this app doesn't do advance/deposit invoicing — schema-correct, not a placeholder. **Discount reconciliation**: `Invoice.total` is the post-discount amount but line items in the DB are pre-discount, and ISDOC has no discount/allowance concept at all (confirmed by grepping the XSD) — proportionally scaling every line by `input.total ÷ (base+vat)` reproduces the app's own `applyDiscount()` blended-average-VAT-rate total *exactly* (proven algebraically: the same weighting falls out either way) while still preserving each line's own real VAT rate for a correct per-rate `TaxSubTotal`, with any residual rounding cents absorbed into the optional `PayableRoundingAmount` field — which exists in the schema for exactly this purpose. `PDFDocument.attach()` from the newly-added `pdf-lib` dependency (zero native deps, `npm install` alone was enough) embeds the generated XML as a named `<number>.isdoc` attachment inside the same PDF bytes `buildInvoicePdfBuffer()` already produces — one download, human-readable PDF and machine-readable ISDOC together, the same pattern hybrid formats like Factur-X/ZUGFeRD use. Deliberately best-effort: `embedIsdoc()` never throws — a malformed build falls back to the plain PDF rather than blocking the document a client actually needs. Quotes don't get one (ISDOC is specifically an invoice format).

*8-hour automatic sign-out with email-remembering login* — `auth.ts`'s NextAuth config gained `session: { strategy: "jwt", maxAge: 8 * 60 * 60 }`; since the default `updateAge` (24h) is longer than that, the session is never silently refreshed mid-use — it's a fixed 8h from login, not a sliding/inactivity timer, and `requireUser()`'s existing per-request DB check (already there for the deactivation feature) is what actually bounces an expired session to `/login` on its next request, no new expiry-detection code needed. Separately, `loginAction` now sets a `lastEmail` cookie (1-year expiry, `httpOnly`) on a successful login, and `/login` reads it server-side to prefill the email field — so after the 8-hour expiry (or any other sign-out) the user isn't stuck retyping their email, just their password. Had to restructure `loginAction` to call `signIn("credentials", { redirect: false })` instead of passing `redirectTo` directly, since a successful `redirectTo` sign-in throws a `NEXT_REDIRECT` before any code after it can run — with `redirect: false`, control returns normally on success, letting the cookie get set before an explicit `redirect("/dashboard")`. **Real gotcha hit while wiring this up**: a `"use server"` file may only export async functions — a plain `export const LAST_EMAIL_COOKIE = "lastEmail"` inside `actions/auth.ts` silently broke the *entire* module's exports at build time ("Export loginAction doesn't exist in target module", "The module has no exports at all") rather than failing on that one export; fixed by moving the constant to its own tiny `src/lib/auth-cookies.ts`.

Verified via clean `npm run build` + `npm run lint`, plus extensive HTTP smoke tests against reseeded demo data (dev DB reseeded back to empty afterward, per standing convention) — since no browser tool was available this session either: logged in over real HTTP (reconstructing the login form's hidden `$ACTION_*` fields, the same technique used earlier in this file), confirmed the session cookie's `Expires` header is exactly 8h after login, confirmed the `lastEmail` cookie prefills the login page's email field on a simulated post-expiry visit; downloaded a real discounted invoice (`26-003_v2`, 10% off) and a real quote (`26-006`) and rendered both to PNG via PyMuPDF for an actual visual check (not just text extraction) confirming the new layout matches the mockup in both English and Czech, diacritics intact; extracted the embedded `.isdoc` attachment from the downloaded PDF with PyMuPDF and validated it against the real downloaded XSD using `lxml`'s `XMLSchema` validator — genuinely schema-valid, not just "looks right" — for both the discounted invoice (confirmed `TaxExclusiveAmount`/`TaxInclusiveAmount`/`PayableAmount` reconcile exactly to the discounted `52272 Kč`, zero rounding drift) and a plain non-discounted one; confirmed the Settings → Company form round-trips the two new bank fields through a real POST and back into the DB, then restored the seeded values afterward.

**Real bug fixed: the generated invoice/quote PDF was rendering light-background/dark-text, not the dark theme the mockup and redesign were built around (done).** User caught this from a side-by-side screenshot (mockup on the left, the actual downloaded PDF opened in macOS Preview on the right) — the PDF redesign in the previous batch changed structure/layout to match the mockup but never actually touched the *colors*, so `shared.tsx`'s `sharedStyles` was still the original light-page palette (no `backgroundColor` at all, `INK` used as a dark-text-on-white color, borders/labels in mid-grays meant for a white page) underneath the new dark-mockup-shaped layout. Fixed by rewriting the color tokens to the exact values the mockup uses: `BG` `#131211`, `INK` (now the *text* color) `#f3f2f2`, plus `MUTED`/`LINE`/`RULE`/`ROW_BORDER` for label/body/rule/border colors — applied to `page.backgroundColor` and every hardcoded gray in `sharedStyles`. Verified by re-downloading a real invoice PDF and rendering it to PNG (PyMuPDF, no browser tool needed for this) — genuinely dark now, matching the mockup.

**Also matched the in-app "PDF preview" card (on the invoice/quote detail pages) to the same redesign — user asked for this explicitly, since it had never been touched at all and still showed the old flat 2-column layout** (single-line address, no big wordmark, no stacked dates, no payment/QR columns, no visible creator block for quotes). Rebuilt both cards' markup to mirror the PDF structure precisely: giant lowercase wordmark, stacked Issued/Due (or Issued/Valid-to) lines, structured multi-line addresses (reusing the exact same `addressLines()`/`clientAddressLines()` helpers from `pdf/shared.tsx` — genuinely shared logic, not a re-implementation), a 4-column Supplier/Customer/Payment-details/QR row for invoices (3-column Supplier/Customer/Created-by for quotes, creator name bumped to 13px matching the PDF), and the "To pay"/"Total" figure in the company's accent color. The on-screen QR code needed the exact same "QR Platba" SPD-string-to-PNG generation the PDF route already does — factored that out of `build-invoice-pdf.tsx` into an exported `getInvoiceQrDataUrl()` (plus a `FALLBACK_SUPPLIER` export) so the invoice detail page calls the identical function rather than duplicating the QR logic. Deliberately kept the existing app-wide `.card` class (dark `--color-surface`-based gradient panel) as the container rather than hardcoding the mockup's literal `#131211` — the app's own theme is already dark end-to-end, so `.card` already reads as "the same dark card everything else on the page uses," which is the more correct interpretation of "look like the app" than introducing a one-off background shade that would look inconsistent next to the sidebar cards beside it.

**Real bug caught and fixed during the same live-browser check**: the invoice preview's Payment-details column initially cut the IBAN off mid-string (`CZ650300000000123456` then a hard clip) instead of wrapping it — the unspaced 25-character IBAN was overflowing its grid column rather than breaking onto a second line. Fixed with `break-words`/`break-all` on that column/line; re-verified live in the browser (Chrome extension connected this session) that it now wraps cleanly onto two lines within its column, and re-checked the quote preview too (Supplier/Customer/Created-by columns, discount-free total in accent color) — both now visually match the mockup and the downloaded PDF side by side. Verified via clean `npm run build` + `npm run lint` plus this live browser pass; dev DB reseeded back to empty afterward.

**Real bug found and fixed: nothing in the generated PDF ever actually rendered bold, despite every "should be bold" element (wordmark, party names, doc number, totals, creator name) correctly having `fontWeight: 700` set (done).** User caught this as a further gap after the dark-theme fix above — visually every PDF text looked the same weight. Root cause, confirmed empirically rather than assumed: `registerAppFont()` registered `NeueRegrade-Variable.ttf` — a genuine variable font (`fontkit` reports a real `wght` axis, min 300/default 300/max 800) — under *both* `fontWeight: 400` and `fontWeight: 700` as the exact same file. `@react-pdf/font`'s fontkit-based pipeline doesn't interpolate a variable font's weight axis per the nominal `fontWeight` key it's registered under — it just loads the file at its own default instance (this file's default, per its `variationAxes`, is 300 — coincidentally close to "regular"-looking, which is exactly why the *lack* of a visual difference was easy to miss rather than looking broken). Confirmed the ceiling of trying to fix this at request-time: `font.getVariation({wght:700}).createSubset()` (fontkit's own documented API for flattening a variation instance) throws outright inside fontkit's own TrueType encoder — a real bug/rough edge in that library's variable-font subsetting support, not something to build a workaround around.

Fixed at the source instead: used `fonttools varLib.instancer` (Python, already used earlier in this project to inspect this exact font's cmap) to flatten the variable font into two genuine static instances — `wght=400` → `NeueRegrade-Regular.ttf`, `wght=700` → `NeueRegrade-Bold.ttf` — committed to `src/fonts/neue-regrade/` alongside the original variable file (which stays untouched and in use — `layout.tsx`'s `next/font/local` for the live web app is unaffected, since browsers *do* correctly interpolate variable-font weight axes; this was purely a react-pdf/fontkit-side limitation). `registerAppFont()` now registers these two distinct files instead of the same one twice. Confirmed the two static instances are visually distinct *before* wiring them in (a quick Pillow render, plain vs. clearly bold strokes) and confirmed full Czech diacritic coverage survived the instancing (`fontTools.getBestCmap()`, same check used when Neue Regrade was first adopted) — flattening a variation axis doesn't touch the glyph/cmap set, but this is exactly the kind of assumption worth actually checking rather than trusting.

Verified via clean `npm run build` + `npm run lint`, plus a fresh HTTP round-trip re-downloading both a real invoice and a real quote PDF (rendered back-to-back in the same server process, deliberately, to re-confirm the earlier digit-corruption-across-renders bug is still fixed) and rendering both to PNG (PyMuPDF) — wordmark, party names, doc number, and totals are now genuinely bold against genuinely-regular body text, matching the mockup exactly; `pdfminer.six` text extraction on both confirmed zero digit/diacritic corruption. Dev DB reseeded back to empty afterward.

**Real bug found and fixed: the previous two PDF-redesign passes matched the mockup's structure but never actually matched its type scale or spacing — the layout/sizes were approximated, not measured (done).** User's third round of feedback on this ("PLEASE DO IT AS WE PREPARED IN MOCKUP") made clear the earlier passes weren't rigorous enough — every font-size/margin/gap in `InvoicePdf.tsx`/`QuotePdf.tsx`/`shared.tsx` had been picked by eye to "look about right" rather than read exactly off the mockup and converted correctly. Fixed by doing the conversion properly this time: the mockup's canvas is 794×1123 CSS px — exactly A4 at 96 DPI (794 = 8.27in × 96) — and the PDF page is A4 in points (595.28pt = 8.27in × 72), so **every mockup pixel value converts to the PDF via the literal 96→72 DPI ratio, pt = px × 0.75** — not an approximation, an exact unit conversion between two representations of the identical physical page. Re-extracted the mockup's actual current `.dc.html` source (confirmed byte-identical to the last saved working copy — nothing had changed in the GUI since), read every `font-size`/`margin`/`gap`/`padding`/`border-width` value directly out of it, and rewrote `shared.tsx`'s entire `sharedStyles` sheet as that literal ×0.75 conversion of each one (e.g. the wordmark: mockup 154px → 115.5pt, previously guessed at 56pt; the "TO PAY" total: mockup 22px → 16.5pt, previously guessed at 14pt) — every value in the sheet now has a traceable source number, not a vibe. Caught along the way: the invoice's parties row and quote's parties row use different gaps in the mockup (18pt vs 24pt) that the previous single shared style couldn't express — split into a document-specific inline `gap` override; the quote's "Created by" column is 38% width in the mockup, not the 29% it was sharing with Supplier/Customer; and the item table had an extra invented `marginTop` that doesn't exist in the mockup at all (the visual gap is entirely partiesRow's own `marginBottom`).

Also rebuilt the in-app "PDF preview" cards (invoice/quote detail pages) the same rigorous way, but *without* the 0.75 conversion — that card is HTML in a browser, not an A4-point PDF page, so the mockup's raw pixel values apply directly, unconverted. Initially made a real arithmetic mistake here mid-fix: halved every font-size for a narrower card but left the reference width unchanged, which silently changes every proportion relative to the mockup (a wordmark at half the mockup's font-size in a container that isn't half the mockup's width is not the same shape). Caught it before shipping and fixed properly: the preview is now `w-[794px]` — the mockup's own native page width, literally — with every value at its literal mockup pixel size, wrapped in `overflow-x-auto` so it scrolls within its own card on narrower viewports exactly like the item table beneath it already does, rather than being scaled down and drifting from the real proportions.

Verified live in the browser (Chrome extension connected this session): opened both a real discounted invoice and a real quote, screenshotted the giant wordmark/stacked-dates/payment-details layout, and scrolled the preview card horizontally to confirm the full 794px-wide reproduction (Payment details + QR Platba column for the invoice, Created-by column for the quote) renders and scrolls correctly with nothing clipped or broken. Also re-downloaded and re-rendered both PDFs (PyMuPDF) to confirm the same fix landed there — wordmark, dates, and totals now visually match the mockup's actual proportions, not just its structure. Clean `npm run build` + `npm run lint` throughout; dev DB reseeded back to empty afterward.

**In-app PDF preview: zoom-to-fit instead of horizontal scroll, plus three real bugs caught during that fix (done).** User watched the pixel-precision fix above happen live and asked for one more thing: the in-app preview cards (built at the mockup's literal 794px width) shouldn't make the user scroll sideways to see the whole document. New `src/components/finance/DocumentPreviewScaler.tsx` — a small client component that measures its container via `ResizeObserver` and applies `transform: scale(containerWidth / 794)` (capped at 1, never scaling up past natural size) to the fixed-width content, with the wrapper's own height set explicitly from the *scaled* content height so it doesn't leave dead space below — the standard technique thumbnail/print-preview widgets use, since `transform` doesn't shrink an element's own layout box. Wraps both preview cards; the old `overflow-x-auto`/scroll approach is gone.

Three real, independent bugs surfaced getting this actually working, none guessable from reading the code — all found by testing live in the browser and checking actual DOM state, not by inspection:

1. **A stale dev server from an earlier turn silently absorbed every "kill and restart" for the rest of that session.** `pkill -f "next dev"` failed to actually kill a background process from several turns earlier; every subsequent `npm run dev` then hit `EADDRINUSE`, silently fell back to port 3001, and left the *old* process still serving port 3000 — which is what the browser was actually testing against the whole time, showing stale code no matter how many times the "current" file was edited or the "current" server restarted. Only caught by reading the dev server's own log output line-by-line and noticing the "Another next dev server is already running… PID 45797" message; fixed with `lsof -ti:3000 | xargs kill -9` before restarting, not a plain `pkill`. Worth remembering: after any dev-server restart in this project, verify with `lsof -i:3000` (one process) before trusting what the browser shows, rather than assuming a restart succeeded just because the command didn't error.
2. **The scaler's own `width: 100%` didn't stop the card from overflowing into the sidebar column** — a classic CSS Grid gotcha: a bare `1fr` track (this page's `grid-cols-[1fr_280px]`) still sizes its minimum width off the *content's* min-content contribution unless the grid item explicitly opts out with `min-width: 0`. The scaled-down 794px content's true (unscaled) layout box was still contributing its full min-content width up through the tree, expanding the whole `1fr` track regardless of the scaler's own percentage width. Fixed with `min-w-0` added directly to the `.card` grid item (not the shared `.card` class itself, which is used everywhere else without this problem).
3. **User caught two more real defects live**: the quote's date row was overlapping the "quote"/"nabídka" wordmark — the invoice's "invoice" wordmark has no descenders (no glyph in it dips below the baseline) so the mockup's shared 38px/28.5pt clearance was fine there, but "quote" (the "q") and "nabídka" (the tall "í" diacritic) both need more room than "invoice" ever did; fixed by bumping just the quote's date-row clearance (38px→60px in the preview, 28.5pt→45pt in the real PDF), leaving the wordmark itself untouched. And the footer/thank-you line wasn't pinned to the true bottom of the page at all in the *real* PDF — it turned out `InvoicePdf.tsx`/`QuotePdf.tsx` had never actually carried over the mockup's `<div style="flex:1"></div>` spacer-before-footer pattern that makes the footer sit at the literal bottom of the A4 page regardless of content length; added `<View style={{flex:1}} />` before both footers, and gave the in-app preview cards a matching fixed `h-[1123px]` (the mockup's own page height) plus the same flex-spacer, so both artifacts now genuinely pin the footer to the bottom of a full page instead of it just trailing right after the totals.

Verified live in the browser end-to-end after all three fixes: the quote and invoice preview cards now show the entire page at a glance with no scrollbar, scaling correctly as the window resizes; scrolled a card to its bottom to confirm the footer note sits at the true bottom of the (mostly blank) page rather than floating right under the totals; re-downloaded and re-rendered both real PDFs (PyMuPDF) and confirmed the same footer-pinning and wordmark-clearance fixes landed there too, with zero overlap between "nabídka" and the date line. Clean `npm run build` + `npm run lint` throughout; dev DB reseeded back to empty afterward.

**Fixed a real tittle-clipping bug in the preview, and added a CZ/EN toggle for the preview card, defaulting to the app's own language (done).** Two more items from live browser testing.

*Tittle clipping* — the dots above both "i"s in "invoice" were getting cut off at the very top. Root cause: `DocumentPreviewScaler`'s outer wrapper uses `overflow: hidden` with a height computed from `scrollHeight` (needed for the zoom-to-fit technique above) — but the wordmark's tight `leading-[0.8]` (matching the mockup exactly) means the dot's ink genuinely renders above the line box's own top edge, which isn't counted in `scrollHeight` and isn't a layout overflow the browser would otherwise clip — it only became a clipping problem once `overflow:hidden` was introduced for the scaler. Fixed with a small `pt-[16px]` buffer at the top of the fixed-height content, absorbed harmlessly into the `flex-1` spacer above the footer rather than changing the overall page height.

*CZ/EN preview toggle, defaulting to the app's language* — added a small `SegmentedTabs` (CZ/EN) above each preview card, wired the same way the app's existing List/Calendar and Table/Calendar switches already work: `href`-driven (`?previewLang=cs`/`?previewLang=en`), not client state, so no new client component or prop-drilling was needed. The invoice/quote detail pages now read `previewLang` from `searchParams`, defaulting to `getLocale()` (the app's own configured language) when absent — the natural "default to whatever you're already using" behavior, not a hardcoded language. Deliberately scoped narrow: only the preview card's own labels (`pt`/`pq`, a *second* `getDictionary()` call at the toggle's language) switch — the rest of the page (buttons, sidebar cards, status pills) stays in the app's own configured language via the existing `t`/`ti`, exactly like the PDF's own EN/CZ chooser has always been independent of the app's UI language. Caught and fixed a small pre-existing rough edge while wiring this up: the invoice preview's "Total" column header was silently reading from the *quotes* dictionary namespace (`t.finance.quotes.total`) instead of its own — added a proper `total` key to `finance.invoices` (both languages) and fixed the reference.

Also hit the exact same stale-dev-server problem as the batch before this one, twice in a row — `pkill -f "next dev"` kept failing to actually kill the background process, silently leaving an old server on port 3000 serving stale code no matter how many times a "fresh" one was started elsewhere. Confirmed via `lsof -i:3000` before trusting any further live-browser check; the reliable fix is `lsof -ti:3000 | xargs kill -9`, not `pkill` by name.

Verified live in the browser: dots above "invoice" now render fully round and uncropped (zoomed screenshot); the toggle appears on both invoice and quote previews, defaults to English (matching the app's own current locale), and switching to CZ correctly re-labels the whole preview card ("invoice"→"faktura", "Issued"→"Vystaveno", etc.) while every other control on the page (buttons, sidebar) stays in English. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Sidebar readability pass (done).** User feedback, unrelated to the finance-PDF work above: the sidebar nav text was too small/low-contrast against the glass panel to read comfortably. `Sidebar.tsx`'s nav links were 11px with the shared `.nav-item` class's default 65%-opacity inactive color — bumped to 14px with `!text-ink/80` on inactive items (only within Sidebar; `.nav-item` itself is shared with `TimeTrackerTabs.tsx`'s horizontal tab bar, which wasn't part of this complaint, so the override went on Sidebar's own `<Link>` elements via Tailwind's `!` important-modifier, not the shared CSS class — same "beat unlayered CSS" pattern documented earlier in this file). Also bumped the "EVENT SYSTEM" wordmark (11px→13px), the user-name/sign-out row (10px/9px→12px/10px), and the avatar placeholder block (20px→24px) for proportion, and widened the sidebar itself (216px→248px) to give the larger text room without cramping. Checked the one real risk before shipping — the longest nav label anywhere in either language, Czech's "Sledování času" (Time tracker) — fits on one line with room to spare at the new size and width (zoomed screenshot). `.nav-item`'s own font-size/padding in `globals.css` were deliberately left untouched so `TimeTrackerTabs`/`MobileTopBar` (which already had its own `!text-[13px]` override) aren't affected by this change.

Verified live in the browser in both English and Czech (switched the app's language via Settings → App settings to check the longer Czech label, then switched back). Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Section navbars +2px (done).** Follow-up to the sidebar readability pass: user asked for the per-section tab bars to get the same treatment, by 2px. Four spots, all bumped +2px: the shared `SegmentedTabs.tsx` component (11px→13px — covers Finance's Quotes/Invoices/Expenses/Reports tabs, plus every other `SegmentedTabs` usage app-wide: Dashboard's List/Timeline/Calendar, Events' Table/Calendar, Time Tracker's Timer/Manual and List/Calendar switches); `EventTabs.tsx` (9px→11px — Overview/Milestones/Expenses/Time/Quotes & invoices/Files); Settings' own hand-rolled tab bar in `settings/page.tsx` (9px→11px, all 5 tabs: Company/Users & roles/Templates/Invoice emailing/App settings); and the shared `.nav-item` class in `globals.css` (11px→13px), which is what `TimeTrackerTabs.tsx`'s Tracking/Overview bar and the Day/Week/Month switch actually read from — the one spot deliberately left alone during the earlier sidebar pass specifically so it wouldn't affect this bar, now bumped on purpose. `Sidebar.tsx`'s own nav (already 14px with its own `!important` override from the prior pass) and `MobileTopBar.tsx` (already `!text-[13px]`) were untouched — not part of this request and already sized independently.

Verified live in the browser across all four locations: Finance's tab bar (`/finance/invoices`) and Settings' tab bar (`/settings`) via screenshot comparison; an event detail page's `EventTabs` (`/events/[id]`) and Time Tracker's `Tracking`/`Overview` + `Day`/`Week`/`Month` bars (`/time-tracker`) confirmed larger by direct navigation and screenshot. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Dashboard section-title sizing, "Latest expenses" made clickable, sidebar sign-out cursor fix (done).** Three small items from direct user feedback. (1) The Dashboard's four section headings ("Needs attention", "Upcoming events", "Latest expenses", "Balance") all use the shared `.heading-label` class, which is also used app-wide as small form-field labels — bumping the class itself would have oversized every form label in the app, so each of the four occurrences got a scoped `!text-[12px]` override directly in `dashboard/page.tsx` instead (8px → 12px), leaving `.heading-label`'s base size and every other consumer untouched. The `AttentionTile`'s own per-metric label (e.g. "Invoice overdue") was deliberately left at its original size — it's a card's internal label paired with a large value figure, not a section title, and wasn't part of what was reported. (2) "Latest expenses" — the whole card is now a `Link` to `/finance/expenses` (was a plain `<div>`), styled with the same row-hover convention used elsewhere in the app (`group` + `hover:bg-ink/5` + the heading turning `group-hover:!text-accent`) rather than a bespoke treatment. No permission gate needed: `/finance/expenses` itself only calls `requireUser()`, no `canView*` check, so anyone who can already see the dashboard's expense summary (scoped via `expenseWhereForUser`) can already reach the full list. (3) The sidebar's sign-out `<button>` had no `cursor: pointer` — confirmed via `getComputedStyle` that Tailwind v4's preflight resets native form controls (including `<button>`) to `cursor: default`, unlike `<a>` elements which keep the browser's own pointer cursor; every other `.btn`/`.btno` in the sidebar is a `Link`, so this is the one spot in the sidebar actually affected. Fixed by adding an explicit `cursor-pointer` class to just that button — scoped to the reported element, not a blanket audit of every native `<button>` in the app.

Verified live in the browser against reseeded demo data: zoomed screenshots confirmed all four section titles render larger; clicked "Latest expenses" and confirmed it lands on `/finance/expenses` with the Expenses tab active and the same rows; `getComputedStyle(signOutButton).cursor` confirmed `"pointer"` post-fix. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Tag text centering, app-wide section-title sizing sweep, client detail page reorder (done).** Three items from direct user feedback following the Dashboard title-sizing pass above. (1) `.tag` (the shared status-chip class in `globals.css` — used by `StatusPill`'s three components and every non-CZK currency badge) gained an explicit `text-align: center` — it's `display: inline-block` with symmetric padding so this was mostly latent, but makes centering correct rather than incidental wherever a tag's box ever ends up wider than its text. (2) Extended the same `!text-[12px]` scoped-override treatment from the Dashboard pass to every other genuine **section title** across the app (8px/9px → 12px), applying the same rule established there: bump a heading that introduces a card/section of content, leave alone (a) table/grid column-header rows, (b) single-input form field labels, and (c) the small "label paired with one big stat value" pattern used by `AttentionTile`/`KpiCell`/`ReportKpi` and the two "eyebrow" captions that sit directly above an `<h1>` page title (`{n} clients`/`{n} events`/`{n} open documents`/`{n} accounts` — those stayed at their original size, they're captions, not section titles). Touched ~30 spots across `clients/[id]/page.tsx`, `finance/invoices/[id]/page.tsx` (Payment state/Record payment/Linked/History/Delete), `finance/quotes/[id]/page.tsx` (Status/Linked/Created by/Delete), `events/[id]/page.tsx` (all four glass cards: Budget vs actual/Time logged/Documents/Team), `events/[id]/quotes/page.tsx`, `events/[id]/time/page.tsx`, `time-tracker/tracking/page.tsx`, `time-tracker/overview/page.tsx`, `settings/page.tsx`'s Templates-tab heading, `finance/reports/page.tsx` (three headings using the separate `.label` class rather than `.heading-label` — bumped the same way), and every settings-tab-rendered form component (`CreateUserForm`, `AppSettingsForm`, `CompanySettingsForm`, `InvoiceEmailingForm`, `RoleReferenceTable`) plus `EventForm.tsx`'s own section headings (Dates/Venues/Client/Contact people) and the Time Tracker hero cards' headings (`RunningTimerBox`'s "Start a timer", `ManualEntryForm`'s "Add manual entry") — both structurally identical to `CompanySettingsForm`'s sections, so held to the same rule. (3) Client detail page: swapped the Contacts and Events sections so Contacts renders first (per explicit request), and bumped the linked event title in the desktop events row from `font-medium` to `font-semibold` to read as genuinely bold (the mobile row via `MobileListRow` was already `font-semibold`, so only the desktop row needed the change) — also bumped that page's own Contacts/Events/Delete headings to 12px as part of the same sweep.

Verified live in the browser against reseeded demo data across every touched page: client detail (Kobra a.s. — Contacts now above Events, event title visibly bold, `getComputedStyle` confirmed `text-align: center` on a real `.tag`), event detail's four glass cards, invoice detail's five sidebar cards, quote detail's four sidebar cards, Finance → Reports' three chart/breakdown headings, Time Tracker's Tracking (manual-entry hero, period-totals card) and Overview (chart title) tabs, all five Settings tabs (Company, Users & roles, Templates, Invoice emailing, App settings), and the New Event form's four section headings — confirming in every case the section title is now visibly larger than its neighboring field labels/table headers, which were deliberately left untouched. Clean `npm run build` + `npm run lint` throughout; dev DB reseeded back to empty afterward.

**Sidebar width reverted, section navbars reverted to their original size (done).** Two corrections on the previous two passes, both direct user feedback that the recent sizing changes had overshot. (1) The sidebar readability pass (bumped nav text 11px→14px) had also widened the panel 216px→248px to give the bigger text room — the user only wanted the text bigger, not the panel wider, so `Sidebar.tsx`'s width went back to `w-[216px]`; the `!text-[14px]` override on the nav links (and the other readability-pass bumps — wordmark/user-row/avatar sizing) were untouched, so the text stays exactly as large as before, just in a narrower panel. (2) The "Section navbars +2px" pass from two turns ago (`SegmentedTabs` 11→13px, `EventTabs` 9→11px, Settings' own tab bar 9→11px, the shared `.nav-item` class 11→13px for `TimeTrackerTabs`) was fully reverted back to its pre-bump sizes — user reported it "feels too much" after living with it. This is a straight revert, not a new decision: all four spots went back to their exact original values. Note this is independent of the *separate* "section title" sizing sweep from the same session (Dashboard's "Needs attention"/"Balance" and its app-wide equivalents on every other page) — those `!text-[12px]` heading-label/label overrides were left completely alone, since the user's complaint was specifically about the tab/navbar bars, not the card/section titles.

Verified live in the browser against reseeded demo data: zoomed the sidebar to confirm the nav text is still clearly large while the panel itself is narrower; confirmed Finance's tab bar, an event detail page's `EventTabs`, Settings' own tab bar, and Time Tracker's Tracking/Overview + Timer/Manual + List/Calendar bars are all back to their smaller pre-bump size, while the section titles on those same pages (Budget vs actual/Time logged/Documents/Team, Company details/Bank/Branding/Connected accounts, Start a timer/My entries/This week) are unchanged and still at the larger size from the previous pass. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Sidebar nav text dialed back to 12px, "My tracker" widget rebalanced (done).** Immediate follow-up to the sidebar-width revert above — user found 14px slightly too large after seeing it narrower, and separately wanted the idle `TimerWidget` (the "MY TRACKER" card at the bottom of the sidebar) rebalanced: its "My tracker" heading was still the tiny default 8px `.heading-label` size while "No timer running" (`text-sm`, 14px) already read well, and "Start one →" (9px) was asked up by 2px. `Sidebar.tsx`'s `!text-[14px]` override → `!text-[12px]` (both the main nav list and the Settings link, which share the same className string, so one `replace_all` covered both). `TimerWidget.tsx`: `t.myTracker`'s heading gained `!text-[12px]` (applied to both the idle and running-timer JSX branches, which duplicate this label independently) and `t.startOne`'s link went from `text-[9px]` to `text-[11px]`. `t.noTimerRunning` (`text-sm`) was left untouched — it was already the size the user liked.

Verified live in the browser against reseeded demo data: zoomed the sidebar and read `getComputedStyle` on real elements — nav items and "My tracker" both confirmed at exactly `12px`, "Start one →" confirmed at `11px`. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**"My tracker" bolded with tighter tracking, main nav rows tightened (done).** Two more direct follow-ups on the sidebar. (1) `TimerWidget.tsx`'s "My tracker" heading (both the idle and running-timer JSX branches) gained `!font-bold !tracking-[0.08em]` on top of its existing `!text-[12px]` override — `.heading-label`'s default tracking (`0.22em`) reads fine at 8px but was too wide once bumped to 12px bold, so it was tightened at the same time rather than left at the wide default. (2) `Sidebar.tsx`'s five main nav rows (Dashboard/Clients/Events/Finance/Time tracker) went from `!py-2.5` (10px vertical padding) to `!py-1.5` (6px) — scoped narrowly to just that `<nav>` list per the user's explicit "the first part of sidebar": the standalone Settings link at the bottom (which shares the same base className string, so this needed an explicit separate edit rather than a blanket find/replace) kept its original `!py-2.5`, since it's a single link, not a list of rows to compress.

Verified live in the browser against reseeded demo data via `getComputedStyle` on real elements: the five nav rows' computed `padding` is `6px 10px` (was `10px`), "My tracker" is `font-weight: 700` with `letter-spacing: 0.96px` (down from the ~2.64px the old `0.22em` default worked out to at 12px), and the Settings link's padding is confirmed unchanged at `10px`. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Sidebar/TimerWidget sizing fully reverted to the last pushed commit (done).** After the several rounds of sidebar sizing tweaks above, user asked to just revert the sidebar and its text back to how it looked in the last actual git commit — none of this session's sidebar work had ever been pushed, so "the last commit" meant undoing every sizing change made across this whole session in one go, not another manual re-tune. Ran `git checkout -- web/src/components/Sidebar.tsx web/src/components/TimerWidget.tsx` to restore both files byte-for-byte to `HEAD` (216px width, original `px-4 pt-4 pb-5 gap-4` padding/gaps, 11px wordmark, plain small nav-item text with no per-link size/tracking overrides, original avatar/username sizing, "My tracker" back to its unstyled default), then re-applied just the one genuine bug fix that had been layered on top of all that sizing work earlier this session — `cursor-pointer` on the sign-out button, which was a real missing-affordance fix (Tailwind's preflight resets native `<button>` cursor to `default`), not a sizing choice, so it didn't belong in the revert. Confirmed via `git diff` that after the checkout + one-line re-edit, the only remaining delta from `HEAD` on either file is that single `cursor-pointer` class.

Verified live in the browser: sidebar renders exactly as it did before any of this session's sizing passes (compact nav rows, small plain "MY TRACKER" label, narrow-but-original-width panel), and `getComputedStyle(signOutButton).cursor` still confirms `"pointer"`. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Per-account language + new Settings → General tab (self-service account management for every role) (done).** User asked directly whether the language setting was global, then asked to make it per-account and add a first "General" tab to Settings with name/surname/photo/email/phone/password-reset, available to every role — not just Admin/Accountant, which is the real point: previously Member/Producer accounts with neither `canManageUsers` nor `canManageCompanySettings` hit a dead-end "you don't have access" message on `/settings` with no self-service option at all.

*Per-account locale* — `User.locale` (new nullable `String?`, "en"/"cs"/null) overrides `CompanySettings.locale` when set. `getLocale()` (`src/lib/i18n.ts`) — previously a bare read of `CompanySettings.locale`, called with **zero arguments from ~40 call sites** across nearly every page — was rewritten to check the current session's user first (`auth()` for the session id, a fresh DB lookup for `locale`) and fall back to the company default, while keeping its zero-arg signature so none of those ~40 call sites needed touching. The DB lookup is new `authz.ts` export `getFreshUserFields(id)` — a single `cache()`-wrapped `prisma.user.findUnique` selecting everything that can go stale on the JWT-backed session (`active`, `name`, `email`, `phone`, `avatarPath`, `locale`) that `requireUser()` (for its existing `active` re-check), `getLocale()` (for `locale`), and `(app)/layout.tsx` (for live name/avatar in the sidebar) all now call — React's `cache()` dedupes by function+args within one request, so three call sites collapse into one actual query rather than three. The root `layout.tsx`'s own separate `<html lang>` computation was also switched to call `getLocale()` instead of duplicating the raw `company?.locale` read, so the per-account override reaches that attribute too.

*The General tab itself* — new `GeneralSettingsForm.tsx` (three independent `<form>`s, each with its own `useActionState`, mirroring the existing multi-form-per-page pattern already used by `EditUserForm.tsx`): **Account** (first/last name, email, phone, photo upload with live preview + remove-checkbox, same sr-only-input-plus-label pattern as the Company logo uploader), **Password** (current/new/confirm via the existing `PasswordInput` hold-to-reveal component, `bcrypt.compare` against the real hash before allowing a change), **Language** (three radio options: company default — labelled with what that currently resolves to, English, Czech). Three new actions in `actions/settings.ts`: `updateOwnProfileAction`, `updateOwnPasswordAction`, `updateOwnLanguageAction` — all just `requireUser()`-gated (no permission check beyond being logged in, since this only ever touches the caller's own row). `settings/page.tsx`'s top-level `if (!canUsers && !canCompany) return <no-access-message>` gate is gone entirely; "General" is now an unconditional first tab and the default landing tab (`tab = params.tab || "general"`, replacing the old `canCompany`-dependent fallback), with the other five tabs still conditionally shown exactly as before.

*No `firstName`/`lastName` columns* — deliberately. `User.name` stays the single stored/displayed field everywhere else it's already read (Users table, quote "Created by", event owner/team, timers, invoice history, ~20 call sites) — splitting it in the DB would have meant touching every one of them or keeping two representations in sync. Instead `GeneralSettingsForm` does a best-effort client-side split of the current `name` on its first space (matching the "structured only where it earns its keep" precedent already set by the Client-address-vs-Event-address decision documented earlier in this file), and `updateOwnProfileAction` rejoins `${firstName} ${lastName}`.trim()` back into the one `name` column on save.

*Avatar storage* — `User.avatarPath` (new nullable column) follows the exact `CompanySettings.logoPath` precedent: `saveAvatar`/`deleteAvatar`/`readAvatar` in `uploads.ts` (PNG/JPG/WEBP, 3MB cap, disk storage under `web/uploads/avatars/` — already covered by the existing blanket `/uploads` gitignore rule, no change needed there), served through a new authenticated route handler `api/uploads/avatar/[filename]`. Deliberately **not** owner-gated beyond "must be logged in" (same trust level as the company logo, not receipts) — a coworker's profile photo isn't sensitive data in this internal tool, and the design leaves room for showing other people's avatars elsewhere later even though nothing beyond the sidebar/mobile-top-bar does yet. Wired into `Sidebar.tsx` and `MobileTopBar.tsx`'s existing user-badge spots (falls back to the pre-existing empty-square / person-icon placeholder when no photo is set) — both now read the **fresh** `getFreshUserFields()` name/avatar in `(app)/layout.tsx` rather than the JWT-sourced `user.name`, so an edit on the General tab shows up in the sidebar immediately instead of waiting for the session token to refresh at next login (the JWT itself is never touched — this was purely about which value gets handed to the two display components).

**Two real bugs caught and fixed while verifying live, both only visible at runtime:** (1) The exact "Functions cannot be passed directly to Client Components" RSC-boundary error already documented earlier in this file, reproduced fresh — `settings/page.tsx` originally passed `t={t.settings.general}` (a dictionary slice containing the parameterized `useCompanyDefault(lang)` function) straight into the client `GeneralSettingsForm`. Fixed the same established way: the component takes a plain `locale: Locale` prop and calls `getDictionary(locale).settings.general` itself, importing from `@/lib/dictionary` (never `@/lib/i18n`, which would pull Prisma into the client bundle). (2) **A genuinely new bug, not the RSC one**: after successfully saving a language change, the radio group kept showing "Company default" checked even though the save worked (page genuinely repainted in Czech, DB confirmed via `sqlite3` as `locale='cs'`) — debugged by injecting temporary `data-debug-*` attributes into the DOM and reading them back via the browser tool, which proved the React *state* (`languageChoice`) was correctly `"cs"`, but the actual DOM radio's `.checked` **property** stayed on the old selection — a real React quirk where controlled `<input type="radio">` siblings don't reliably get their `checked` property re-patched by an out-of-band (Server-Action-triggered) refresh, even though the state driving them is provably correct. Fixed by switching the three radios from controlled (`checked`) to uncontrolled (`defaultChecked`) and wrapping just that inner `<div>` (not the whole form) in `key={currentLocale ?? "default"}` so a change to the real saved value remounts fresh DOM nodes instead of trying to patch existing ones — scoped to that one div specifically so remounting doesn't also reset the `useActionState` success/error message, which sits as a sibling outside it. Also needed React's documented "adjust state during render" pattern (comparing against a `prevCurrentLocale` state value, not a `useEffect`, since this project's eslint config's `react-hooks/set-state-in-effect` rule forbids synchronous `setState` inside a plain effect) to keep the `languageChoice` value itself in sync with the fresh prop in the first place.

Verified live end-to-end in the browser: logged in as Admin, edited name/email/phone and saved (confirmed the sidebar's bottom user row updated to the new name immediately, no re-login needed); uploaded a real PNG avatar via the browser tool's file-upload capability, confirmed it rendered live in the sidebar (`<img src="/api/uploads/avatar/...">`, checked via DOM query since a 20×20px sidebar avatar isn't visually distinguishable in a screenshot), then removed it and confirmed it reverted to the placeholder on a fresh page load; changed password with a deliberately wrong current password (correctly rejected), then with the right one (succeeded), then changed it back; switched personal language to Czech and confirmed the *entire app* — sidebar nav, tab labels, everything — repainted in Czech immediately with no full reload, then confirmed via `sqlite3` that only that one user's row changed (`CompanySettings.locale` stayed `"en"` throughout), then switched back to English and to "Company default" and confirmed each correctly round-tripped including the radio-desync fix. Finally logged in as the seeded Member account (`m.dvorak@eventsystem.cz`, no Settings permissions at all) and confirmed `/settings` now shows exactly one tab — General — with their own real name ("M." / "Dvořák", split correctly) instead of the old permission-denied message. Clean `npm run build` + `npm run lint` throughout; dev DB reseeded back to empty afterward, test avatar file cleaned up from disk.

**"Company default" language option removed, App settings' Language section deleted entirely (done).** Follow-up user request, right after the per-account-language batch above: per-account language should always be a real, explicit choice — no "inherit from company" option — and the company-wide language toggle that used to live in Settings → App settings is gone outright. `GeneralSettingsForm.tsx`'s language radios are now just English/Čeština (the third "Company default (X)" option and its `useCompanyDefault()` dictionary function are both gone); the initial/re-synced selection falls back to `locale` (the app's currently-resolved language) instead of a `"default"` sentinel when the account has never explicitly chosen one, so the radio always shows *something* real rather than a placeholder. `AppSettingsForm.tsx` lost its entire Language section (heading, intro, two radios, the "applies to the whole app" note) — only the six colour swatches + Preview remain. `updateAppSettingsAction` no longer reads or writes `CompanySettings.locale` at all, which matters beyond just "the field is unused now": leaving the old `formData.get("locale")` read in place would have silently reset the company's locale to English every time someone saved a colour change, since the form no longer submits that field. `CompanySettings.locale` itself is untouched in the schema — it's frozen at whatever value it already holds (still read by `getLocale()`'s fallback for a not-yet-personalized account and for the unauthenticated login page), it's just no longer settable from anywhere in the UI. Dead dictionary keys (`appSettings.languageHeading`/`english`/`czech`/`appliesWholeApp`, `general.useCompanyDefault`) removed from both `en` and `cs`.

Verified live in the browser: General tab shows exactly two language radios with the correct one pre-selected on load; App settings tab shows only Colours + Preview, saving colours confirmed via `sqlite3` to leave `CompanySettings.locale` at `"en"` unchanged; re-tested the language save/switch round-trip (English → Czech → English) on the General tab with only two options and confirmed the earlier radio-desync fix still holds. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Real bug fixed: a role/custom-role/card-holder change made by an admin had no effect on an already-logged-in target user until they logged out and back in — reported directly** ("I changed one account's role from Member to Admin, but from that account I still can't see all the Settings tabs"). Root cause: `requireUser()` already re-checked `active` against the DB on every request (built specifically for the deactivation feature), but for everything else it just returned `session.user` — the JWT's payload, written once at sign-in by `auth.ts`'s `jwt` callback and never refreshed on subsequent requests. `role`/`isCardHolder`/`customRole` — every field `resolvePermissions()` actually uses — were the JWT's stale snapshot from login, not the current DB row, so an admin's edit to someone else's role/custom-role/card-flag silently didn't apply until that person's session token was reissued (next login, or the fixed 8h expiry). Fixed by widening the already-`cache()`-wrapped `getFreshUserFields()` (added for the General-tab work above) to also select `role`/`isCardHolder`/`customRoleId`/`customRole`, and rewriting `requireUser()` to build its returned `SessionUser` entirely from that fresh row instead of the JWT — `id` is still read from the session (identity only), everything permission-relevant now comes from the DB on every single request. Since `getFreshUserFields` is request-scoped `cache()`, this doesn't add a new query on top of the existing `active` check — it's the same call, just selecting more columns.

Verified live in the browser with a technique that avoids the multi-tab cookie-sharing problem (browser tabs in this automation share one cookie jar, so a second logged-in tab isn't a real second session): logged in as the seeded Member account in one tab, confirmed only the General settings tab was visible, then — without touching that browser session at all — ran a direct `sqlite3 UPDATE User SET role='ADMIN'` to simulate exactly what an admin's "Edit user → change role" action does to the DB, then simply reloaded `/settings` in the same already-authenticated tab (same session cookie, no re-login) and confirmed all six tabs (Company/Users & roles/Templates/Invoice emailing/App settings, plus General) now appeared immediately. Also confirmed the new bold-green success-message styling (below) on the same page. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

**Every "saved successfully" message across the app is now bold and colored (positive teal, the app's existing "success" token) instead of plain body text — direct user request.** Found and fixed nine call sites reading `state.success`/similar and rendering it as plain `<p className="text-sm">`: `GeneralSettingsForm.tsx` (all three forms — profile/password/language), `CompanySettingsForm.tsx`, `AppSettingsForm.tsx`, `InvoiceEmailingForm.tsx`, `CreateUserForm.tsx`, `EditUserForm.tsx`, `UsersTable.tsx`'s two inline password-reset confirmations, `SendInvoiceEmailButton.tsx`, and `ExpenseForm.tsx` — each gained `text-positive font-bold` (or kept its existing box/border styling and just added the color+weight) on top of whatever it already had, rather than reusing "green" as a new literal color: `--color-positive` is this app's existing semantic "good/confirmed" token (the same teal used for `PAID`/`CONFIRMED`/`ACCEPTED` status tags), so this reuses established vocabulary instead of introducing a one-off shade. Error messages (`text-warning`) were already styled distinctly before this — success messages were the one state left as plain, unstyled text.

Verified live in the browser: saved a General-tab profile edit and confirmed "Account details saved." now renders in bold teal, matching the same visual weight the app already gives `text-warning` error text elsewhere.

**"No permission" access-denied messages made noticeably larger app-wide.** All 10 occurrences of the pattern (a page whose create/edit action a role lacks permission for renders a one-line explanation instead of the real form) shared the exact same tiny, low-contrast styling — `text-sm placeholder-text` (14px, 40%-opacity ink) — easy to miss, especially next to a bold `<h1>` page title sitting right above it. Bumped every one to `text-lg font-semibold text-ink` (18px, full-opacity, semibold) across `clients/new`, `clients/[id]/edit`, `settings/users/[id]/edit`, `finance/layout.tsx` (the section-wide "no Finance access" case), `finance/invoices/new`, `finance/quotes/new`, `finance/quotes/[id]/edit` (both its "not a draft" and "no permission" branches), `finance/expenses/[id]/edit`, `events/new`, and `events/[id]/edit`. Deliberately not colored as a warning/error (`text-warning`) — this isn't something wrong, it's a normal access boundary — just made genuinely legible instead of the previous barely-there gray.

Verified live in the browser: logged in as Producer (no Finance-create access) and confirmed `/finance/invoices/new` now shows "You don't have permission to create invoices." at a clearly larger, bolder, full-contrast size. Clean `npm run build` + `npm run lint`; dev DB reseeded back to empty afterward.

## Environment

This machine had **no Node.js, npm, Homebrew, Docker, or working Postgres** when the project started (MacPorts is present but broken — OS/platform mismatch). Network access works.

- **Node.js** (v24.19.0 LTS) is installed user-local at `~/.local/node-v24.19.0-darwin-arm64`, symlinked into `~/.local/bin` (already on `PATH`). No sudo, no Homebrew. If a fresh shell can't find `node`/`npm`, check `~/.local/bin` is on `PATH`.
- **Database**: SQLite via Prisma for local dev (`web/prisma/dev.db`, gitignored). No separate DB service to run. Swapping to Postgres later means changing the Prisma datasource + running a migration — not a rewrite.
- **Git**: the *entire home directory* (`/Users/simonpletka`) is already a separate, commit-less git repo. Don't touch it. This project has its **own** nested repo at `PRODUCTION_APP/.git`, initialized separately — that's the one to use for all commits here.

## Tech stack

- Next.js **16.3.1** (App Router, TypeScript, RSC, Turbopack by default), project lives in `web/`. This is newer than most training data — `web/AGENTS.md` (auto-written by `next dev`) points at the bundled docs in `web/node_modules/next/dist/docs/`; read the relevant guide there before assuming Next 15-era APIs. Concretely: `params`/`searchParams` in pages are `Promise`s (must `await`), `middleware.ts` is renamed to `proxy.ts` (not used in this project — auth is checked per-layout/per-server-action instead, which is Next's own current recommendation), and parallel-route slots need a `default.js`.
- Prisma **7.9.1** — also a major-version jump from most training data. Breaking changes that bit during setup: `datasource.url` can no longer live in `schema.prisma` (goes in `prisma.config.ts` instead), SQL providers require an explicit driver adapter package (`@prisma/adapter-better-sqlite3` + `better-sqlite3` here), and the generated client lives wherever `generator client { output = ... }` points (`web/src/generated/prisma`, gitignored, import via `@/generated/prisma/client` and `@/generated/prisma/enums`) instead of `node_modules`. `prisma init` vendors a `.claude/skills/prisma-*` directory into the project with detailed migration guides — check there before fighting a Prisma error that looks version-related.
- SQLite (dev), via the adapter above
- NextAuth (Auth.js), Credentials provider only for now — bcrypt-hashed passwords, accounts are admin-seeded, no self-registration (per brief §2.1). Google OAuth provider to be added later purely via env vars once the user supplies Google Cloud credentials.
- Tailwind CSS v4, theme tokens taken directly from the wireframes (dark theme):
  - background `#131211`, surface `#1a1918`, text/border `#f3f2f2`, accent `#ec3013`, plus (since the "glass & gradient" redesign) two semantic-only tokens: `--color-positive` `#2dd4bf` (teal, positive status) and `--color-warning` `#dc2626` (red, warnings)
  - accent orange is reserved for primary actions and general emphasis — not decorative; positive/warning are reserved even further, for the `.tag` status-chip variants only (see `StatusPill.tsx`)
  - Neue Regrade font (self-hosted, `web/src/fonts/neue-regrade/` — a commercial font, not on Google Fonts, licensed for commercial use per `LICENSE.txt` there; loaded via `next/font/local` in `layout.tsx`, not `next/font/google`; replaced Archivo, which was only ever a Phase 1 placeholder pick), 2px rules on primary dividers / 1px on secondary. **Superseded**: the original Phase-1 "0 border-radius everywhere" decision is gone as of the "glass & gradient" redesign — buttons/pills are now fully rounded, cards/inputs use a ~10–16px radius, and the sidebar/sticky toolbars use a frosted `.glass-panel` (`backdrop-filter: blur` + a `color-mix(bg)` tint layer — blur alone doesn't obscure sharp text behind a floating panel, learned the hard way when the unpinned sidebar's hover-overlay was unreadable against a real page heading underneath).
- `npm install` blocks postinstall scripts by default (npm 11's allow-scripts). When adding a new dependency that needs a postinstall (prisma, esbuild, native modules, etc.), run `npm approve-scripts <pkg>` after install or it silently won't work.
- `@react-pdf/renderer` (server-side invoice/quote PDF generation, `src/lib/pdf/InvoicePdf.tsx` + `QuotePdf.tsx`, rendered via `renderToBuffer` in route handlers — not part of the app's own React tree, so no version-conflict risk) + `qrcode` (renders the Czech "QR Platba" payment string to a PNG data URL for the PDF). Registers the Neue Regrade TTF rather than using the standard "Helvetica" font — Helvetica's WinAnsi/cp1252 encoding has no glyphs for ě/ř/ů/ď/ť/ň, so Czech diacritics were silently dropping from PDF output. **Not registered once at module scope** — `shared.tsx`'s `registerAppFont()` is called fresh inside each PDF component's render body under a uniquely-suffixed family name, because `@react-pdf/font`'s `FontStore` caches the loaded font/glyph-subsetting state for the process lifetime under a fixed family name, and that state was found to leak across unrelated `renderToBuffer()` calls (confirmed: rendering an invoice then a quote in the same server process silently corrupted specific digits in the second document). See the Status entry on the Czech-PDF batch for the full repro. Registers `NeueRegrade-Regular.ttf`/`NeueRegrade-Bold.ttf` — real static instances flattened out of the variable font via `fonttools varLib.instancer`, **not** the variable `NeueRegrade-Variable.ttf` the live web app uses via `next/font/local` — react-pdf's fontkit pipeline doesn't interpolate a variable font's weight axis per the registered `fontWeight` key (it loads the file at its own default instance regardless), so registering the variable file under both weights silently produced identical, never-actually-bold glyphs. See the "nothing rendered bold" Status entry for the full repro.
- **pdf-lib** (`src/lib/pdf/build-invoice-pdf.tsx`'s `embedIsdoc()`) — post-processes the `@react-pdf/renderer` output to attach the invoice's generated ISDOC 6.0.2 XML (`src/lib/isdoc.ts`) as a named file inside the same PDF, so one download is both the human-readable PDF and a machine-readable e-invoice. Zero native deps, no version-conflict risk with react-pdf (only touches the finished PDF bytes, never the React tree).
- **ARES** (`https://ares.gov.cz/ekonomicke-subjekty-v-be/rest/ekonomicke-subjekty/{ico}`) — free public Czech business-registry API, no auth/key required. `src/lib/ares.ts` does the fetch + shape-mapping (`obchodniJmeno`→name, `sidlo.textovaAdresa`→address, `dic`→DIČ), `src/app/api/ares/[ico]/route.ts` wraps it behind session auth, `src/hooks/useAresLookup.ts` is the client-side hook. Used by both the Event form (client/company fields) and Settings → Company.
- **Photon** (`https://photon.komoot.io/api/`) — free public worldwide address-autocomplete API (komoot's instance over OpenStreetMap data), no key. Same "free public API, auth-gated route + client hook" shape as ARES: `src/lib/address-search.ts` (fetch + label formatting), `src/app/api/address-search/route.ts`, and a self-contained debounced dropdown component `src/components/ui/AddressAutocompleteInput.tsx` (used wherever an address field appears — Event form, Company settings, Client form). Chosen over the Czech-only RÚIAN registry specifically because the user asked for worldwide coverage.
- **nodemailer** (invoice-emailing transport, `src/lib/email.ts`) — plain SMTP, no vendor account needed; reads `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASS`/`SMTP_FROM` from the environment (unset in this dev environment — commented-out placeholders in `.env`) and throws a clean `EmailNotConfiguredError` rather than crashing when they're missing. Template text (`{{token}}` placeholders) lives in `src/lib/email-templates.ts`, deliberately import-free of `email.ts`/nodemailer so a Client Component (the Settings → Invoice emailing form) can use the tokens/renderer without pulling Node's `net`/`tls` into the browser bundle — see that entry in Status for the exact failure this avoids.
- **googleapis** (Drive v3 client, `src/lib/gdrive.ts`) — a **service account**, not OAuth: reads `GOOGLE_SERVICE_ACCOUNT_JSON_BASE64` (the downloaded key JSON, base64'd) and `GDRIVE_FINANCE_ROOT_FOLDER_ID` (the user pastes this once from the Drive folder's URL) from the environment, same "unset in dev, throws a clean `DriveNotConfiguredError` rather than crashing" shape as the SMTP setup. Automatically exports paid invoices/accepted quotes/expense receipts into a `Root/<Quotes|Invoices issued|Invoices received>/<year>/<filename>` tree the user maintains in Drive themselves — see the Status entry for the exact filename/year rules and the best-effort (never blocks the real action) call sites.
- Uploaded expense receipts are stored on local disk at `web/uploads/receipts/` (gitignored, **not** under `public/`) and served only through an authenticated route handler (`src/app/api/uploads/receipts/[filename]/route.ts`) that checks the requesting user actually has access to the expense the receipt belongs to. Won't survive a redeploy to a stateless host — fine for local dev, would need object storage (S3-alike) before shipping anywhere else. Company logos follow the same disk-storage pattern at `web/uploads/logos/` (`src/app/api/uploads/logo/[filename]/route.ts`), but without the per-owner check — just requires being logged in, since it's shared branding, not per-user data.
- ESLint's `react-hooks/purity` rule flags `Date.now()` (but not `new Date()`) as an impure call inside a component body — write `new Date().getTime()` instead if a component needs "now".
- ESLint's `react-hooks/set-state-in-effect` rule flags calling `setState` synchronously at the top of a `useEffect` (e.g. seeding a ticking-clock value before starting the `setInterval`). Fix: use the lazy `useState(() => Date.now())` initializer for the starting value, and let the effect only set up the interval/subscription — see `TimerWidget.tsx` / `RunningTimerBox.tsx`.
- ESLint's `react-hooks/refs` rule flags writing to `ref.current` during render (the classic "always-latest callback ref" pattern). If a callback used inside an effect doesn't need to be in the dependency array because the effect itself unmounts the component before it could re-fire, just say so with an inline `eslint-disable-next-line react-hooks/exhaustive-deps` rather than reaching for a ref — see `QuickCreateEventModal.tsx`.

## Data model decisions

- **`Quote.createdById`** (required FK to `User`) + **`User.phone`** (new, optional) — a quote's "Created by" block shows name/email/phone plus `issuedAt` as the creation date; no separate `Quote.createdAt` field, since `issuedAt` is already `@default(now())` and never independently editable, so it already *is* the creation timestamp.
- **`User.locale`** (nullable `String?`, "en"/"cs") overrides `CompanySettings.locale` when set — `null` means "inherit the company default," not "unset English." **`User.avatarPath`** (nullable) follows the exact `CompanySettings.logoPath` precedent (disk storage, authenticated route handler). Neither `firstName` nor `lastName` exist as columns — the Settings → General form splits/rejoins the single `User.name` field client-side instead, since `name` is read directly at ~20 other call sites (Users table, quote "Created by", timers, invoice history) that a real split would have needed to either touch or keep in sync.
- `EventStatus` enum has the brief's 6 states **plus `CANCELLED`** — brief §4.3 explicitly invites adding a cancelled state ("klidně doplníme další stav, např. 'Zrušeno'"); added now since it's cheap pre-launch and painful to migrate later.
- Event detail layout: built as **3c (tabbed)**, not 3d (single-scroll) — user's explicit choice between the two wireframe options the brief left open.
- `TimeEntry` still exists only as a Phase 1 stub (Event-detail relation counts need it); `Quote`/`Invoice`/`Expense` are now the real Phase 2 tables.
- **Numbering (superseded — see the six-item-batch Status entry for the full rationale)**: events, quotes and invoices now share one "YY-XXX" scheme keyed off `Event.number` (e.g. `26-001`), assigned at creation by taking the lowest unused integer for the year (so a deleted event's number is reusable) rather than the old independent per-year `YYYY-Q##`/`YYYY-####` counters this replaced. Quotes/invoices reuse their event's base number, appending `_v2`/`_v3`/… only past the first document of that type for the same event (`src/lib/document-number.ts`). Still not safe under heavy concurrent writes (no locking) — same accepted tradeoff as before.
- **`EventContact`** (1+ per event, replacing the old single `clientName`/`clientPhone`/`clientEmail` fields) syncs onto the linked `Client`'s own `ClientContact` list on save, deduped by email-or-name, as independent copies (no FK back to the source row — deleting one side never touches the other). `Client.invoicingEmail` auto-fills from the first synced contact with an email, but only while still blank.
- **`ItemCategory`** — org-wide default line-item categories (Settings-editable), deliberately *not* a foreign key on `QuoteItem`/`InvoiceItem.category` (plain string instead) — a quote can tag an item with a one-off category name that's never added to the global list, and deleting a default category never touches items already tagged with it, since there's no referential link to break.
- **Invoice discounts** (`discountType: NONE | PERCENT | FIXED` + `discountValue`, invoice-only, not on quotes) apply to the pre-VAT base and recompute VAT on the discounted amount, rather than a flat cut off the final total — the legally/practically correct order, not just the simpler one to implement. `Invoice.total` stores the final post-discount amount, same field every existing consumer already reads.
- **`InvoiceStatus` has no stored `OVERDUE` value** — deliberately. It's `ISSUED | PARTLY_PAID | PAID` only; "overdue" is derived everywhere as `dueDate < now && status !== PAID` (`isOverdue()` in `src/lib/queries/finance.ts`, and inline in `StatusPill.tsx`). Storing it would go stale without a cron job.
- **VAT**: `CompanySettings.isVatPayer` flag + a per-line-item `vatRate` (defaults 21%, the standard CZ rate) — the business decision is data, not code. No editing UI for `CompanySettings` yet (Settings, Phase 4) — seeded directly with a real IBAN so the QR code has something to encode.
- **`Currency` enum** (`CZK | EUR | USD`) on `Quote`/`Invoice`, default CZK, user-selectable at creation; converting a quote to an invoice propagates it. No FX conversion anywhere — aggregate figures (Reports, Dashboard, event budget-vs-actual, a Client's "total charged") sum raw amounts across whatever currencies are present and are documented/labeled as CZK, a deliberate simplification per explicit user instruction, not an oversight. The Czech "QR Platba" QR code is skipped for non-CZK invoices since `CC:CZK` is hardcoded into the SPD payment-string spec — generating one would be actively misleading, not just unhelpful.
- **`Client`/`ClientContact`** — added going forward only, never backfilled from existing `Event.companyName`/`companyIco` free-text fields (auto-matching those by name/IČO would be fragile — near-duplicate company names would silently merge under one client). `Event.clientId` is a new nullable FK; Event's own embedded `companyName`/`companyAddress`/`companyIco`/`companyDic` fields are untouched and still what every other screen (PDFs, event detail, quotes/invoices) actually reads from — `clientId` purely powers the Clients section's own event/revenue rollup, so an event that's never explicitly linked just doesn't show up under any client. `canManageClients()` reuses `canManageFinance() || canCreateEvent()` rather than a new fifth permission dimension.
- **"Paid by" on an expense**: defaults to the current user. A non-card-holder can only submit as themselves; a card holder (or Admin/Accountant/Producer) gets a dropdown of all card holders plus themselves. This reconciles brief §5.3 (paid-by limited to card holders) with §2.2 (a Member can add their own expense) — it's a judgment call, not a direct spec quote.
- **Company overhead**: `Expense.eventId` is nullable rather than a fake pseudo-Event row; the new-expense form's event picker has an explicit "Company overhead — not tied to an event" option.
- **`CustomRole`** stores permissions as four tier enums (`EventsAccess`/`FinanceAccess`/`ExpensesAccess`/`SettingsAccess`), deliberately the same granularity the four built-in roles already use — not a generic permission-string/bitmask system. `User.role` (the built-in enum) is never nullable; when `customRoleId` is set, `role` just holds a least-privilege (`MEMBER`) fallback that's ignored, because `resolvePermissions()` in `authz.ts` checks `customRole` first. This avoided a nullable-column migration and means a defensive/missed call site still fails closed rather than open.
- **Invoice "History" panel** (`4c`) is a small, targeted `InvoiceEvent` action log (created/issued/payment-recorded/marked-paid), not a general audit log — that's still out of scope.
- **A running timer is a `TimeEntry` row with `running: true`**, not a separate concept — `startedAt` set, `endedAt`/`minutes` filled in on stop. Enforcing "only one running timer per user" is app-level (the start action stops any existing one first), not a DB constraint. `TimePhase` (Planning/Suppliers/On site/Wrap-up) is a new enum on `TimeEntry`, added specifically because Compare Events' "hours by phase" stacked bar (`5c`) needs it — the brief itself never names these phases, they're inferred from the wireframe's legend.
- **Compare Events' "cost per hour"** is just `event.quotedValue ÷ totalHours` — re-reading the wireframe numbers directly instead of inventing a per-user hourly-rate field. No schema needed for it.
- **"Invite user" (`5d`) creates the account immediately** with a random one-time password shown once in the UI, rather than a real emailed invitation — there's no email system (per Phase 1's auth decision) and the brief's own login model is "issued credentials," which this matches directly. There's no "pending invitation" state as a result — an account is either created or it isn't.
- **`User.lastSeenAt`** is updated on successful login only (not on every request) — cheap approximation of the wireframe's "Last seen" column without hammering the DB on every page load.
- **`Invoice.sentAt`/`lastReminderAt`** are separate nullable timestamps, not folded into the existing `InvoiceEvent` history log alone — the History panel is an append-only feed (good for "what happened, when"), but the Payment State card's "Invoice sent"/"Reminder" rows need the *latest* value at a glance without scanning history, so both live on the row directly. The two `InvoiceEvent` entries (`EMAILED`/`REMINDER_SENT`) still get written too, for the history feed — the timestamp columns and the log entries aren't redundant, they answer different questions.
- **Invoice email templates live on `CompanySettings`** (one shared pair of templates, not per-user or per-event) since there's exactly one company sending invoices — matches the existing `accentColor`/`logoPath`/branding fields' precedent of "one singleton row, Admin/Accountant-editable." Template text is plain `{{token}}` placeholders resolved by simple string replacement (`renderEmailTemplate()`), not a templating library — the token set is small and fixed (7 tokens), so a real template engine would be more machinery than the problem needs.

## Open questions from the brief (still open, will shape later phases)

From brief §8 — chapter 8 in the PDF has more detail on each. Resolved by Phase 2: invoice numbering, VAT handling, partial payments, company overhead. Still open:
- Event budget vs. actual tracking (beyond the simple quoted-value-vs-expenses shown on the event overview).
- Expense approval workflow — the `approved` field and UI hint text exist, nothing enforces it yet.
- Notifications (email/Slack) for milestones, due invoices, expense approvals — no email system exists; "Send by mail" and reminder-email copy in the UI are inert/labelled as such.
- General audit log for edits (not just invoice actions).
- Time tracker → invoicing (hourly rate → invoice line item) — brief explicitly says time is internal-only for now; Compare Events' cost-per-hour is a read-only derived figure, not a step toward this.

## Commands (from `web/`)

- `npm run dev` — dev server
- `npm run build` / `npm run lint` — typecheck + build / lint
- `npx prisma migrate dev` — apply schema changes locally
- `npm run seed:demo` (or `npx prisma db seed`) — wipes and reseeds the demo dataset (`prisma/seed.ts`): 4 users, 7 events, quotes/invoices/expenses/time entries, company settings.
- `npm run seed:empty` — wipes everything and leaves a genuinely blank slate: a single `admin@eventsystem.cz` login (password `changeme123`, same as the demo accounts) and nothing else — no events, quotes, invoices, expenses, time entries, custom roles or company settings.
- `npm run dev:demo` / `npm run dev:empty` — the two above, but each followed by `next dev` in the same command, since running a seed script alone just seeds and exits (it's not a server) — that confused the user into thinking something was broken ("it ends immediately") when really the seed step had just finished doing its one-shot job and there was no second step queued up to keep the process alive. These two scripts are the fix: one command reseeds *and* starts the server, so there's always a persistent process at the end. Plain `npm run dev` is unchanged — starts the server against whatever data already happens to be in `dev.db`, without touching it. **The dev DB is currently in the empty state** (as of 2026-08-17) — don't assume the demo dataset described elsewhere in this file is present; run `npm run seed:demo` (or `dev:demo`) first if a task needs it.
